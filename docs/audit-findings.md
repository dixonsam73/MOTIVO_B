# Études — Audit Finding Register

From a four-phase client audit and a Supabase backend audit at commit `ec2f52f`,
plus the migration baseline builds. The audit itself compiled nothing except at
migration — findings marked *(build)* came from the Release compile.

**Severity:** P0 critical · P1 release blocker · P2 important · P3 improvement.

**State** — deliberately separate from severity, and the column that moves as
work lands:

| State | Meaning |
|---|---|
| **Confirmed defect** | The code is wrong today. Evidence is in hand. |
| **Unverified** | Suspected, mechanism plausible, not yet established. The cell says what would settle it. |
| **Confirmed gap** | Factually true and worth tracking, but not a defect in behaviour (coverage, tooling, process). |
| **Product decision settled** | We know what the correct behaviour is. The code does not do it yet. |
| **Resolved** | The implementation now matches the decision, and that has been verified. |

The distinction between the last two is the point of this register. An item
moves to **Resolved** only when code has changed and been checked — never
because a design was agreed. C-23 and C-24 are Resolved; both were filed during
Phase 1 rather than by the audit. C-1 is the first audit finding to move, and it
is Resolved **for its client scope only** — the server half is tracked separately
as C-26. Where a state is qualified like that, read the cell rather than the
word.

---

## Client findings

| ID | Finding | Sev | State | Phase |
|---|---|---|---|---|
| C-13 | **Unterminating loop in the purchase path.** `ConnectedMembershipStore:236` — `if let entitlementRefreshTask` shadows with the unwrapped non-optional, so `while entitlementRefreshTask != nil` is always true. Entered only when a refresh is already in flight; reachable after purchase (`:131`), restore (`:199`) and transaction updates (`:225`). A foreground refresh triggered by the StoreKit sheet dismissing is a plausible and possibly common trigger. Consequence: `purchase()` never returns, `isPurchasing` never resets, the user pays and never reaches sign-in. | **P1** (possibly P0 depending on trigger rate) | **Confirmed defect** *(build)*. Originally filed P3 "busy-wait, terminates in practice" — that was a misread. Fix landed on `feature/solo-connected` (`fix: remove the unterminating poll in refreshEntitlement`). **Resolved** — QA B2 run on TestFlight build 129 against **real StoreKit**: a Monthly purchase completed through to sign-in with no hang, the first time that path has ever executed against Apple rather than the synthetic configuration. **Scope of the evidence, stated precisely:** one purchase, not the throttled repeats B2 specifies; and the *hazard condition* was never present. The old loop was entered only when a forced refresh ran while another was in flight, and across nine device runs every forced refresh logged `inFlight=false`. So the result is consistent both with the fix working and with the race never arising — it confirms the purchase path works, not that the fix averted a hang on this run. The loop itself is provably gone from the source and build-verified, which is what closes the finding | **1, first** |
| C-23 | **Ownership race in `refreshEntitlement`'s owner tail.** `ConnectedMembershipStore:271-275` cleared `entitlementRefreshTask` unconditionally after awaiting its own task. Once C-13's fix made the forced re-entry reachable, a waiter that resumed before the owner could install a newer task and have the owner then nil it — leaving a live refresh unregistered (so a later caller starts a duplicate), `isRefreshingEntitlement` false while one is in flight, and a stale `membershipState` published over a newer one. **Not present in any build:** unreachable before the C-13 fix, because the poll meant the forced re-entry never ran. Closed by an identity guard in the same commit | P2 | **Resolved** — introduced and closed within the C-13 fix; build-verified. **Never exercised at runtime:** an earlier version of this cell claimed B2 would cover it incidentally, and it did not. `refresh.owner.superseded` has not fired once in nine device runs, synthetic or real, because the forced re-entry it guards has itself never executed. The guard is correct by inspection and costs nothing; it has simply never been reached | 1 |
| C-38 | **A successful purchase reports failure, because a single immediate read of `currentEntitlements` is treated as authoritative.** `purchase()` (`ConnectedMembershipStore:129-138`) finishes the verified transaction, refreshes, and gates on `guard isEntitled`. `Transaction.currentEntitlements` is an **async aggregation with no guarantee of reflecting a transaction that has only just been finished**, so that read can legitimately return `.notEntitled` for a purchase that succeeded — raising "Purchase unavailable — The purchase was verified, but no active Études Connected entitlement was found." (`:43`, `MembershipSelectionView:274`). **The purchase is not lost.** The entitlement is granted and Connected activates on the next foreground, because `MOTIVOApp:257` refreshes on every `scenePhase == .active`. So the defect is the *report*, not the transaction — but the user has paid and been told it failed, and the recovery is a background-and-foreground gesture nobody would guess. The natural response to that alert is to purchase again or seek a refund. **This is precisely the failure QA B2 names** ("watch for a wrong entitlement state after a verified purchase") and it is now observed rather than anticipated. **Relation to C-1:** it disproves that finding's corollary, "`guard isEntitled` (`:133`) is reliable only because a second read follows" — a second read is not what makes it reliable, since *any* number of immediate reads can precede the aggregation settling. C-1's own evidence says so: only the refresh **moments later** published `entitled`. **`Transaction.updates` (`:214`) is not a dependable backstop** — StoreKit 2 does not generally re-deliver a transaction already returned by `product.purchase()` (reasoned from the API contract, not observed). **MECHANISM CORRECTED 2026-08-11.** This row first said the defect was `refreshEntitlement(forceAfterCurrent:)` ignoring its parameter when no refresh is in flight. **That was wrong.** The parameter means "do not accept a result from a refresh already running; get one that starts after this call", and both paths honour it — with nothing in flight, starting a fresh read immediately already satisfies the contract, so the parameter is *redundant* there, not broken. The proposed hoist would have performed two back-to-back reads: a fix by luck that would have looked verified the first time it passed. **The lag is in StoreKit, not in the refresh plumbing.** | **P1?** | **Unverified — suspected synthetic StoreKit configuration artefact; real-StoreKit reproduction pending.** Downgraded from *Confirmed defect* on 2026-08-11 after a day of instrumented testing failed to reproduce it on Apple's own pipeline. **Evidence it can occur:** one device observation, on device A under the synthetic configuration (`[Environment: Xcode]`, C-9), after clearing transactions in the Transaction Manager and re-purchasing. That observation stands and is not retracted — the synthetic environment demonstrably can produce the false-failure state. **Evidence it may not be real:** QA B2 on TestFlight build 129 did not reproduce it; and two instrumented TestFlight runs on 2026-08-11 (17:03 and 17:21) both resolved `entitlementRead resolved=entitled forced=false wasInFlight=false` within a second of launch, with activation proceeding normally to Connected in both. No lag was observed on real StoreKit at any point. **Two causal explanations have been offered and both withdrawn** — first that `forceAfterCurrent` was ignored when no refresh was in flight (wrong: the parameter is redundant in that case, not broken), then that publishing `.entitled` early consumed a `removeDuplicates` transition that could never fire again (wrong: the instrumentation shows that publisher re-emitting on every body evaluation). **A fix was attempted and reverted (`78edac7`)** because it caused a real activation regression — the app reached a live entitlement and stayed in Solo permanently — and the cause of *that* regression is also still unknown. **THE BOUNDED ATTEMPT WAS RUN ON 2026-08-11 AND DID NOT REPRODUCE IT. CLOSED.** The register has no state meaning "closed without reproduction", so this stays **Unverified** — but the attempt is spent and the stopping rule agreed in advance applies: no further hypotheses are manufactured. Device B was never used. **What was run:** a fresh Sandbox Apple Account on device A, old sandbox state cleared, the app deleted and reinstalled from Xcode as a development build with the Run action on Release and StoreKit Configuration cleared to None. Precondition established from the trace, not the screen — `entitlementRead resolved=notEntitled forced=false wasInFlight=false`, repeatedly, before any purchase. Environment established from Apple's own payment sheet, headed **"Sandbox"** with "For testing purposes only" and an Account line, not the synthetic `[Environment: Xcode]` sheet. **Result:** `entitlementRead resolved=entitled forced=true wasInFlight=false` → `membershipState notEntitled -> entitled changed=true` → a second `entitlementRead resolved=entitled forced=true wasInFlight=false` → **`purchaseGuard isEntitled=true`** → SIWA → `resolved=connected`. **The first read after the verified transaction already resolved `entitled`. There was no stale-read window at all**, so the hypothesised lag did not merely fail to matter — it did not occur. **The two `forced=true wasInFlight=false` reads are `purchase()` (`:142`) and the `Transaction.updates` handler (`:239`), both taking the direct owner path with no contention**, so the waiter-path window this cell flagged as C-38's untested shape did not arise on the purchase path either. That window is real — both launches in this session showed it, as a `membershipState` publish with no `entitlementRead` of its own (the waiter publishes at `:250` and never logs) followed by the owner's read — but it arises between `start()` and the `scenePhase` refresh, not around a purchase. **Environment caveat, stated rather than glossed:** a development build on sandbox is a third environment, not TestFlight's distribution build on sandbox. The StoreKit machinery is identical and the attached debugger slows execution, which makes a timing race *more* likely to surface, not less — so a non-reproduction here is meaningful evidence, though not proof about TestFlight specifically. **The single synthetic observation stands and is not retracted.** It has now failed to recur across four real-StoreKit runs (QA B2 on build 129, two instrumented TestFlight runs, and this one). Reopens only on a real observation; QA B2 is where that would surface, and its note about treating the alert as the failure stays exactly as written | 1 |
| C-24 | **Reconnect after reinstall never completes.** Fresh install of an existing Connected member. The Keychain survives app deletion, so `AuthManager:198/210-220` restores `appleUserID` and the Supabase access token at init and re-derives `backendUserID` from the JWT subject — the app is already "signed in" at first launch. SIWA then writes the same stable Apple user ID (`AuthManager:876`), so `currentUserID` does not change, and every completion path keyed on that transition fails to run: `ProfileView:1621` never dismisses the SIWA sheet or unwinds the navigation stack, and `MOTIVOApp:291/306` (`removeDuplicates`) never re-resolves the app mode. `shouldSuppressSignedInProfileAfterGateSignIn` (`ProfileView:300`) then renders `Color.clear` permanently — the blank screen with only a back chevron — because its only reset requires `currentUserID == nil`. Device-observed on Release: sheet will not dismiss, user is returned to Solo with the Connected account intact on Supabase. **Not C-1 and not C-13**; independent of StoreKit. Note for C-1: the expiry-cleanup gate (`isSignedIn` + token + `backendUserID`) is Keychain-satisfied from the first millisecond of a reinstall, so only a missing entitlement transition stands between a reinstall and the deletion path | **P1** | **Resolved** — fixed at `29828b6`, device-verified on Release. Cause was device-confirmed first: `auth.siwa.applied changed=false` with no `profile.currentUserID.changed` ever emitted, in four runs; Connected was reached only because the *entitlement* transition drove activation, and the user escaped the stuck UI by swiping the sheet away and pressing the back chevron. Verified after the fix in both entitlement states — without an entitlement (sheet dismisses, lands in Solo, correctly) and through a purchase (unwinds into Connected), both logging `auth.signIn.completed` followed by `profile.signInCompleted`. Note the environment: verified under the synthetic StoreKit configuration, which is sufficient here because the defect and its fix are independent of StoreKit. **The full journey is now verified too.** On TestFlight build 129 with real StoreKit, a purchased entitlement survived app deletion — entitlements are account-bound, unlike the synthetic configuration where deleting the app destroys its transactions (C-9). That put the device into the exact state the register had recorded as untestable: existing member, live entitlement, fresh install. Reinstalling routed correctly past membership selection to sign-in, and sign-in completed cleanly into Connected with no manual swipe or back-chevron | 1 |
| C-1 | Entitlement resolution cannot express "indeterminate"; one negative read triggers irreversible backend deletion. `ConnectedMembershipStore:230-267` → `MOTIVOApp:339/377` | P0 | **Resolved (client authority).** Device evidence (Release, instrumented, 2026-08-09), reproduced in three independent runs: a forced refresh fired by `transactionUpdate.verified` immediately after a *verified purchase* published `notEntitled`; only the second refresh moments later published `entitled`. A negative read can therefore be spurious rather than authoritative — previously argued from structure, now observed. Corollary: `purchase()`'s `guard isEntitled` (`:133`) is reliable only because a second read follows. **That corollary is FALSE — see C-38.** A second read is not what makes the guard reliable: `Transaction.currentEntitlements` is an async aggregation that may not yet reflect a just-finished transaction, so any number of immediate reads can precede it settling. The evidence recorded here says as much — only the refresh **moments later** published `entitled`. The observation stands; the reassurance drawn from it did not. **That corollary is now known to be FALSE — see C-38.** `forceAfterCurrent` is consulted only when a refresh is already in flight, so on the ordinary purchase path there is exactly one read and no second read follows. The observation above stands; the reassurance drawn from it did not. **Environment:** both observations were under the synthetic StoreKit configuration, not real StoreKit (C-9), so the *frequency* under real conditions is still unknown — the mechanism is established, the rate is not. **Fixed at `6109116`:** the detect → arm → gate → delete pipeline is gone, `handleMembershipState` does nothing but `applyActivation`, and no client entitlement path can reach `deleteCurrentConnectedAccount`. **Device-verified on a genuine subscription lapse**, not a forced deletion: the entitlement expired at term end, the app published `notEntitled` and dropped to Solo, Connected credentials and identity survived untouched (`auth.init appleID=true backendID=true token=true` on relaunch), and the Supabase account survived — proven by Erase All Études Data subsequently finding and deleting a live account. Re-subscribing restored Connected unaided. The lapse was observed across a force-quit and relaunch, which also satisfies C4. No indeterminate state was added and none is needed: without deletion a spurious negative read only withdraws access, which invariant 3 permits because it is reversible. **RE-VERIFIED ON THE DISTRIBUTION ARTIFACT, 2026-08-13** — TestFlight build 131, Device B, on a lapse engineered by cancelling the sandbox subscription the previous day so it expired on a known date. The app resolved to Solo and **did not require Sign in with Apple**: identity survived the expiry untouched, and Profile name, avatar and location were all present. **The backend was read twice, once before the device foregrounded and once after it had run** — every value identical: `auth.users` 16, directory 16, `posts` 99, `follows` 8, `post_comments` 4, `connected_attachments` 35, storage objects 11 and 4, and `auth.users.created_at` unchanged at `2026-07-23 13:42:16Z`. **Nothing was deleted, on the artifact beta testers install.** **One half of this row is NOT covered by that run, and a green result must not be read as if it were.** This cell also claims the local journal, Scores and media survive expiry. Device B had none to lose — B5 had deleted the Release container the previous day, and the two practice sessions recorded since went into Études Dev, a separate container. Profile identity showing is also consistent with re-hydration from `account_directory` rather than local preservation, and the run does not discriminate. **Local durability across expiry remains verified only by the 2026-08-09 development-build run.** **Scope: this closes the client authority only.** Server-side authority and the interim retention gap are tracked as C-26 | 1 |
| C-26 | **No server-side membership authority yet.** C-1 removed the client's ability to act on expiry. App Store Server Notifications V2 — the sole authority for membership-expiry cleanup under the settled architecture — are Phase 3 and do not exist. **Interim consequence: no membership-expiry cleanup happens at all. A lapsed member's Supabase account, posts, attachment references and storage objects are retained indefinitely.** Accepted deliberately: retention pending authoritative evidence beats deletion on client evidence now observed to be wrong in three separate runs. Closing this is the Phase 3 scope already planned — server-side entitlement state, ASSN ingestion, Billing Grace, replay protection, idempotent processing — and QA Group C rows C5–C10 are already written against it | **P1** | **Confirmed gap** — scheduled, not missing by accident. Becomes a defect only if the release ships without it. **The retention half is now OBSERVED rather than argued, 2026-08-13**: a genuine lapse on TestFlight build 131 (Device B) deleted nothing. Backend read before the device foregrounded and again after it had run, both identical to a baseline recorded while still entitled — `auth.users`, `account_directory`, `posts`, `follows`, `post_comments`, `connected_attachments` and both storage buckets all unmoved, with the account's `created_at` unchanged. So a lapsed member's data is retained exactly as this row says, and the interim consequence is real rather than theoretical. **This closes nothing.** The gap is the *absence of server-side authority*, not the retention behaviour, and that authority is still Phase 3 work. **EXPIRY SEMANTICS SETTLED 2026-08-16, AND THEY ARE EXPLICITLY DISTINCT FROM ACCOUNT DELETION — recorded here so the next reader does not re-derive the conflation from the same documents.** Ordinary expiry **retains** the lapsed member's comments on other members' surviving posts, their sent attachments while a live recipient reference remains, the `account_directory` row (undiscoverable, `display_name` intact) and `auth.users`; explicit account deletion removes all four. The 2026-08-13 revision that made a departing member's own UGC deletable was scoped to *account deletion* by its own text and justified by Apple's *account-deletion* guidance; `docs/architecture.md`'s "On expiry" table was untouched by it and is **not** superseded. QA C7's 2026-08-14 amendment asserting that expiry "must match `delete_account_v1`'s deployed semantics" was an unsupported inference and is **withdrawn**. What this row closes on is unchanged: server-side authority, fed by verified notifications, with cleanup scheduled behind a 60-day quarantine and executed only after a **live** authoritative Apple read. **U5's EVIDENCE MODEL WAS CORRECTED BEFORE IMPLEMENTATION, 2026-08-16 — see B-24. Read the correction precisely, because it is narrow.** What is superseded is only the wording that the server *"ignores"* the client artefact. What survives unchanged is that **client data is never authoritative for current entitlement**: the server still obtains current entitlement state **directly from Apple**, and no client assertion grants or revokes anything. What is added is that the **Apple-signed transaction JWS, after server-side signature verification, is evidence of possession and therefore of ownership** — it can only come from `currentEntitlements` on a device signed in as the owning Apple Account. The old wording discarded that, which is what left ownership unproven. So the corrected model is: **JWS = who, live Apple read = now, `appAccountToken` = durable binding.** | 3 |
| C-29 | **The app cannot vend its subscriptions — App Store Connect was never fully configured.** Build 129 on TestFlight showed "Membership options are unavailable" with no error beneath it, which is diagnostic: `MembershipSelectionView:84-92` only renders `productLoadError` when the call throws, so `Product.products(for:)` returned an **empty array**. That is the signature of identifiers the App Store will not vend, not a client fault. Observed gaps: Paid Applications Agreement not Active; app-level Price Schedule *and* App Availability both unset; the Monthly subscription had no Availability configured while Annual had "all countries or regions"; both subscriptions in "Prepare for Submission". Everything else was correct — product IDs matching the code exactly, durations, per-subscription and group localisations, prices across 175 territories — which is precisely why it looked configured. **This is C-9 realised.** Every StoreKit test before this used the local `Etudes.storekit` configuration, which defines products client-side and therefore cannot surface a server-side gap; the synthetic config masks this every time. Blocks QA B1/B2 and blocks release | **P1** | **RESOLVED** *(environment, not code)*. Remediation was: bank and tax forms completed, agreement moved to Processing 2026-08-10. **Resolved.** Cause was the three App Store Connect gaps, not the agreement alone: app-level Price Schedule unset, App Availability unset, and Monthly with no Availability while Annual had all territories. Once all three were configured — price schedule £0.00 across 175 regions with a UK base, 175 available, Monthly availability matched to Annual — build 129 loaded the products with **no code change and no new build**, product loading being a live query. Note that propagation is not immediate: the products did not appear straight after the changes were saved. **Settled so it is not re-litigated:** submission for review is *not* required for sandbox or TestFlight. Apple DTS on this exact symptom — "Testing In-App Purchase in the Apple sandbox environment doesn't require you submit your In-App Purchases for review. TestFlight uses the sandbox for In-App Purchases" — and TN3186 lists the real prerequisites, which are pricing, availability, territory match and an active Paid Applications Agreement. The App Store Connect banner about the first subscription needing an app version is about release, not testing; community reports blaming review status are misattributed configuration failures | 1 |
| C-32 | **"About Études" and "Explore Connected" copy needs a joint rewrite before release.** The two sit adjacent in Profile — `ProfileView:741` and `:753` — and are read in sequence by anyone deciding whether to subscribe, but they were written at different times and have never been reviewed against each other. Both must also describe the app that actually ships, which has changed materially during Phase 1 and will change again in Phase 4. Constraints already settled and easy to violate: Connected is **not** "private by default" — "Share with followers" defaults ON, deliberately, with a persistent user preference, and the copy must say so accurately rather than implying the opposite. Phase 4 will alter what is uploaded (shared-only, analytics mirror removed), so the copy cannot be finalised before that lands. Files: `ConnectedIntroductionView.swift`, `MeView.swift`, and the About rows in `ProfileView.swift`. Related: Phase 4 already owns aligning onboarding, settings and App Store privacy disclosures — this is the customer-facing half of the same job | P2 | **Confirmed gap** — content, not code. Cannot be completed before Phase 4 | 4 / RC |
| C-30 | **Both subscriptions carry the same customer-facing display name.** In App Store Connect each subscription's localisation display name is "Études Connected", so Apple's own subscription UI shows the plans as two identically-named entries distinguished only by price. Visible in the real manage-subscription sheet, where the group reads "Études / Études Connected" and both plans sit behind *See All Plans* under the same name. **Masked during development:** StoreKit Test's simulated `Edit Subscription [Xcode]` sheet auto-appends "(1 month)" and "(1 year)", so the ambiguity only appears once real StoreKit is used — the same class of masking as C-9 and C-29. The *reference* names already distinguish them; the customer-facing localisations do not. Fix is App Store Connect only, no code | P2 | **Confirmed gap** — configuration, observed on TestFlight build 129 | RC |
| C-31 | **Billing Grace Period is not enabled in App Store Connect.** The settled architecture assumes Apple absorbs renewal failures via Billing Grace, so a failed payment should retain the entitlement while Apple retries rather than expiring it immediately. With it disabled, that assumption does not hold at runtime. Two consequences: QA C6 ("Failed payment on renewal — entitlement retained throughout grace") is not testable, and Phase 3's server-side authority work lists Billing Grace among the states it must ingest, so the notification stream will never carry them. No implementation impact on the client, which reads entitlement rather than reasoning about renewal state | P2 | **Confirmed gap** — configuration. Prerequisite for Phase 3 and for QA C6 | 3 / RC — **Sandbox-first, 2026-08-16.** Enable Billing Grace for **Only Sandbox Environment** first (App Store Connect offers exactly that option), configure the sandbox notification URL, implement and observe, and promote to **Production and Sandbox** only after handling is accepted. **C-31 closes at that promotion, not at the sandbox step.** The earlier plan to enable production Grace as step zero was riskier and less useful than necessary |
| C-28 | **Received Connected attachments survive "Erase All Études Data".** `ReceivedConnectedAttachmentStore` (`ConnectedAttachmentSharing:379`) is a singleton owning `Application Support/ReceivedConnectedAttachments/` and has no factory-reset hook. `LocalFactoryReset` wipes only `App Support/MOTIVO/*` (Staging, PracticeTimer, SessionSyncQueue, AttachmentPrivacy) and `App Support/Profiles`, so the directory is never touched **[STRUCTURE CORRECTED 2026-08-15 — the description above is the state at audit time and is preserved because it is what the finding was reasoned from; it is no longer the current layout. Phase 2 U4 moved `AttachmentPrivacy.json` OUT of `App Support/MOTIVO/` to the Application Support ROOT, because permanent user intent must participate in backup and a child of an excluded directory cannot be exempted in place. The erase remains correct — `AttachmentPrivacy.wipeOnDiskAndCacheForFactoryReset()` is an explicit call that follows `fileURL()` to the new location and also sweeps the legacy path — but the map is now a root-level permanent file, which is structurally the same shape as C-48's `CommentsStore.json`. This row's acceptance meaning and its 2026-08-14 PASS are unchanged; only the filesystem description is corrected. Phase 2 carries a mandatory erase-regression gate covering the new location — see QA Group D, 'Phase 2 erase-regression gate'.]** — media other people shared with the user remains on the device after an explicit full erase. Same defect class as C-2 (a store owning files with no reset counterpart) but deliberately **not** folded into it: the content was authored by other people, so what Erase All should do with it is a product decision as well as an implementation gap. Note the backend rule already settled — attachments a departing member *sent* survive for existing recipients — says nothing about the departing member's own local copies of what they *received* | **P1** | **RESOLVED 2026-08-14 — device-verified end to end against a prediction committed beforehand.** Product decision settled 2026-08-13; fix landed `579f3cd`, logging `ea0beb2`, prediction committed `995603f`. **The rule: Erase All erases everything Études itself owns or manages locally on that device, regardless of provenance — and reaches nothing the user has explicitly exported into system-owned locations (Files, Photos), which are no longer exclusively ours.** Received Connected media inside Études-managed storage is therefore deleted like any local media, and an asset adopted into Scores goes with Scores. **Storage boundary established by reading writers rather than path references** — several views *resolve* attachments from Caches and Library, but nothing writes media there, and remote feed attachments are streamed through short-TTL signed URLs and never persisted. Three durable homes exist: `App Support/ReceivedConnectedAttachments/` (this defect), `Documents/Scores/` (already wiped since C-2), and `tmp/` export staging. **AN ADOPTED ATTACHMENT EXISTS TWICE, AND THAT IS WHAT MADE THIS LOOK FIXED.** `ScoreLibraryStore.importPDF:67` **copies** rather than references, so adopting a received PDF creates a second file; the erase killed the Scores copy and spared the inbox original, leaving a user who checks Scores with every reason to believe the wipe worked. **A SECOND MISS WAS FOUND IN THE SAME SWEEP AND IS FIXED WITH IT:** `LocalFactoryReset.wipeTemporaryMediaArtifactsBestEffort` carried its **own, shorter** extension literal — no `pdf`, no `wav/aif/aiff/caf/heic/m4v/dat` — while the Documents sweep covered all of them, so a PDF left in `tmp/` outlived the reset. **THE ROUTE ORIGINALLY GIVEN FOR THAT WAS WRONG AND IS CORRECTED HERE; THE CONCLUSION SURVIVES.** This cell first said "Save to Files copies into `tmp/` before presenting the exporter (`ConnectedAttachmentShareUI:868`)", and QA could not stage it. **`saveToFiles()` is unreachable for a PDF:** the received-attachment detail view offers exactly one adoption action, dispatched by kind (`:755`) — PDF goes to **Scores**, image and video to **Photos**, audio to **Files**, and `.file` gets none. So only an *audio* attachment can reach `saveToFiles()`, and `.m4a` was already in the old extension set, which means that route could never have demonstrated the gap even where it does apply. The tester's export went through the native share sheet, which stages nothing in the app container — confirmed on device, where `tmp/`'s mtime was unchanged by it. **THE REAL UNCLEANED WRITER, VERIFIED IN SOURCE:** `ConnectedAttachmentShareUI:488` — **sending a Score with a page selection to another Connected member** — exports a subset to `tmp/` via `PDFSubsetExporter` and **never removes it**; the only `removeItem` in that file belongs to `saveToFiles()` and does something else. Its sibling `AttachmentViewerView:406` exports the same way but **does** clean up (`registerTemp` → `cleanupTempFiles`, plus an explicit remove at `:472-479`), which is why it is not the route either. So a reachable supported path does leave a `.pdf` in `tmp/` permanently, and the fix is right — but the reason first given for it was not, and the reason is what the next person implements from (D14). Two lists that must agree with no reason to differ is a drift defect rather than a bug; the fix promotes one authoritative set (`AttachmentStore.factoryResetSweepExtensions`) and deletes the duplicate, which removes the class instead of re-aligning the instance. **CONVERGENCE CONFIRMED FROM SOURCE:** `ProfileView:1671` (Solo, gated on `!auth.hasConnectedIdentity`) and `:1744` (Connected, after revocation and `delete_account_v1`) call the **same** `LocalFactoryReset.perform`, differing only in the `reason` string, and nothing else calls it — so the coverage fix lands on both destructive paths at once and no naming, copy or DELETE-vs-ERASE distinction is touched. The Solo branch genuinely reaches this state: after C-45 credential withdrawal, identity is cleared while received files remain. **`C-48` was filed separately from the same investigation** — `CommentsStore.json` at the Application Support root — and is fixed in this unit for the same reason, but is not this row. **Verification note that decides how QA is run:** `items` is in-memory and empty after a reset either way, so **the app's own screens look identical whether the fix works or not. The container is the verdict, not the screen** — the same trap as D13 and C-34. **RUN EXECUTED 2026-08-14 ON DEVICE A, RELEASE, AND EVERY ASSERTION PASSED.** Container afterwards: `ReceivedConnectedAttachments/` **absent**, `Documents/Scores/` **gone**, both local `Documents` media **absent**, the `tmp/` export copy **absent** — leaving an empty `Documents/`, empty `MOTIVO/` and `Profiles/`, `tmp/TemporaryItems`, and a rebuilt Core Data store. **All three new assertions would have failed on the pre-fix build.** **The Release log carried what the filesystem cannot:** `[C-28] localReset receivedAttachmentsRemoved=2 temporaryFilesRemoved=1`, emitted after `[C-44] revocation … outcome=revoked` and after `LocalFactoryReset begin`, matching the committed prediction exactly — **including `temporaryFilesRemoved=1` rather than 2**, because `tmp/TemporaryItems` is a directory with no path extension and cannot match the sweep. That count is what separates "the wipe ran" from "the wipe threw" and from "this build predates the wipe", none of which an empty directory could distinguish. **Backend 9/9 against prediction:** `auth.users` 16→15, directory 16→15, `connected_attachments` 33→31, `follows` 8→6, with `posts` 98, `post_comments` 2, `post_comment_views` 9 and `avatars` 3 unchanged; A zero on every per-account measure; B intact at 4 posts, its directory row and 8 objects. **`attachments` objects stayed at 10, which was predicted:** both received objects sit under the *sender's* prefix while A's sweep is scoped to `users/<A>/`, so they survive unreferenced — D14's expected residue, now +2 on **B-8**'s Phase 4 pile. A count of 8 would have meant the function reached outside the departing member's prefix. **Both overreach checks held — the PDF exported to Files and the photo saved to Photos both survived** — so the settled boundary is verified in both directions, not only the deleting one. **One deviation, filed separately as C-49:** the app landed on the journal rather than first-launch onboarding. Navigation state only; no data implication, and it clears on relaunch | 1 |
| C-35 | **"Erase All Études Data" in Études mode leaves the entire Connected account, avatar included.** `ProfileView:1566` short-circuits to a local-only reset whenever `canShowConnectedAccountManagement` is false, and that is only `mode == .connected` (`AppModeManager:109`), while mode falls back to `.solo` whenever `isEntitled` is false (`:119-129`). So a **lapsed member still holding a full backend account** — directory row, posts, follows, avatar — gets a local wipe and nothing else. The reset then clears the Keychain, so the app offers no route back to the deletion path unless they re-subscribe. The solo-mode sheet copy (`ProfileView:1489`) does not claim backend deletion, so nothing dishonest is shown; the gap is that there is no way to ask for it. **Read alongside C-26**, which accepts indefinite retention of a lapsed member's Domain 3 data pending server authority. Together they mean that today a lapsed member's remote data can be deleted by nobody — not by the server, which has no authority yet, and not by the user, who cannot reach the control. **Check before release:** App Store Review Guideline 5.1.1(v) requires an app offering account creation to offer account deletion; whether gating that control on an active subscription satisfies it is a question for review rather than for this register, but it is the kind of thing that returns a rejection. The decision needed: should Erase All in Solo attempt backend deletion whenever Connected credentials survive? The settled decisions already class an explicit user-triggered erase as a valid client-initiated destructive action, so extending it is consistent with invariant 3. **DECIDED 2026-08-13: yes.** Deletion authority is now `auth.hasConnectedIdentity` — signed in, a stored Supabase token, a resolved backend user id — and is deliberately entitlement-free. Membership governs access to Connected and never whether an existing account can be deleted. Landed at `079ca5e`. **THERE WERE TWO ENTITLEMENT GATES, NOT ONE, AND THE SECOND IS THE MORE INSTRUCTIVE.** The first was direct (`canShowConnectedAccountManagement`) and fell to the fix. The second was `BackendEnvironment.shared.isConnected` — a *runtime service-selection* mode, set by `AppModeManager.applyBackendRuntimeMode`, which writes `.localSimulation` for `.solo` and `.backendConnected` for `.connected`, with `AppMode` resolved from `isEntitled`. So a lapse silently flipped a `UserDefaults` key and the deletion request was refused with *"Études Connected is not currently available. Please try again"* — a retry that could never succeed. **The guard was also testing the wrong thing entirely:** `deleteCurrentConnectedAccount` builds its own `URLRequest` against `BackendConfig.apiBaseURL` via `URLSession` and never touches `BackendEnvironment`. Replaced with `BackendConfig.isConfigured`, which is independent of `AppMode` and is the request's actual precondition. **THE LESSON GENERALISES AND IS WORTH MORE THAN THE FIX.** The deletion path was audited structurally for `isEntitled`, `AppMode` and `canShowConnectedAccountManagement`, and passed — because it contained none of them **by name**. The dependency travelled through a persisted `UserDefaults` key, so no grep for entitlement terms could have found it. **An entitlement dependency can be laundered through persisted state; a structural audit for identifiers cannot see it, and only running the path can.** This was caught by the destructive QA *before* anything was deleted, which is the whole argument for staging a fixture and pressing the button rather than reasoning about it | **P1** | **RESOLVED 2026-08-13** — decided, implemented and device-verified. A lapsed member holding a live backend account can now delete it without re-subscribing, proved on Device A: deleted while in Solo, no renewal at any point, and every count matched a prediction written before the erase. Two entitlement gates had to fall, not one — see the cell for the second, which a structural audit could not see | 1 |
| C-33 | **The avatar object delete is fired and forgotten, and can strand the object with no pointer to it.** `ProfileView:1434` and `AuthManager:272` both `_ = await NetworkManager.shared.deleteAvatarObject(...)` and then patch `account_directory.avatar_key = nil` regardless of the outcome. A failed or offline object delete followed by a successful patch leaves `avatars/users/<uid>/avatar.jpg` present with nothing pointing at it, and that state is unrecoverable from the client because every subsequent path keys off the column now set to null. This is what turns B-20 from a theoretical gap into a reachable one, since `delete_account_v1` deletes the avatar by reading that same column. Second route into the same state: `ProfileView:1399-1418` uploads successfully and then fails the directory patch, leaving bytes with a null pointer on a first-ever avatar — the pending marker usually repairs it on retry, but Erase All can land first. Same class as C-21 (discarded results in `NetworkManager`), with retained personal data as the consequence rather than a lost diagnostic. **Note for whoever fixes this:** `clearAvatarFromBackendIfPossible` (`ProfileView:1424`) has no callers — the live delete path is the pending-marker route at `AuthManager:269-287` — so there are two implementations of one operation and the dead one disguises which is real. Delete it with the fix rather than repairing both | P2 | **RESOLVED 2026-08-14 — structural fix, build-verified; the runtime failure path is recorded as UNEXERCISED rather than dressed up.** **One correction to this row, found by reading the source before changing it:** it names two live call sites, but `ProfileView`'s `deleteAvatarObject` call sat **inside** `clearAvatarFromBackendIfPossible` — the dead function — so there was only ever **one** live path, and deleting the duplicate removed the other site rather than needing its own repair. **The fix is one branch, and it makes that branch match its own neighbour.** `AuthManager.syncPendingLocalAvatarToConnectedIfNeeded`'s deletion branch discarded the object-delete result and patched `avatar_key = nil` regardless; the **upload branch three lines below already checked its results and retained the pending marker on failure**. The deletion branch now does the same. **Ordering is the substance: the pointer is the only handle on the object, so it must outlive any failure to delete what it points at.** Prove the object gone, then drop the pointer; on anything else retain the marker and retry. **One judgement call, stated rather than buried.** If the object is *already* absent there is nothing to strand, and refusing to clear the column would leave the member's deletion never propagating to peers — worse than the orphan this prevents. So `avatarObjectIsAbsent(after:)` treats "not there" as success. **The exact response Supabase Storage returns for a DELETE of a missing object is NOT established**, so the body check is a deliberate superset and the code says so; transport failures, 401/403 and 5xx all retain, which is the safe direction. **The dead duplicate is deleted, not repaired** — repairing both would have preserved exactly the ambiguity that hid the defect. Its four helper symbols were checked first and all remain in use by the live upload path, so nothing cascaded. **WHAT IS NOT VERIFIED, AND NO FAULT INJECTION WAS INVENTED FOR IT:** the failure path itself — a failed object delete followed by a successful directory patch — has never been executed. Producing it needs the same fault-injection capability D10 lacks, and manufacturing one for a P2 would be disproportionate. Debug and Release compile clean; the branch is identical in shape to the one beside it. Settled properly whenever a disposable or local backend exists | 1 |
| C-34 | **A replaced avatar never reaches anyone else.** The storage half is correct: `NetworkManager:478` uploads to the fixed key `users/<uid>/avatar.jpg` with `x-upsert: true`, and the RLS insert and update policies pin the name to exactly that, so a replacement overwrites in place and no stale duplicate can accumulate. But `avatar_key` is content-invariant, so nothing anywhere can signal that the bytes changed. `RemoteAvatarImageCache` (`NetworkManager:619-642`) is an `NSCache` with no TTL keyed on `avatars\|<key>`, and only the owner's own device invalidates (`ProfileView:1451`, `AuthManager:327`). Every other member's device therefore serves the previous image, across eight render sites — feed rows, People, comments, profile peek, session detail, and both toolbars. Worse on the owner's own second device, where it survives relaunch: `ProfileView:995` prefers any local avatar file and returns before consulting the backend, hydration seeded that file from the backend on first sight (`:1056`), and `avatarRefreshTrigger` (`:967`) is keyed on the unchanged `avatar_key`, so there is no signal at all. Same drift class as C-19 and C-27 — a local mirror of backend identity with no invalidation path. Cheap fix: a short TTL on the image cache, so staleness self-heals on the signed-URL timescale. Exact fix: a version signal (`avatar_updated_at` in `account_directory`, folded into the cache key), which is directory plumbing rather than a client change | P2 | **Confirmed defect, but NARROWER than filed** — device-observed on 2026-08-11 during the D5–D12 run, on a non-owner device. **A replacement does propagate to `ProfilePeekView` and `BackendSessionDetailView`, after a delay and with no relaunch.** Only the feed row (`RemotePostRowTwin` → `DirectoryAvatarCircle`) stays stale for the process lifetime and needs a force-quit. The original cell claimed the previous image was served at every site until relaunch; that is wrong, and the correction matters because it changes what the fix has to do. **Mechanism, read from source after the observation:** `ProfilePeekView:538` and `DirectoryAvatarCircle:571` are structurally identical — same `@State`, same branch order, same `.task(id: key)` on the whole `Group` — so the difference is not logic but **view lifetime**. The sheet and the detail push are recreated per presentation, so `.task` re-runs; once the `NSCache` entry has been evicted (`countLimit = 256`, plus memory-pressure purging) the refetch picks up the new bytes. The feed row is long-lived inside the list, so its `.task` never re-runs and it holds the stale image in `@State` as well as in the shared cache. **So the self-healing is incidental eviction, not an invalidation path** — nondeterministic, unrelated to the signed-URL TTL, and not something to rely on or to mistake for correct behaviour. A TTL on the image cache would make every site behave like the two that appear to work; only the version signal makes any of them correct. Storage half still unobserved — E11 settles it | 5 (TTL) / 4 (version signal, with directory work) |
| C-37 | **The feed row's avatar fetch is attached to the placeholder, so it can never refetch.** `ContentViewSessionRow:531` puts `.task(id: ownerAvatarKey)` **inside the `else` branch** — the initials/placeholder case — rather than on the enclosing `Group`. Once any of the three image branches renders (`:490` local file, `:494` shared cache, `:498` `@State`), the task is not in the view tree at all, so there is no code path that could fetch again for as long as that row exists. This is permanent staleness **by construction**, distinct from C-34's cache policy: C-34 is a cache that never invalidates but is at least re-consulted, whereas here the fetch itself is unreachable. Note the different scope — the branch is gated on `viewerIsOwner` and keyed on `auth.backendAvatarKey`, so this is the **viewer's own** avatar on their own session rows, not another member's. Compare `ProfilePeekView:538` and `DirectoryAvatarCircle:571`, which attach the same task to the whole `Group` and are correct in this respect. Fix is to move the modifier out of the `else` and onto the `Group`, matching the two sites that already do it | P3 | **Unverified** — read from source while establishing C-34's mechanism; not observed on device. Settled by: set an avatar, confirm it renders on your own session row in ContentView, replace it with a visibly different image, and see whether that row updates without a relaunch once the other sites have. Filed separately from C-34 because the fix is a one-line modifier move with no cache or directory work behind it | 5 |
| C-39 | **The feed shows the viewer's own posts with no approved follows — and that contradicts a recorded QA result.** Observed 2026-08-11 on Device B's Release app moments after its first sign-in: ContentView's feed displayed two posts owned by the viewer while `FollowStore` logged `following=0 followers=0 incoming=0 outgoing=4`. The policy alone explains it — `posts_select_public_or_owner` is `(owner_user_id = auth.uid()) OR (is_public AND EXISTS(approved follow))`, and the owner clause carries **no `is_public` test**, so any client list query that does not exclude the viewer's own rows will return them. **The contradiction is what makes this worth filing:** `qa-plan.md` D7/D8 record that on the same day the feed was *empty* because the viewer followed nobody, and that is the stated reason the client-rendering halves of B-3 and B-19 could not be reached — even though that viewer owned the post in question. Both observations cannot be right. **Not investigated, deliberately** — filed mid-D14 setup under an instruction not to chase it unless it blocked the run, which it does not. The feed's list query was not located in a short search: it is not a raw PostgREST path, not `from("posts")` through the SDK, and there is no feed RPC in the schema snapshot. **Settled by** finding that query and reading its filters, then re-checking D7/D8. **Possible good news rather than a defect:** if the feed does include the viewer's own posts, D7/D8's "needs a viewer who follows a third account" prerequisite is simply wrong, and both rows become reachable with two accounts | P3 | **Unverified** — one device observation, mechanism consistent with the deployed policy but the client query unread. **PARTIALLY CORROBORATED 2026-08-14, AND THE LIMIT IS STATED RATHER THAN GLOSSED.** The D8 rendering run reached a comment thread on one of Account B's **own** posts while B held **zero approved outgoing follows** — verified read-only in `follows` immediately beforehand — so the viewer's own content was reachable in exactly the condition D7/D8 claimed made it unreachable. **That settles the old QA prerequisite, which is now withdrawn from both rows.** It does **not** settle this finding: `CommentsView` has two presentation sites (`BackendSessionDetailView:265` via the feed, and `PeopleView:414`), the route taken was not recorded, and this row's specific claim is about the **feed's list query** — which is still unread. Do not upgrade this row on the strength of the D8 run. **Settled by** locating that query and reading its filters, as originally written | 5 |
| C-40 | **A brand-new Supabase session reported `JWT expired` on its first authenticated request.** Observed 2026-08-11 on Device B's Release app within seconds of a first-ever Sign in with Apple: `[FollowStore] backend refresh failed (following): HTTP error 401 body={"code":"PGRST303",…,"message":"JWT expired"}`, followed by `backend refresh ok` on the next attempt. Self-healing, and benign on this run. **It should not be possible on a session seconds old**, which is the reason to record it. Candidate explanations, none investigated: a refresh firing with a token captured before the sign-in exchange completed; device clock skew against the token's `exp`; or a token surviving from an earlier session — unlikely here, since the Release app was a fresh container with its own keychain access group, isolated from the Debug app on the same device. **Why it matters beyond the blip:** a 401 on a healthy session has the same shape as a genuine authorisation failure, and C-1's whole lesson is that treating one unverified negative as authoritative is how irreversible damage happens. The question worth answering is not why this 401 occurred but **whether any client path treats a 401 as authoritative** rather than retrying. **Settled by** capturing the token's `exp` alongside the request timestamp, and auditing 401 handling | P3 | **Unverified** — one observation, recovered on retry | 5 |
| C-36 | **The Solo location is published on joining Connected and then overwritten with `NULL` about half a second later.** The promotion itself is correct and easy to acquit: `AuthManager:481-548` runs on every sign-in route including a new account (`:446`), reads the local-scope location with a user-scope fallback (`:524-527`), publishes it (`:546`), and copies it forward into the user scope (`:529`). What defeats it is a second writer. The instant `backendUserID` publishes — synchronously, long before any network work returns — `ProfileView:1687` sets `locationText` from `profile.<uid>.location`, which is **empty on a first join** because the Solo value lives under `profile.__local_etudes_profile__.location`. The field visibly blanks; that counts as a change, so `:1705` schedules the debounced sync (`:1260`, 650 ms), which converts empty to nil (`:1290-1291`) and sends it (`:1308`). **The write is unconditional:** `AccountDirectoryService:427` always includes `"location": sanitizedLocation(location) ?? NSNull()` under `Prefer: resolution=merge-duplicates` (`:446-448`), so nil is an explicit `NULL` over whatever is there. Note the asymmetry — `account_id` is omitted when blank with a comment saying exactly why (`:336-337`), and `instruments` likewise (`:434`); **location is the only identity field left out of that protection.** The two writers then race: the publish is behind `fetchSelfRow` plus an upsert, the clobber behind a 650 ms timer, so on a normal connection the good write lands first and the `NULL` lands second. **Second path, no race needed:** `AuthManager:522` returns before location is considered when the Core Data `Profile.name` is empty, leaving the `NULL` as the only write. **Why it presents as intermittent:** `:529` repairs the *local* copy, so Profile shows the location again on a later appearance while `account_directory.location` is `NULL` — and that column is what both directory RPCs return, so peers see nothing. Two tails: `ProfileView:1300-1301` caches the bad sync's fingerprint, so nothing re-sends until another identity field changes; and signing out while `locationText` is still the stale empty string writes it over the local Solo value (`:1671`, and `ProfileStore:110-114` has no empty guard) — the mirror image of C-27, which is the same transition failing to copy at all. Fix: give `ProfileView:1687`/`:939` the local-scope fallback `AuthManager:526` already performs — one read, existing precedent, and it closes the blank field, the `NULL` write and the sign-out clobber together. Seatbelt afterwards: omit location when blank like its two neighbours, which first needs the product answer `account_id` already lives with — how a user explicitly *clears* a value under omit-when-blank | P2 | **Confirmed defect** — read from source and the deployed schema; **still not observed on device, and one path has now been tested and did NOT reproduce it.** QA B7 settles it, and the column is the verdict rather than the screen. **Negative result, 2026-08-12, TestFlight build 131, Device B — the delete/reinstall automatic-recovery path.** After an iOS-level delete and reinstall the container was empty, which is the condition this finding needs, so the path was expected to be hazardous. It was not: Profile was opened and left on screen for over a minute, Location rendered **"London" immediately with no blank state at any point**, and `account_directory.location` was still populated afterwards. **The non-reproduction corroborates the stated mechanism rather than weakening it.** C-36 turns on `ProfileView:1687` reading `profile.<uid>.location` and finding it *empty*; on a reinstall an `account_directory` row already exists, so `hydrateMissingLocalIdentity` (`AuthManager:461`) seeds the user-scoped local value from the backend at session restore, before Profile is ever mounted. The field therefore reads populated, nothing counts as a change, the 650 ms debounce never schedules, and no `NULL` is written. On a **first join** there is no backend row to hydrate from and the Solo value lives under `profile.__local_etudes_profile__.location` — the very absence the reinstall path supplies. *(Hydration winning is the explanation consistent with "populated immediately, never blank"; it was inferred from that observation, not watched directly.)* **This closes nothing.** The first-join case, both its race and the no-race `AuthManager:522` path, is untested and remains the finding |  1 |
| C-27 | **Location set while Connected does not carry to the Solo profile on sign-out unless `ProfileView` is mounted.** The backend→local copy lives in `ProfileView:1676-1681`, inside the `currentUserID` observer, so it only runs if the view exists at the moment of sign-out. Split from C-19, which concerned *expiry* and is Resolved. **No data loss:** `profile.<backendUserID>.location` is not deleted, so signing back in with the same identity restores it — the local Études profile simply shows the older local value until then. Cosmetic asymmetry: the sign-*in* promotion (`AuthManager:532-537`) is unconditional and view-independent; only the sign-out direction is not | P3 | **Confirmed defect** — by trace | 5 |
| C-2 | "Erase All Études Data" does not clear the Scores library, and the singleton resurrects it on next mutation. `LocalFactoryReset:24`, `AttachmentStore:412`, `ScoreLibraryStore:159` | P1 | **Resolved** — fixed at `1596b4b` and device-verified on Release: the Scores library is empty after Erase All Études Data and does not return. Three separate survival paths were closed, not one: the PDFs in `Documents/Scores/` (missed because the Documents sweep is extension-filtered and skips directories), the live singleton's `items` (which the next `persist()` would have written back), and the legacy `scoreLibrary_v1::` keys (which `load()` migrates forward whenever `scoreLibrary_v2` is absent — the post-reset state). The fix lives in `ScoreLibraryStore` rather than widening the Documents sweep, because `AttachmentStore.isProtectedScoreLibraryURL` deliberately keeps attachment deletion out of that tree | 1 |
| C-3 | Staged video held wholly in memory; re-read plus full temp copy on every foreground, synchronously on main actor. **Mechanism re-verified in source 2026-08-14 and unchanged:** on every `scenePhase == .active`, inside the SwiftUI `onChange` closure and therefore synchronously on the main actor, `hydrateTimerFromStorage()` does `Data(contentsOf:)` per staged attachment — the whole video into RAM — then `generateVideoThumbnail(from:id:)` **writes that data straight back out** to `tmp/<id>.mov` (`:4705`) and runs a synchronous `copyCGImage`. Per foreground, per staged video: a full read, a full write and a synchronous decode, before the UI can respond | **P3** (was `P1?`) | **MEASURED 2026-08-14 ON DEVICE A; SEVERITY DOWNGRADED, MECHANISM UNCHANGED.** Three rungs against thresholds fixed beforehand: 19.1 MB → **53.2 ms**, 93.6 MB → **120.7 ms**, 278.7 MB → **322.9 ms** (medians; worst single foreground 472.7 ms). Scaling is ~35 ms fixed + ~1 ms/MB. **The P1 hypothesis was memory, not latency, and it did not materialise:** every log line across all three rungs came from **the same PID**, so with 279 MB of video staged the app was never jetsammed or watchdogged. That lands it in the 250–1 000 ms band — **P3**. **Measured on device rather than the simulator deliberately**, because the simulator's SSD and absent jetsam would have understated every term and could not have observed the one outcome that mattered. **Two costs the latency figure does not capture, and they are the better argument for the Phase 5 fix:** ~279 MB held resident for as long as the attachments stay staged, and **the same bytes rewritten to `tmp` on every single foreground** — flash wear and battery on a path users hit constantly. **No refactor performed:** Phase 1 owned the severity, Phase 5 owns the fix, and that split held. **SUPPORTING OBSERVATION FROM THE SAME RUN, recorded as Phase 5 evidence rather than filed separately.** At all three video sizes, backgrounding Études made the staged-video thumbnail image briefly vanish from its square while the play overlay stayed put; foregrounding showed an empty square that then repopulated. Consistent and reproducible across every rung, and **cosmetic** — no data is lost and the thumbnail always returns. It is almost certainly the visible face of this very mechanism: `videoThumbnails` is rebuilt wholesale by `hydrateTimerFromStorage()`, so the square is empty for exactly as long as the synchronous regeneration takes, which is why the flash grew with the same sizes that grew `mainActorMs`. **Expected to disappear on its own when Phase 5 addresses the synchronous foreground regeneration**, so it should not be fixed as a separate cosmetic defect — if it survives that work, then it is a real finding and needs its own row. Probe removed the same day and verified as a pure deletion — the tree is byte-identical to its pre-probe state | 1 (measured — DONE) / 5 (fix) |
| C-19 | Location is keyed per backend user ID (`profile.<userID>.location`, `ProfileStore:32`) and its preservation across membership expiry appears to depend on a mounted `ProfileView`. The architecture states location survives expiry, so this is an implementation question, not an open product decision. | P2 | **Resolved** — the mechanism no longer exists. Verified by the trace this row asked for: location lives at `profile.<backendUserID>.location`, and the only code that deletes it is `wipeLocalIdentityForFactoryReset`, called solely from `LocalFactoryReset:78` (Erase All Études Data) — never on expiry, never on sign-out. Since C-1, expiry does not clear `backendUserID` at all, so the key stays resolvable and `ProfileView:944/1697` reads it normally. Location therefore survives expiry unconditionally, with no mounted-view dependency; the dependency was a consequence of the expiry path clearing identity, which is gone. Corroborated on device: yesterday's genuine lapse occurred with no `ProfileView` mounted and `auth.init` reported `backendID=true` on relaunch. Residual sign-out wrinkle split out as C-27 | 1 |
| C-7 | ~11.6 MB `debug_upload_test.*` in the app bundle | P2 | **RESOLVED.** Inspected `Release-iphoneos/Etudes.app`: `debug_upload_test.mp4` (11,228,233 B) and `.m4a` (382,447 B) are both present, 11.6 MB of a 71 MB bundle. Their only consumer is `DebugViewerView`, which is `#if DEBUG`-gated, so in Release the payload ships and nothing can reach it. Cause is the flat `fileSystemSynchronizedGroups` layout: everything under `MOTIVO/` is copied as a resource regardless of any `#if DEBUG` in Swift. Fix is an exclusion or a move out of the synchronized group, not a code change. **Resolved:** both fixtures added to the existing `PBXFileSystemSynchronizedBuildFileExceptionSet` — the mechanism already in use for `Info.plist` — so they stay in the repo but are excluded from the target in every configuration. Verified by inspecting rebuilt bundles rather than by reading the diff: absent from `Release-iphoneos` and `Release-iphonesimulator`, and the device bundle fell from 71 MB to 59 MB. `DebugViewerView`'s copy updated, since it previously told the reader to add the files to Copy Bundle Resources | 1 |
| C-8 | No `PrivacyInfo.xcprivacy`; required-reason APIs used. TestFlight history suggests uploads were accepted | P2 | **RESOLVED.** No `PrivacyInfo.xcprivacy` existed in the repo or the built bundle. First-party required-reason usage is narrower than filed: exactly two categories. **UserDefaults** (42 files) and **file timestamps** — a single site, `PracticeTimerView:4508` reading `attrs[.creationDate]`. *Not* disk space (only `.fileSizeKey`/`.size`, which are out of scope), not system boot time, not active keyboards. Bundled third-party manifests: AudioKit and swift-crypto ship their own; supabase-swift 2.5.1 and KeychainAccess ship none. **Resolved:** `MOTIVO/PrivacyInfo.xcprivacy` added, declaring the single category — UserDefaults, reason CA92.1 (app's own defaults; no app group, no shared suite). The file-timestamp category was **eliminated rather than declared**: its only site was the dead read in `stageAudioURL`, removed with C-18. Verified in the built bundle, not from the diff — present at the bundle root of `Release-iphoneos/Etudes.app` and parsing correctly. `NSPrivacyCollectedDataTypes` is deliberately omitted rather than declared empty, which would be false; it is authored in Phase 4 alongside shared-only uploads and the App Store Connect disclosures, so the two cannot describe different systems. Still worth checking: whether Apple sent ITMS-91053 warnings on past uploads | 1 |
| C-20 | Main-actor-isolated `PersistenceController.shared` and `fetchUserInstruments` reached from a nonisolated context. `AuthManager:494`. Touches Core Data `viewContext` off-main — a real data-race risk, and a hard error under Swift 6 | P2 | **Confirmed defect** *(build)* | 5 |
| C-21 | Three discarded `let status` reads in `NetworkManager` (258, 504, 555) — the shape of a dropped check | P2/P3 | **Confirmed defect** *(build)*, needs reading to establish consequence | 5 |
| C-4 | Permanent user media excluded from Apple backup while Core Data is not. Architecture Decision 4 settles the target: user-authored media participates in Apple backup, with a reconciliation pass for media already on disk. **The exclusion was never a durability decision** — it arrived as item 4 of five in `5c845e2` "Local Attachment Deletion & Filesystem Hygiene Hardening", grouped with orphan/tmp cleanup under "no behavior changes", with no rationale recorded anywhere. **Three things the original finding did not say, all established 2026-08-15.** (i) The Scores restore outcome is *not* an empty library: the index lives in `UserDefaults` and restores, so the user gets a fully populated library whose every PDF is missing. (ii) `AttachmentPrivacy.json` was excluded too — permanent user intent, excluded *incidentally* because `PracticeTimerStore` flagged the whole `Application Support/MOTIVO/` directory as a side effect of a migration helper. Losing it fails **closed** (`isPrivate` defaults `true`), so it is silent loss of choices rather than a leak. (iii) **Backup participation alone is insufficient** — `Attachment.fileURL` stores an absolute path containing the container UUID, which changes on restore, and nine consumers read it. | — | **RESOLVED 2026-08-15 — implemented as units U1–U6 plus a bounded D1–D6 remediation, and device-verified on a genuine encrypted Finder backup → restore onto Device A, scored against predictions committed before every mutation.** Closure independently audited against the repository on 2026-08-15 before being recorded. **Nine device assertions, scored separately — seven PASS, one NOT EXERCISED with a named owner, one satisfied by an adjacent run.** **PASS:** the **U5 pre-backup reconciliation gate** (`examined=7 alreadyEligible=0 cleared=7 failed=0`, identical to a prediction committed 8 minutes earlier — and `alreadyEligible=0` with `cleared=7` is the *provenance* discriminator, since a file written by the Phase 2 build would necessarily have been already eligible); **F1** (journal, media and Scores all present and openable after a real restore); **F2** (all 7 files authored by the pre-Phase-2 build came back **byte-identical**, and the adopted Scores copy survived while the byte-identical received-inbox cache did not — one backup, one restore, opposite outcomes, the only variable being the backup flag); **F2b** (reconciliation correctly did **not** re-run; the completion key lives in `UserDefaults` and restored as `true`); **F4** (a session whose attachments carried paths **two container generations stale** was deleted — the 12.6 MB image was removed, which pre-U2 would have been silently orphaned, while the score-library original survived because `isProtectedScoreLibraryURL` refuses to delete library originals: two directions in one assertion); **D15**, the erase-regression gate (the root-level `AttachmentPrivacy.json`, 520 bytes and 10 entries, **removed**, with 10 of 10 backend blast-radius measures matching and B's data intact); and **C-49** (first-launch onboarding immediately on completion, no relaunch). **NOT EXERCISED — and NOT a pass: F3.** It is unreachable through the shipping UI, because the editor's save rewrites `attachment.fileURL` from resolved URLs ~80 lines before publishing and so self-heals the very condition F3 exists to test; running it would have scored an assertion the test never reached. **The implementation exposure is closed by U2; the outstanding runtime verification of the queued-publish/container-rotation route is C-51, owned by Phase 4.** **F5 is satisfied by the D15 run**, not by a separate destructive run: D15 executed on the restored device with stale paths present and removed every populated fixture, which is exactly F5's erase-after-restore condition — see the C-51 row and `docs/qa-plan.md` for the evidence relationship and its carve-outs. **Empty preconditions were refused as passes throughout:** the received cache, `CommentsStore.json`, the local avatar and the legacy privacy-map location were absent at D15 time, and the 2026-08-14 C-28/C-48 acceptance remains the populated-state evidence on its own terms — verified still applicable, because Phase 2 touches none of `LocalFactoryReset.swift`, `CommentsStore.swift` or the received-cache wipe. **Three obligations close on source verification only, each on a stated sufficiency argument rather than a fixture blocker, and none is given a later owner:** the transient `motivo_vid_*` exclusion (fails toward scratch entering a backup, never toward loss of permanent data); the two deletion-safety guards (both changed monotonically in the safe direction — worst case a retained orphan, never deletion of referenced media); and the legacy privacy-map erase sweep (the same statement as the exercised root sweep, on a different element of the same array). **Historical, superseded by the above and kept only as the record of how the phase was worked:** implementation first landed 2026-08-15 as U1–U5 with static and simulator verification green, including a simulator reconciliation of `examined=5 cleared=5 failed=0`; at that point the row read *not yet exit-complete* pending three device gates, all three of which have since run. **Deferred cleanup, no code change at closure:** `ScoreLibraryStore.excludeFromBackup(_:)` now calls `BackupPolicy.include` and so asserts the opposite of what it does at three call sites — renaming it is **Phase 6**, not closure work | 2 |
| C-5 | Duplicate Score adoption: `savedToScoresAt` never consulted by the UI. `ConnectedAttachmentShareUI:755`. Server rejects the second write; the client still creates a duplicate local entry | P2 | **Confirmed defect** | 5 |
| C-6 | `fatalError` on Core Data store load. `Persistence:43` | P2 | **Confirmed defect** | 5 |
| C-9 | All StoreKit testing to date used the synthetic local config; real StoreKit only exercised in Release | P2 | **Resolved 2026-08-12 — root cause removed 2026-08-11, coverage checkpoint now met.** The shared scheme no longer pins `Etudes.storekit`, so running from Xcode now gets real StoreKit against Apple's sandbox **by default**; the synthetic configuration is opt-in in Run → Options for when synthetic behaviour is deliberately wanted. That inverts the trap this row describes — the failure mode was that synthetic was the silent default and applied in Release too. A sandbox loop is documented in `qa-plan.md` (Release + None + dedicated Sandbox Apple Account), which removes the last reason to reach for the synthetic config out of convenience — though **it is not cheaply repeatable, corrected 2026-08-12:** clearing a tester's purchase history does *not* withdraw a live entitlement, so a clean first-purchase state needs a new tester or a waited-out period. **THE DISTRIBUTION ARTIFACT HAS NOW BEEN EXERCISED — 2026-08-12, TestFlight build 131 on Device B.** The in-place update over the Xcode-installed Release build preserved the container and Connected identity, and **B1 passed on the artifact beta testers install**: Monthly purchase completed on real sandbox StoreKit, no C-13 hang, no C-38 alert, Connected activated. That is the environment this row was opened for, and it is the one thing four sandbox runs could not supply. **RESOLVED 2026-08-12 — Group B has now run on build 131.** B1 pass (purchase → Connected); **B2** counted as that single attempt, its throttled repetitions **not runnable** because the plan assumed a resettable first-purchase state that does not exist on TestFlight, where subscriptions renew daily for up to a week regardless of the tester's accelerated rate; B3 pass (pre-membership sessions present, private, and **zero `posts` rows created** — verified across a count taken while they already existed on device); B4 pass (`is_public = true`, no explicit shares — D-1's default confirmed on the artifact); B5 pass via automatic recovery, with Restore Purchases **not applicable** rather than skipped, and the backend untouched against a baseline recorded beforehand; B6 pass, with Device B's `auth.users` row still `created_at = 2026-07-23`, so identity was restored rather than re-minted. B7 was **not run** — it needs a first-join account and is a C-36 probe, not a StoreKit-coverage row, so it does not gate this. **What this row asked for is now satisfied: every environment is exercised, including the distribution artifact.** E12 and E13 also ran on 131 and carried B-5 to Resolved. **Three things surfaced on this build and are filed separately rather than folded in:** **C-42** (membership screen priced in USD while Apple's sheet showed GBP), the sandbox-reset correction in `qa-plan.md`, and B5's Restore Purchases reachability — examined and deliberately **not** filed, because the control is correctly hidden from a state that does not need it | 3 / RC — **ANNOTATION 2026-08-16: the `3` is a stale artefact of when this row was written. C-9 is Resolved (2026-08-12) and owes Phase 3 NOTHING.** It is counted in the Phase-3-tagged denominator because the cell contains a literal `3` and the denominator must stay mechanically recomputable — and it is excluded from the open obligations. **ARITHMETIC CORRECTED 2026-08-17: this annotation originally read "(9)" and "the 8 open obligations", which were the counts on the morning it was written. B-24 was filed later the same day, moving the denominator to 10 and — with B-4, B-9, B-12, B-13 and B-23 all discharged by U2/U3 — the open count to 4. The current figures are 10 tagged rows and 4 open (C-26, C-31, B-11, B-24), which is what `CLAUDE.md` states and what parsing the Phase cells reproduces. The stale pair is corrected here rather than left to be re-derived; no state, evidence or disposition changes, and C-9 still owes Phase 3 nothing.** Status unchanged; this is a cell annotation, not a status change** |
| C-10 | `.file` attachments send `application/octet-stream`, rejected by the bucket allow-list; failure is fatal and wedges the sync queue permanently. `BackendShim:1384`; importers use `[.item]` | P2 | **Confirmed defect** (client side) | 5 |
| C-11 | Favourite button announces "Open comments" to VoiceOver. `SessionDetailView:1704` | P3 | **Confirmed defect** | 5 |
| C-12 | `SessionDetailView.deleteSession()` unreachable dead code | P3 | **Confirmed gap** | 6 |
| C-14 | Backend user IDs and handles logged via NSLog in release | P3 | **Confirmed defect** | 5 |
| C-15 | `PDFSelectedPagesStore` unscoped, never collected | P3 | **Confirmed defect** | 6 |
| C-16 | `try!` on directory creation. `SessionSyncQueue:279` | P3 | **Confirmed defect** | 5 |
| C-17 | Unit test suite is an empty template | P3 | **Confirmed gap** | — |
| C-18 | `stageAudioURL` reads file attributes after deleting the file | P3 | **Resolved** — pulled forward from Phase 6 because it was the same three lines as C-8. `Data(contentsOf:)` succeeds, the file is deleted, and only then is `attributesOfItem` called on the now-missing path, so the `.creationDate` branch never ran and the `Date()` fallback has always produced the auto-title. The dead branch was removed rather than repaired: the sole caller is `AudioRecorderView`'s completion, so there is no import path where a file's creation date would differ meaningfully, and repairing it would have changed shipped titling *and* obliged a file-timestamp declaration in the privacy manifest. Behaviour is unchanged | 6 → 1 |
| C-25 | **Expiry-specific naming on a policy-neutral primitive.** `AuthManager:802` `clearConnectedIdentityAfterMembershipExpiry()` names a caller's motive rather than the operation. What it does is mechanical — clear the local Connected identity and session — and has several legitimate triggers beyond expiry: server-confirmed cleanup, Sign in with Apple credential revocation, a server-forced sign-out. Since C-1 removed the client's expiry responsibility, the suffix asserts a context the function has no way to know. Target: a policy-neutral primitive taking the motive as a parameter, `clearConnectedIdentity(reason:)`, mirroring `deleteCurrentConnectedAccount(reason:)` which already models this. Phase 3's server-confirmed expiry orchestration may carry the policy-specific name and *call* the primitive — the policy must not be folded back into it | P3 | **Resolved 2026-08-13 — renamed to exactly the signature this row specified, `clearConnectedIdentity(reason:)`.** Pulled forward from Phase 6 rather than by choice: C-45 gave the function a caller whose motive is credential revocation, which this row had itself anticipated as a legitimate trigger, and at that moment the expiry suffix became actively misleading rather than merely imprecise. **Acceptance criteria met in full:** the motive is now a parameter; no policy is folded into the primitive; and no expiry-named wrapper was retained, so Phase 3 remains free to add its own orchestration that *calls* this. **C-1 is unaffected in substance** — no client-side ENTITLEMENT path reaches it; Apple reporting `.revoked` is an authoritative statement about the credential, not an inference from a StoreKit read. The C-1 comment on the function was updated, because its "deliberately uncalled" claim became false | 6 |
| C-44 | **Account deletion does not revoke the user's Sign in with Apple authorization, and we hold nothing that could.** Audited 2026-08-13 while scoping C-35, because C-35 changes the exact UI path this obligation attaches to. **What we capture:** `AuthManager.process(_:)` (`:883`) reads `credential.user`, `credential.fullName` and — in `signInToSupabaseIfPossible` (`:982`) — `credential.identityToken`. **`ASAuthorizationAppleIDCredential.authorizationCode` is never read anywhere in the codebase.** **What we send:** `signInWithIdToken` (`:1001`) passes the ID token and nonce only. That is an OIDC id_token flow, not an authorization-code exchange, so **no call to `appleid.apple.com/auth/token` ever happens** and no Apple refresh token is obtained — by us or, on this flow, by Supabase. **What the backend does:** nothing. The string `revoke` does not appear in `supabase/functions/` at all, and `delete_account_v1` deletes the `auth.users` row and its data without any Apple call. **So the current deletion flow removes our copy of the relationship and leaves Apple's side intact:** the user still sees Études under Settings → Apple Account → Sign in with Apple, and signing in again silently re-creates an account. **Why it matters:** Apple's account-deletion guidance for apps offering Sign in with Apple expects the app to revoke the user's Apple token as part of deletion (the `POST https://appleid.apple.com/auth/revoke` endpoint), and deleting a backend user plus clearing local Keychain values is **not** equivalent. This is a plausible App Review rejection, which is why it is filed P1 rather than as tidying. **What a compliant route requires, none of which exists today:** an Apple private key (`.p8`) with its Key ID and Team ID; a **server-side** component to mint the ES256 client-secret JWT and call Apple, because that key must never ship in the binary; capture of the single-use `authorizationCode` at sign-in; an exchange at `/auth/token`; and the revoke call at deletion. **The fallback matters here and makes this tractable:** because no historical Apple refresh tokens were ever stored, the documented route for exactly this situation is to obtain a **fresh** `authorizationCode` by re-authenticating the user with Sign in with Apple *at deletion time*, exchange it immediately, and revoke that — no historical token needed. Confirm the current wording against Apple's own documentation (TN3194 is the relevant technote) before building, rather than from this cell. **Composes with C-35 rather than conflicting:** C-35 already introduces a "sign in with Apple again, then retry" recovery for an expired session, and that is the same gesture this fallback needs. **Deliberately NOT folded into C-35**, which is a one-predicate change; this needs Apple credentials, a new Edge Function and secret management. **AUTH-FLOW TRACE, 2026-08-13, recorded so the implementation does not have to rediscover it.** `configure(_:)` (`AuthManager:843`) sets `requestedScopes = [.fullName, .email]` and a SHA-256 nonce; `handle(_:)` (`:851`) → `process(_:)` (`:906`) reads `credential.user` and `credential.fullName`, writes `appleUserID` to the Keychain, then calls `signInToSupabaseIfPossible` (`:939`, defined `:998`), which passes **only** `identityToken` + nonce to `signInWithIdToken`. **`process(_:)` is the one place holding the full credential, so it is the only possible capture point for `authorizationCode` — but see below, because the recommended design does not need one.** `supabase/config.toml` currently declares a single function block; a second function is a new `[functions.<name>]` section plus a new `supabase/functions/<name>/index.ts`. The client calls no Edge Function except deletion, via a hand-built `URLRequest` in `ConnectedAccountDeletionService`, so there is no shared Edge-Function client to extend. **RECOMMENDED SHAPE — revoke at deletion, store nothing.** Rather than capturing the `authorizationCode` at every sign-in and storing an Apple refresh token at rest, obtain a **fresh** authorization at deletion time and exchange-and-revoke it immediately server-side. **This dissolves the historical-account fallback rather than implementing one:** there is no stored-token path, so old and new accounts take the identical route and no migration or backfill exists. It also adds no Apple secret at rest, no new table, no RLS surface, and nothing extra that itself needs deleting — and it requires **no change to the sign-in path at all**. **Do the revocation in a SEPARATE Edge Function called before deletion, not by extending `delete_account_v1`:** that function is a P0 path just verified end-to-end on 2026-08-13, and revocation must be able to fail without blocking deletion, which is far easier to guarantee when it is a different call. Ordering is forced — revoke first, because deleting `auth.users` invalidates the session the revoke call authenticates with. **Secrets required (none currently exist):** the `.p8` private key contents, Key ID, Team ID, and `client_id`, which for native iOS Sign in with Apple is the **bundle ID** `com.sdsongs.etudes` rather than a Services ID. **NOT YET VERIFIED AGAINST APPLE'S LIVE DOCUMENTATION** — the endpoints, the ES256 client-secret construction and the ~5-minute single-use lifetime of the authorization code are stated from knowledge and must be confirmed against TN3194 and Apple's current REST docs before any implementation. **VERIFIED AGAINST APPLE'S LIVE DOCUMENTATION, 2026-08-13 — that caveat is discharged, with three corrections.** Sources were fetched from the doc site's JSON render source (`developer.apple.com/tutorials/data/documentation/….json`), because the HTML is an SPA shell that returns no body text; note the route, it is the only way to read these pages programmatically. **Confirmed unchanged:** the endpoints `POST https://appleid.apple.com/auth/token` and `POST https://appleid.apple.com/auth/revoke`; the ES256 client-secret construction (`alg` ES256, `kid` the 10-character Key ID, `iss` the 10-character Team ID, `aud` `https://appleid.apple.com`, `sub` equal to the `client_id`, `exp` at most `15777000` seconds out, signed ECDSA P-256 / SHA-256) per *Creating a client secret*; the code's lifetime **verbatim** — "The code is single-use only and valid for five minutes" (*Token validation*); and `client_id` = the bundle ID, now citable rather than assumed — TN3159's Overview: "by using the native app's bundle identifier (or App ID) as its client identifier", with the Services ID being the web equivalent. TN3107 adds one implementation constraint that is easy to trip in Deno: the JWT library must **not** emit `ASN.1 DER` signatures — Apple wants the raw r‖s form. **CORRECTION 1 — `/auth/revoke` does NOT accept an authorization code.** Its `token` parameter is "The user refresh token or access token intended to be revoked", with `token_type_hint` of `refresh_token` or `access_token`. The `/auth/token` exchange is therefore **mandatory**, not an optimisation, and this row read as though it were one. Revoke is idempotent: 200 means the token "has been revoked successfully or was previously invalid". **CORRECTION 2 — "This dissolves the historical-account fallback rather than implementing one" is WRONG.** TN3194's fallback does not disappear; its *trigger* moves from "no stored token" to "no fresh code obtainable" — the user cancels, Apple is unreachable, or the credential is already `.revoked`. All three mandated steps still have to be built: "1. Delete the user's account data from your systems. 2. Direct the user to manually revoke access for your client. 3. Respond to the credential revoked notification to revert the client to an unauthenticated state." The claim that old and new accounts take one identical route is correct and is the real benefit; the claim that no fallback is needed is not. Recorded per the D14 lesson — the proposed fix was right, the stated reason was not, and the reason is what the next person implements from. **CORRECTION 3 — the Debug/Release bundle-ID split is a trap this project specifically has.** TN3107 lists "The `client_id` does not match the client for which the `code` was issued" as an `invalid_grant` cause, so a code minted by a Debug build (`com.samueldixon.motivo.dev`) can never be revoked with the Release `client_id` (`com.sdsongs.etudes`). `APPLE_CLIENT_ID` must be one fixed server-side value and must never be client-supplied. QA is unaffected, because the shared scheme's Run action is Release. **SETTLED — revocation must NOT block deletion, now cited rather than assumed.** TN3194: "If you don't have the user's refresh token, access token, or authorization code, you must still fulfill the user's account deletion request and meet the account deletion requirement." Note the obligation's true strength while we are here: *Offering account deletion in your app* says apps "**should** use the Sign in with Apple REST API to revoke user tokens", and **Guideline 5.1.1(v) does not mention Sign in with Apple revocation at all** — the hard requirement is in-app deletion. This stays P1 on App Review risk, not on a quoted mandate. **THREE QUESTIONS WERE OPEN BECAUSE APPLE SETTLES NONE OF THEM. (a) AND (b) ARE NOW CLOSED EMPIRICALLY, ON DEVICE, 2026-08-13. ONLY (c) REMAINS, AND IT IS A PRODUCT-COPY QUESTION RATHER THAN A BLOCKER.** (a) **No Apple page states that `authorizationCode` is non-nil on a REPEAT authorization of an already-authorized user.** TN3194 confirms repeat authorization is a normal flow and that the suppression is scoped to name and email — "they won't be presented with the initial authorization flow to enter their full name, email address, or both" — which implies the code is still issued, but that is inference, not Apple's sentence. It was the single load-bearing assumption of the store-nothing design. **GATE (a) — GREEN, device-observed on Device B (Release), 2026-08-13.** A temporary probe (`C44Probe.swift`, one run, since removed) performed a bare `ASAuthorizationController` request outside `AuthManager` entirely — no Keychain write, no Supabase call, no network of any kind — and reported: `authorization=success identityMatchesStored=true authorizationCodePresent=true authorizationCodeBytes=63 identityTokenPresent=true fullNamePresent=true`. **A repeat authorization does carry an authorization code, so the store-nothing design stands and TN3194's stored-refresh-token flow is not forced on us.** **The "already authorized" premise comes from the fixture's history, not from the probe output**, and that distinction matters: Device B's Apple Account has been authorized for `com.sdsongs.etudes` since `auth.users.created_at` = 2026-07-23, has signed in repeatedly since, and has never been revoked. `identityMatchesStored=true` alone could not have established it — Apple's `sub` is stable across a revoke-and-resignin, so it does not discriminate first from repeat. **`fullNamePresent=true` is a weakness in the probe, not a contradiction of TN3194.** The field tested `credential.fullName != nil`, and `PersonNameComponents` is routinely non-nil on repeat sign-ins with every component nil — so it does not show that Apple treated this as a first authorization. **`AuthManager.process(_:)` already handles that correctly** (`:918`): it joins `givenName`/`familyName`, tests the result for emptiness and falls back to the Keychain, rather than trusting non-nilness. No defect; the probe field was simply weaker than the code it was describing. **One constraint the probe surfaced and production inherits in reverse:** *Verifying a user* warns that requesting a new identity token "more frequently than once a day, can result in your request failing due to throttling", and the same request yields both token and code. That makes repeated probing hazardous — a throttled failure reads exactly like "no code" — but it is a non-issue in production, where a deletion-time authorization happens once in an account's lifetime. (b) **`redirect_uri` for a native code exchange is contradictory within Apple's own page** — the parameter table says `Required: false` while the description says "Authorization code requests require this parameter", and native apps have no registered return URL (*Configuring your environment* attaches them to Services IDs only). Resolve empirically, **in two stages, and the evidence standard for them differs — this is a correction to how the first was originally pitched.** **(b1), preliminary and PROVISIONAL only:** POST to `/auth/token` with a valid client secret, the real `client_id` and a deliberately invalid code, in three variants — no `redirect_uri`, with one, and a control omitting `code` entirely — and read TN3107's taxonomy (`invalid_request` = "missing a required parameter", `invalid_grant` = the code "is invalid, previously consumed, or expired"). The control is what makes the first variant readable at all. **It is NOT proof, and must not be recorded as such: Apple's validation ORDER is unknown**, so `invalid_grant` on a garbage code is equally consistent with the server rejecting the code before it ever examines parameter completeness. It earns the right to proceed and nothing more. Costs no authorization code, no device and no deploy. **(b1) RAN 2026-08-13 AND BEHAVED AS PREDICTED — PROVISIONALLY GREEN.** `A no redirect_uri, garbage code → 400 {"error":"invalid_grant","error_description":"The code has expired or has been revoked."}`; `B with redirect_uri, garbage code → 400 invalid_grant`, byte-identical to A; `C control, code omitted → 400 {"error":"invalid_request"}`. **The control did more work than it was designed for.** C shows Apple runs a parameter-completeness stage *before* grant validation — a missing `code` is caught as `invalid_request` rather than falling through to `invalid_grant` — and in A that same stage passed with no `redirect_uri` present. If `redirect_uri` were required for this client, the stage that caught the missing `code` should have caught it too. **That is a stronger inference than `invalid_grant` alone supports, and it is still not proof:** it is unobserved whether the completeness check is exhaustive or conditional (Apple may require `redirect_uri` only for Services ID / web clients, which would happen to be the answer we want but has not been seen), and no real code has been exchanged. **Only (b2) settles it.** **The incidental result is the more valuable one: NO `invalid_client` anywhere.** Apple accepted the `.p8`, Key ID, Team ID, `client_id = com.sdsongs.etudes` and — critically — **the ES256 signature produced by Web Crypto**, which is the exact `crypto.subtle` code destined for the Deno Edge Function. TN3107's DER trap is now empirically avoided rather than merely designed around, and the key is confirmed configured against the correct primary App ID. That was the single most likely cause of a first-deploy failure and it is retired before a line of the function exists. Nothing was consumed: no code, no token issued, no revocation, no mutation; the minted secret carried a five-minute `exp`. **(b2), definitive:** one **real** native authorization code from Device A, exchanged once, **with no `/auth/revoke` call anywhere in the path**. Only a real code can distinguish "structurally accepted" from "actually exchanges". **(b2) RAN 2026-08-13 ON DEVICE A AND IS FULLY GREEN. GATE (b) IS CLOSED — `redirect_uri` IS NOT REQUIRED.** A **real** native authorization code, obtained by a fresh `ASAuthorizationController` request on Device A (Release), was POSTed to a temporary authenticated Edge Function (`c44_exchange_probe`, deployed for this one run and since removed) which minted the ES256 secret in Deno and exchanged it. Result: `apple_status=200 exchange_ok=true sent_redirect_uri=false access_token_present=true refresh_token_present=true id_token_present=true token_type_is_bearer=true expires_in_present=true id_token_claims_readable=true id_token_iss_is_apple=true id_token_aud_matches_client_id=true caller_apple_identity_found=true id_token_sub_matches_caller=true revoke_attempted=false`. **`sent_redirect_uri=false` beside `apple_status=200` is the finding** — Apple's page contradicts itself and the endpoint has now answered. **Three secondary results, each a live risk before this run.** (i) **`id_token_aud_matches_client_id=true`**: the `aud` claim came back as `com.sdsongs.etudes`, so TN3159's "the native app's bundle identifier … as its client identifier" now has device evidence behind it and not merely a citation. (ii) **`refresh_token_present=true`**: the exchange yields precisely the token type `/auth/revoke` accepts, which is what makes Correction 1's mandatory exchange step pay for itself. (iii) **`id_token_sub_matches_caller=true`**: the server-side binding of Apple's `sub` to the authenticated Supabase caller's Apple provider identity **worked as designed** — pre-registered as able to fail, because Supabase might not have exposed the Apple `sub` where the binding looked for it (`identities[].identity_data.sub`), and it did not fail, so the production authorisation control needs no rethink. **NO REVOCATION AND NO MUTATION OCCURRED**: `revoke_attempted=false`, and the probe contained no `/auth/revoke` call and no insert/update/delete/upsert/storage call at all, both verified by grep before deployment. No token value, fragment, hash or length, and no Apple `sub`, was returned or logged — the response is booleans plus Apple's status. **Two consequences recorded rather than left implicit.** The run minted **one live Apple refresh token and abandoned it**, disclosed in advance; nobody holds it, and the eventual end-to-end revoke clears it. And **the re-authorization prompt seen mid-run is the production UX cost of the store-nothing design, not a fault** — `b2.start` is emitted immediately before `performRequests()`, so Apple's sheet appears there by construction, and `identityMatchesStored=true` confirms it was the same identity rather than a second account. In the shipped flow the user taps Delete Account and gets that sheet before deletion proceeds. That is now observed rather than described. **The signer for (b1) is deliberately Web Crypto (`crypto.subtle`) rather than a Node-only API**, so the same code that will run in the Deno Edge Function is what gets tested — Web Crypto's ECDSA output is raw r‖s, which is what TN3107 requires when it says the library must not "decode using `ASN.1 DER` byte format". A rejected signature then surfaces at the cheapest possible point instead of during the first device run. (c) Apple nowhere addresses what is expected when a user **cancels** a deletion-time re-authorization; TN3194's fallback answers what to *do*, but the expectation itself is unwritten. **One flow correction worth keeping:** TN3194 documents the stored-token flow as the norm for all new account creations — transmit the code at sign-in, exchange it, "securely store the token response", revalidate up to once a day. Store-nothing is compliant and far smaller here, but it is **not** the flow Apple writes up as standard, and that trade should be stated rather than glossed. **Consequence for re-signup, in our favour:** TN3194 warns that where revocation is never completed the returning user is not re-prompted for name and email. Today Études revokes nothing, so a user who deletes and signs in again gets `credential.fullName == nil` and a `displayName` the factory reset has just erased — a nameless account. Successful revocation *restores* the first-run prompt, so this work fixes an existing rough edge rather than risking one. **IMPLEMENTED AND DEPLOYED 2026-08-13; NOT YET RUN END-TO-END.** `revoke_apple_identity_v1` is ACTIVE at version 1 with `verify_jwt = false`, and production was re-downloaded and verified **byte-identical to `df13e7e`** (sha256 `3a7c2035…`). `delete_account_v1` was re-downloaded in the same pass and is **byte-identical to its committed source** (sha256 `993b505b…`), still `verify_jwt = false`, and this time did **not** bump — it stayed at version 7. All four `APPLE_*` secrets confirmed present by name. **Static leakage checks on the deployed source:** the function contains **no logging primitive of any kind** (no `console.*`, no `Deno.stdout/stderr`), and of its 13 response payloads the only dynamic values are `exchangeBody.error` and `revokeBody.error` — Apple's short error enum, `typeof`-guarded and defaulted to `unparsable`. No authorization code, access token, refresh token, Apple `sub`, client-secret JWT or key material is referenced in any returned payload. **One methodological note worth keeping:** the first automated leakage check reported two false positives, matching `authorization_code` and `id_token` inside the *reason strings* `"missing_authorization_code"` and `"id_token_rejected"`. A substring grep cannot distinguish an identifier from a label; the check was redone with string literals stripped before matching. `revoke_apple_identity_v1` (new Edge Function, `verify_jwt = false` mirroring `delete_account_v1`), `AppleRevocationService.attemptRevocation` (**non-throwing signature — that is the structural guarantee that revocation failure cannot block deletion; do not "improve" it into a throwing call**), `AccountDeletionTransaction`, and the root-level TN3194 notice. `delete_account_v1`, `NetworkManager` and `LocalFactoryReset` are all untouched. **`AccountDeletionTransaction` exists because `LocalFactoryReset.isInProgress` COULD NOT BE REUSED, and the reason is a defect it would have caused rather than a matter of taste.** That flag becomes true at the first statement of `LocalFactoryReset.perform`, which runs only *after* `delete_account_v1` has already succeeded, so it covers the reset phase alone. **Widening it would have broken deletion outright:** `AuthManager.ensureValidSessionForConnectedAccountCleanup` (`:772`) is itself guarded by `!LocalFactoryReset.isInProgress` and returns `false` when set; it is the first thing `ConnectedAccountDeletionService` calls, and `false` throws `.sessionInvalid`. Every deletion would have failed with "Session is not valid" — **C-35's signature a third time, a non-authoritative condition blocking deletion.** `ProfileView.deleteAccountInFlight` was also rejected: nearly the right span, but view-local `@State` and therefore invisible to C-45's observer, which is the one caller that must read it. The new primitive is scoped (`run(reason:_:)`, `defer`-cleared on every exit path including cancellation) and depth-counted so a nested call cannot clear an outer one. **The TN3194 fallback notice is presented from the root `ZStack` in `MOTIVOApp.body`, NOT from `ProfileView`** — `ProfileView` is a conditional overlay driven by `appRoute.isProfilePresented` and can legitimately be gone by the time the notice is due, whereas the `WindowGroup` content is never destroyed while the app runs. Its state is in-memory only and deliberately **not** `UserDefaults`, which `LocalFactoryReset` wipes wholesale. It fires only when revocation did **not** succeed and deletion did, so it never points the user at a Settings entry that is no longer there. **An earlier proposal to gate deletion behind a Continue/Cancel notice was withdrawn** — it needed a stored continuation inside the transaction for no benefit and re-opened a decision the user had already made by typing DELETE. **ONE FAILURE MODE RECORDED BEFORE IT IS DISCOVERED:** if the app dies between a successful revocation and a successful `delete_account_v1`, the user is revoked at Apple with a live Supabase account. It is recoverable — re-authorizing yields the same Apple `sub`, hence the same Supabase user, and deletion can be retried — and the reverse ordering would be strictly worse. **END-TO-END ASSERTION SET, narrowed deliberately.** **Authoritative:** `revoked: true` from the API, the identity binding holding, `delete_account_v1`'s blast radius matching a prediction written beforehand, and the local reset completing. **Supporting only:** Apple credential state reading `.revoked`, disappearance from Settings → Apple Account → Sign in with Apple, and a renewed first-run name prompt. **Apple documents no timing guarantee for any of the three**, so a stale credential state is to be re-checked and its propagation behaviour recorded — never treated as C-44 failing. **BASELINE READ AND PREDICTION COMMITTED BEFORE THE RUN, 2026-08-13.** Fresh read-only snapshot: `auth.users` 16, `account_directory` 15, `posts` 98, `follows` 6, `post_comments` 3, `connected_attachments` 35, storage `attachments` 10, storage `avatars` 3. **Device A's fixture is `6b370c0e…`, created 2026-08-13, and it is EMPTY** — zero posts, zero comments authored or addressed to it, zero follows in either direction, zero attachments sent or received, zero storage objects in either bucket, and **no `account_directory` row at all**, which is what accounts for 16 users against 15 directory rows. **PREDICTION: `auth.users` 16 → 15, and every other count unchanged.** **The weakness of that assertion is stated rather than glossed:** an empty fixture cannot re-prove the content-deletion semantics, which were established by C-35's run and by D5–D14. What it *can* prove, and what it is actually for, is that **inserting revocation ahead of deletion disturbs nothing else** — any stray deletion by the new path moves one of the seven unchanged counts. The run also does not exercise the directory-row or avatar deletion paths, because this account has neither. **Two incidental checks come free:** Device A signed in through the existing-member route without purchasing, so a successful deletion re-verifies C-35's entitlement-free rule with the revocation path now in it; and b2 already confirmed `id_token_sub_matches_caller=true` for this exact account, so the server-side binding is known to hold. **RUN EXECUTED 2026-08-13. DELETION SAFETY VERIFIED; REVOCATION DID NOT SUCCEED. C-44 REMAINS OPEN.** **All eight counts matched the committed prediction exactly** — `auth.users` 16 → 15, and `account_directory` 15, `posts` 98, `follows` 6, `post_comments` 3, `connected_attachments` 35, storage `attachments` 10, storage `avatars` 3 all unchanged; the `6b370c0e…` row is gone, confirmed by direct query. `LocalFactoryReset` ran to completion and C-45 stayed silent throughout — no spurious sign-out. **But the manual-revocation notice appeared, which fires only when `didRevoke == false`.** **THREE THINGS THIS RUN PROVED, AND THEY ARE NOT SMALL.** (i) **The non-blocking guarantee held under a REAL failure rather than a simulated one** — revocation failed and deletion completed anyway, exactly as TN3194 requires and as the non-throwing `attemptRevocation` signature was designed to force. (ii) **The durable presentation path works in production**: the notice rendered from the app root over the first-launch onboarding screen, *after* the navigation hierarchy had been reset — the precise failure mode that a `ProfileView`-attached alert would have hit. (iii) Inserting revocation ahead of deletion disturbed nothing. **WHY IT FAILED IS NOT ESTABLISHED, AND THAT IS AN INSTRUMENTATION FAILURE OF MY OWN MAKING.** The outcome line is `#if DEBUG NSLog` (`ProfileView:1709-1711`), and this rig runs **Release** — Debug carries a bundle ID App Store Connect does not know. **So the single line that would have named the cause was compiled out of the only build that can execute the path.** CLAUDE.md already records this exact lesson from the MembershipTrace episode — use `os.Logger` with `privacy: .public` for anything that must be readable outside Debug — and it was not applied. **LEADING HYPOTHESIS, structural and untested: the Apple authorization could not present, because the deletion confirmation sheet was still up.** `performDeletion` is invoked from a button *inside* `.sheet(isPresented: $showDeleteAccountSheet)` (`:1873`), and `showDeleteAccountSheet` is cleared only at `:1718`, **after** `deleteCurrentConnectedAccount` returns. So `performRequests()` runs while a sheet is presented, and `ASAuthorizationController` presents from the anchor window's root view controller, which already has a presented controller. **Gate (b2) could not have caught this**: its probe was triggered from a plain Profile row with no sheet presented, which is why the identical authorization succeeded there. A probe validates the mechanism, not the presentation context it will ship in, and the difference between those two is where this defect lived. **Rig consequence:** because revocation did not occur, Device A's Apple Account still has Études authorized and the (b2) refresh token is still live, so a re-signin on A will NOT produce a first-run name prompt — which is itself the supporting observation TN3194 predicts. **RUN 2 BASELINE AND PREDICTION, COMMITTED BEFORE THE RUN, 2026-08-13 19:23 UTC.** Device A's second fixture is `62971b7f…`, created 19:22:16 UTC — **one minute before this baseline was read**, which is what establishes it as the sign-in just performed rather than a survivor. It signed in through the existing-member route with **no purchase**, so the app is in Solo and the run again exercises C-35's entitlement-free deletion rule. Backend baseline: `auth.users` 16, `account_directory` 15, `posts` 98, `follows` 6, `post_comments` 3, `connected_attachments` 35, storage `attachments` 10, storage `avatars` 3. The fixture is again **empty** on all nine per-account measures and again has **no `account_directory` row**, which is what accounts for 16 users against 15 directory rows. **PREDICTION: `auth.users` 16 → 15, every other count unchanged, and — the part run 1 could not deliver — `outcome=revoked` in a Release-readable log line.** The first run's record is unchanged and stands on its own: revocation failed for an unrecoverable reason, deletion completed regardless, 8/8 backend prediction passed, local reset passed, the manual-revocation notice survived the reset and rendered from the app root, and C-45 did not interfere. **RUN 2 EXECUTED 2026-08-13 AND IS FULLY GREEN. C-44 IS RESOLVED.** `[C-44] revocation reason=delete-account outcome=revoked`, emitted in **Release** through `os.Logger` at `privacy: .public`, and emitted **before** `LocalFactoryReset begin` — so the ordering the design forces (revoke, then delete, then reset) is visible in the log rather than merely intended. **All nine measures matched the committed prediction**: `auth.users` 16 → 15, the `62971b7f…` row gone, and `account_directory` 15, `posts` 98, `follows` 6, `post_comments` 3, `connected_attachments` 35, storage `attachments` 10 and `avatars` 3 all unchanged. No manual-revocation notice appeared, which is correct and is itself an assertion — it fires only on `didRevoke == false`. C-45 stayed silent throughout. **THE SHEET HYPOTHESIS IS CONFIRMED BY THE FIX, WHICH IS WEAKER EVIDENCE THAN A NAMED ERROR CODE AND IS RECORDED AS SUCH.** Run 1 failed with the confirmation sheet still presented; run 2 succeeded with the workflow moved to the sheet's `onDismiss`, changing nothing else about the authorization. That is strong circumstantial evidence and not a direct observation — run 1's actual `ASAuthorizationError` code was never captured, because the only diagnostic was `#if DEBUG` in a Release-only path. **The general lesson is the one worth keeping: gate (b2) validated the MECHANISM in a presentation context production does not have.** A probe triggered from a plain list row cannot discover that the same call fails from inside a presented sheet. Where a probe's environment differs from the shipping call site, that difference is itself untested surface. **Entitlement-free deletion re-verified**: Device A was in Solo with no purchase, so C-35's rule held with revocation now in the path | **P1** | **RESOLVED 2026-08-13, device-verified end to end on the second run.** `revoke_apple_identity_v1` is deployed and ACTIVE; a real deletion revoked the Apple authorization and all nine backend measures matched a prediction committed beforehand. Two gates were established on device before any code was written — a repeat authorization does carry a code, and a native code exchanges with no `redirect_uri` — and both are recorded above with their evidence. **Run 1 is retained in full and is not superseded:** it failed to revoke and deleted correctly anyway, which is the only real-world proof that the non-blocking guarantee works. **One dependency remains open, deliberately:** C-45 is implemented and built but its `.revoked` path has never executed, so TN3194 compliance is complete in construction and not yet in observation | RC (before submission) |
| C-48 | **Historical local comment residue survives "Erase All Études Data".** **RE-SCOPED 2026-08-14 after a reachability check that changed the finding.** Filed 2026-08-13 while mapping C-28's storage boundary, originally as "comments survive Erase All". **The location is the defect:** `CommentsStore:46-57` resolves Application Support and appends `CommentsStore.json` at the **root**, unlike every other JSON store in the app, which lives under `App Support/MOTIVO/`. `LocalFactoryReset` sweeps `Documents` (extension-filtered), `Documents/Scores`, the four `MOTIVO/` stores by name, and `App Support/Profiles` — so nothing it does can reach a file sitting beside those directories. **What is in it:** comment bodies plus `authorByCommentID` and `recipientByCommentID`, i.e. text together with the Supabase user ids of authors and of the recipients of private owner replies. **THE WRITE PATH IS UNREACHABLE IN THE SHIPPING UI, AND THAT WAS ESTABLISHED ONLY BECAUSE THE TESTER CHALLENGED THE INSTRUCTION.** QA was told to "comment on a local session" to stage the file; the tester replied that no comments control exists on a Journal session and asked for reachability to be re-checked **before** acting. It closes in four links: (1) both comment controls are gated on `appModeManager.canComment` (`SessionDetailView:1708`, `ContentViewSessionRow:956`), which is `mode == .connected` (`AppModeManager:79`) — so in Solo the control is absent, which is what the tester observed; (2) `mode == .connected` makes `applyBackendRuntimeMode` set `.backendConnected` (`AppModeManager:50`), and `auth.isConnected` mirrors exactly that (`AuthManager:155`), so `canComment` implies `auth.isConnected`; (3) both sheets route `if auth.isConnected, let postID = sessionUUID` to `.connectedPost`, leaving `.localSession` as the `else`; (4) `sessionUUID` is the session's **own Core Data `id`**, not a backend post id, and is assigned on save (`AddEditSessionView:1919`), so it is non-nil for any saved session. **Whenever the control is visible both conditions of the `.connectedPost` branch hold; when it is not visible no sheet presents at all. The `.localSession` branch cannot execute**, and all four `CommentsStore` mutations live inside it. Commenting on a Connected feed post goes to `BackendCommentsStore.addComment` (`CommentsView:1824`) and writes nothing locally. **So no current install can create this file through normal use.** **BUT THE RESIDUE IS REAL, AND IS OBSERVED RATHER THAN ARGUED.** A read-only container listing of Device B's long-running Études Dev install (a file listing only — no launch, no write, no network, fixture untouched) shows `Library/Application Support/CommentsStore.json`, **7 KB, dated 2026-01-10** — the 8H era, when comments were local-only by design. It has survived every build since. **And the store is still READ:** `ContentViewSessionRow` observes `commentsStore.objectWillChange` and renders a comment count from it, and `BackendSessionDetailView:79` observes it too, so legacy data can still surface in today's UI. **Forward risk for a new install is zero; the risk is entirely for upgraded devices, and for those the defect stands exactly as filed.** The `.localSession` branch itself is vestigial — C-41's family — and removing it is Phase 6 cleanup, not this row. **THREE SURVIVAL PATHS, WHICH IS C-2's SHAPE, AND DELETING THE FILE CLOSES ONLY ONE.** (1) the file; (2) **the live singleton** — every mutation calls `save()`, which writes the whole in-memory state back, so a file delete with populated dictionaries is undone by the next mutation; (3) **the legacy `commentsStore_v1` UserDefaults blob**, which `load():248` migrates forward **whenever the file is absent** — precisely the post-reset state. Path 3 is also covered by the UserDefaults domain wipe, so it is belt-and-braces; it is handled in the store's own wipe anyway so correctness does not depend on call order. **`clearAll()` was the closest existing primitive and is the wrong one** — it calls `save()` and re-creates an empty file where a reset should leave none. **METHOD NOTE, worth more than the row:** the original staging instruction was produced by reading what `CommentsView` does when handed `.localSession` and never asking whether anything hands it that. Same failure as C-47 — reasoning forward from a mechanism without checking the premise that it is reachable. It was caught by the tester refusing to act on it, not by the analysis | P2 | **RESOLVED 2026-08-14, device-verified.** The residue is observed in production on Device B; the write path is proven unreachable by source. Fix implemented and verified with C-28; **runtime verification staged under the D13 precedent — the state is SIMULATED, NOT PROVOKED.** A synthetic `CommentsStore.json` with fabricated bodies and non-sensitive ids is placed in Device A's container before the destructive run; Device B's real legacy file is never copied and none of its data leaves that device. D13's reasoning applies and is stronger here: that row tested the deletion's behaviour *given* a state rather than how the state arose, and unlike D13 this state has been **observed in production first**. Severity held at P2 — the residue's sensitivity is unchanged; only its provenance is. **RESOLVED 2026-08-14 on the same Device A run.** `Library/Application Support/CommentsStore.json` is **absent** afterwards, and the Release log recorded `[C-48] localReset commentsStoreFileExisted=true` — so the file was genuinely present when the wipe ran, which an absence check alone could never establish. The staged state was **simulated, not provoked**, as recorded here and in `qa-plan.md`; what the run verifies is the reset's behaviour **given** the residue, which is the whole of this row's claim. The unreachable `.localSession` write path is untouched and remains Phase 6 cleanup | 1 |
| C-50 | **Études does not hold off idle auto-lock while a video recording is in progress, so the device can lock mid-take and interrupt the recording.** Observed 2026-08-14 on Device A during the C-3 measurement run, with iOS Auto-Lock set to 30 seconds: recording inside Études was interrupted by the device auto-locking. Setting Auto-Lock to Never made long recordings work normally, which is what identifies idle lock as the cause rather than anything in the capture pipeline. **Confirmed absent by grep:** `isIdleTimerDisabled` appears **nowhere** in `MOTIVO/`, so no idle-timer management exists anywhere in the app — this is a gap, not a broken implementation. **Why it matters beyond annoyance:** the whole point of the feature is recording a practice take, takes are routinely longer than a short Auto-Lock interval, and the default is not something the app can assume. A user loses the take, and the cause looks like an Études bug rather than a system setting. **INTENDED BEHAVIOUR, as specified:** while an Études video recording is **actively in progress**, the app temporarily prevents idle auto-lock via `UIApplication.shared.isIdleTimerDisabled` (or the current equivalent), and **reliably restores the previous idle-timer state on stop, cancel, failure, and teardown / view disappearance**. It must **never modify the user's actual system Auto-Lock preference** — the flag is a runtime request, not a settings write. **Scope limit, and it is deliberate:** this applies to **idle** auto-lock only. A deliberate manual lock, or the user deliberately backgrounding Études, must retain normal iOS lifecycle behaviour and must not be defended against. **Implementation note for whoever takes it:** `VideoRecorderView` already has the lifecycle hooks this needs — `onAppear`/`onDisappear` (`:212`/`:227`) delegating to `controller.onAppear()`/`onDisappear()` (`:483`/`:497`) — so the restore path has an existing home rather than needing new plumbing. The flag must be tied to *recording in progress*, not to the recorder being on screen, or a user who opens the recorder and walks away never sleeps their phone. Audio recording is out of scope until someone establishes whether it has the same exposure | P2 | **Confirmed defect** — device-observed, with the cause isolated by changing the system setting and re-running. Not yet fixed; deliberately not pulled into Phase 1 | 5 |
| C-49 | **After a successful account deletion the app lands on the journal, not first-launch onboarding.** Observed 2026-08-14 on the C-28/C-48 destructive run — Device A, Release from `ea0beb2` — as the single deviation from a prediction that otherwise passed in full. **Filed separately rather than folded into C-46**, which is a different mechanism one level down: C-46 was *stayed on Profile*, and its fix worked here — Profile was dismissed correctly. **MECHANISM, READ FROM SOURCE AFTER AN EMPIRICAL DISCRIMINATOR RATHER THAN INSTEAD OF ONE.** `MOTIVOApp.body` switches on `appRoute.route` (`:205`): `.timer` renders `PracticeTimerView(presentationMode: .home)`, `.content` renders `ContentView`. **The onboarding gate exists only in the first branch** — `shouldRenderAppSetUpRoot = isHomePresentation && requiresAppSetUpNow()` (`PracticeTimerView:1255`). `ProfileView` is an overlay above both (`MOTIVOApp:216`). C-46's `dismissProfileAfterSuccessfulErase()` clears `isProfilePresented`, revealing whatever `appRoute.route` already held — and when Profile was opened from the journal that is `.content`, where the gate **is not in the view tree at all and is never evaluated**. Nothing in `LocalFactoryReset` resets `appRoute.route`, and by C-46's own reasoning nothing should: the reset owns data, not navigation. **C-46 fixed the overlay and left the route beneath it** — the same gap one level up. **THE DISCRIMINATOR WAS RUN BEFORE THE MECHANISM WAS WRITTEN DOWN.** A force-quit and relaunch showed onboarding correctly, establishing that the reset cleared persistent state properly and that `requiresAppSetUpNow()` gives the right answer once evaluated — a fresh launch defaults `appRoute.route` to `.timer`. **It also eliminates the competing candidate:** `requiresAppSetUpNow()` early-returns `false` when `auth.isSignedIn && BackendConfig.isConfigured && backendBootstrapState == .existingAccount` (`:1235`), and had that been the cause the user would have landed on the **timer**, not the journal. The observed screen discriminates the two with no further work. **Why it is not merely cosmetic:** it is on the account-deletion path an App Review reviewer exercises deliberately, and what they see is an app that still appears set up — the family of C-24 and C-46, a stale view of destroyed state. **No data implication:** every backend and filesystem assertion on the same run passed, and the condition clears on the next launch. **Likely fix, not implemented:** set `appRoute.route = .timer` alongside the existing dismissal inside `dismissProfileAfterSuccessfulErase()`, which already runs only on success and only after a completed reset, so it inherits C-46's correct never-on-failure gating rather than needing its own | P2 | **RESOLVED 2026-08-15 — device-verified, and the acceptance cost no fixture of its own, exactly as this row's carry condition required.** Fix applied 2026-08-14 and build-verified; acceptance folded into the **D15 destructive run** on 2026-08-15 and **scored separately from it**, against a prediction committed beforehand in `8487bc1`. **Immediately on completion of Delete Account & All Études Data, with no relaunch, the app was on first-launch onboarding** — "Set up Études — Add your name and your first instrument to start", with an empty Display name field. Not the journal, and not a stale Profile. **The no-relaunch condition is what makes it meaningful:** the 2026-08-14 deviation was precisely that the app landed on the journal and then cleared on relaunch, so later relaunch behaviour could never have substituted for this observation. **One transient frame is recorded so it is not misread later:** mid-deletion the screen briefly showed Profile with a Sign in with Apple sheet, which the log explains as C-44's re-authorization request for revocation — `outcome=notAttempted(authorization/AuthorizationError/1001)`, 1001 being *canceled*. The deletion continued regardless, which is the settled semantics; C-44 behaving as accepted, not a new defect. **Implementation, unchanged from 2026-08-14:** an explicit `onEraseComplete` closure on `ProfileView`, supplied by both presenters, rather than the `@EnvironmentObject` first written. **That first attempt was backed out on its own merits:** ProfileView has a second construction site (`PracticeTimerView:1883`, a `.sheet`), and a missing environment object traps at **runtime**, only when the body evaluates, on the screen that leads to account deletion. Sheets do inherit environment objects on iOS 14+, so it would very likely have been fine — but "very likely fine" is not a standard this path should be held to, and the closure is compile-checked at both call sites instead. **`PracticeTimerView`'s presentation was never the broken one** — dismissing that sheet already reveals the timer, where the gate lives — but it is set there too, so correct behaviour does not rest on an implicit fact about which view happens to be underneath. **C-46's gating is preserved exactly:** `onEraseComplete` fires only from `dismissProfileAfterSuccessfulErase()`, which runs only after a completed, successful reset and never on the failure path, where dismissing would hide the alert saying the account was **not** deleted. **The acceptance criterion, now met:** a successful reset lands immediately on first-launch onboarding, with no force-quit or relaunch | 1 |
| C-47 | **A user-typed attachment title becomes unreachable when Connected authentication is withdrawn, because the title store is namespaced by an identity that withdrawal clears.** Observed on Device A, 2026-08-13, during C-45's runtime verification: a video titled "video test" showed no title after Apple credential revocation, while sessions, notes, Scores and the attachments themselves all survived. **THE TITLE WAS NOT DELETED, AND THAT IS ESTABLISHED EXHAUSTIVELY RATHER THAN ASSUMED.** Exactly two places in the app remove a namespaced title key: `LocalFactoryReset:54-58` and `AuthManager.signOut:902-903`. `LocalFactoryReset` demonstrably did not run (no log lines), and `signOut` is not reachable from C-45 — the Release log recorded `action=clearConnectedIdentity`, and `clearConnectedIdentity` contains neither call. **So the bytes are still in `UserDefaults` and the app is reading the wrong key.** **MECHANISM FAMILY IS CERTAIN; THE EXACT BRANCH IS NOT, AND IS NOT GUESSED HERE.** `sessionDetailNamespaceUserID` (`SessionDetailView:104`) resolves the namespace in priority order: the backend uid when `BackendEnvironment.shared.isConnected`, else `auth.currentUserID` (the Apple user ID), else `PersistenceController.shared.currentUserID` — which is a plain in-memory `var` (`Persistence:26`), not persisted. `clearConnectedIdentity` nils `currentUserID` and removes the backend-uid defaults, so the resolution can land on a **different branch after withdrawal than it did at write time**, producing a different key for the same data. Which branch was used on each side has not been observed. **DECISIVE TEST, cheap and non-destructive: sign in again with the same Apple ID. If the title reappears, it was orphaned by namespace and no data was ever lost.** **C-45 DID NOT CAUSE THIS AND ITS OWN CONTRACT HELD** — it erased nothing, which is exactly what it promises. But C-45 is the first caller `clearConnectedIdentity` has ever had (C-1 left it deliberately uncalled), so this was latent and unreachable until now. **It will recur in Phase 3's server-authoritative expiry**, which calls the same primitive, and it sits directly against C-1's promise that profile and preference data survive expiry untouched. **Severity is judged from the user's seat:** a title they typed is gone from the screen, and "the bytes are still on disk under another key" is not a defence **Confirmed defect** — established from the deployed source by exhaustive enumeration of the delete sites, plus a device observation. Not yet reproduced under a known branch, and the orphan-versus-loss question is settled by the re-signin test above, which has not been run. **PREMISE CHECK RUN FIRST, 2026-08-13, AND IT CHANGED THE PICTURE.** The re-signin did **not** restore the title, even though it reused the same Supabase account (`auth.users` 16, `44a6018e…` still newest — same Apple `sub`, same uid), so the namespace should have resolved exactly as at write time. **That weakens the orphan hypothesis substantially.** It also exposed that this row's premise — that the title had ever been durably saved — was never established; it rested on the tester's recollection, and the C-45 prediction treated its absence as diagnostic without ever verifying it was there. **A plausible competing mechanism exists in the write path:** `SessionDetailView:528-529` has two silent early returns — an attachment match by filename stem, and `guard let userID = sessionDetailNamespaceUserID(...)` — either of which drops a typed title with no error and no feedback. **TEST A (title → force-quit → relaunch) PASSED**, so titles do persist and the premise now holds for a fresh title. **TEST B PREDICTION, committed before the run:** revoke via Settings with a known-persisted title in place. Baseline `auth.users` 16, fixture row present. **If the title SURVIVES, the original loss was a one-off write failure unrelated to credential state, this row is not reproducible as written, and the namespace analysis above is wrong.** If it disappears, the defect is real and reproducible and the namespace flip becomes the live hypothesis — still requiring the specific branch to be identified rather than assumed. **TEST B RAN 2026-08-13 AND THE TITLE SURVIVED. THE CREDENTIAL-STATE CLAIM IN THIS ROW IS WITHDRAWN, AND THE NAMESPACE ANALYSIS ABOVE IS WRONG.** With a title verified persistent across a force-quit, revoking the Apple credential did **not** remove it, and all nine backend measures were unchanged with the `44a6018e…` row intact. **Authentication withdrawal does not lose attachment titles, and the namespace-flip mechanism reasoned out above never applied.** **What the original observation actually was is unexplained, and is left that way rather than re-explained.** The likeliest account is that the first title was never durably written — its premise was never checked, and the write path can drop one silently. The observation is not retracted; it simply has no established cause and did not recur under a controlled single-variable test. **WHAT SURVIVES AS A REAL FINDING, RE-SCOPED AND DOWNGRADED: the title write path can fail silently.** `SessionDetailView:528-529` has two unguarded early returns — an attachment match by filename stem, and `guard let userID = sessionDetailNamespaceUserID(...)` — either of which discards a typed title with no error, no feedback and no log. Worth fixing on its own terms, independently of credentials | P3 | **Unverified** — the credential-state defect this row was opened for did **not** reproduce under a controlled test, and its proposed mechanism is withdrawn. What remains is the silent-failure surface in the write path: real by inspection, never observed failing. **Method note, worth more than the row itself:** the analysis was built on an unverified premise — that the title had ever been saved — and from it produced a confident, fully-reasoned, wrong mechanism, including an exhaustive proof that "nothing deleted it" that was true and beside the point. Check the premise before explaining the symptom | 5 |
| C-46 | **After a successful account deletion the app stayed on Profile, showing an emptied profile for an account that no longer existed, instead of returning to first-launch onboarding.** Observed on Device A, 2026-08-13, immediately after C-44's second run. **Caused by C-44's own sheet-sequencing change, and attributed rather than left as a coincidence.** Before that change the confirmation sheet was dismissed *after* `delete_account_v1` returned, and dismissing it took the profile hierarchy with it — which is why C-44 run 1 landed on onboarding. Moving the workflow to the sheet's `onDismiss` (so Apple's authorization UI gets a clean presenter) means `ProfileView` is now the top overlay for the whole workflow and survives the reset: **nothing in `LocalFactoryReset` clears `appRoute.isProfilePresented`, and nothing should — the reset owns data, not navigation.** Same family as C-24: a stale view of state that has been destroyed. **App-Review-facing**, because it is on the account-deletion path a reviewer will exercise deliberately, which is why it was fixed immediately rather than deferred to Phase 5 | P2 | **Resolved 2026-08-13.** `dismissProfileAfterSuccessfulErase()` calls the existing `onClose` closure, from exactly two sites, each immediately after a `LocalFactoryReset.perform` that completed — the Connected branch and the local-only branch. **Deliberately NOT called on the failure path:** dismissing Profile as generic cleanup would hide the error alert that tells the user their account was *not* deleted. Verified by grep that the `catch` block contains no dismissal, and that both call sites are immediately preceded by a completed reset. Debug and Release compile clean | 1 |
| C-45 | **Études observes neither `credentialRevokedNotification` nor `getCredentialState(forUserID:)`, and Sign in with Apple is the app's ONLY authentication mechanism.** Found 2026-08-13 while verifying C-44 against Apple's documentation. `grep` for `credentialRevokedNotification`, `getCredentialState` and `credentialState` across `MOTIVO/` returns **nothing** — neither symbol appears anywhere in the client, so no Apple credential-state signal is consumed at any point in the app's life. **The single-provider fact is what raises this above tidiness.** There is no email/password and no second identity provider, so the Apple credential is the *sole* basis for the authenticated session. When Apple reports `.revoked` — "The given user's authorization has been revoked and they should be signed out" — or `.notFound` — "The user hasn't established a relationship with Sign in with Apple" — **there is no independent login credential left that could justify retaining the authenticated state.** Yet the Supabase access and refresh tokens, the Keychain `appleUserID` and the `NetworkManager` bearer all survive, and the app goes on believing the user is signed in. The user's own revocation in Settings is therefore invisible to Études. **Independently, this is step 3 of TN3194's mandatory manual-revocation fallback** — "Respond to the credential revoked notification to revert the client to an unauthenticated state" — so **C-44 cannot be closed as TN3194-compliant while this is absent**, even though the two are built separately. **Kept separate from C-44 deliberately:** C-44 is Apple credentials, a new Edge Function and secret management; this is client-side lifecycle with no backend surface and no secret, and folding them would couple a release blocker to an Apple-credential dependency it does not need. **Scope when implemented:** observe `ASAuthorizationAppleIDProvider.credentialRevokedNotification`; call `getCredentialState(forUserID:)` against the stored `appleUserID` at launch and on foreground; on `.revoked` or `.notFound`, drive the app to an unauthenticated state through the existing `signOut()` primitive. **Apple's own note makes the polled check REQUIRED rather than belt-and-braces:** when a user permanently deletes their Apple Account, "For native apps, the system doesn't send a `credentialRevokedNotification`. Use `getCredentialState(forUserID:completion:)` to respond to account deletion events." Observing the notification alone would silently miss the account-deleted case. **Two constraints that must be respected and are easy to get wrong.** (i) **Invariant 1 — the local journal is never deleted by any Connected action.** Signing out is not erasing; `signOut()` preserves the journal today and this must never become a deletion path, however tempting `.notFound` looks. (ii) **`.notFound` needs guarding during a factory reset**, where the identity is legitimately being torn down — `LocalFactoryReset.isInProgress` is already the guard `ensureBackendIdentityIfNeeded` uses for exactly this class of hazard, and is the precedent to follow. **`.transferred` is out of scope** and belongs with any future app-transfer work (TN3159). **RUNTIME VERIFICATION PREDICTION, COMMITTED BEFORE THE RUN, 2026-08-13 19:51 UTC.** Fixture: Device A, Release, fresh SIWA identity `44a6018e…` created 19:50:30 UTC — one minute before this baseline — signed in through the existing-member route with **no purchase**, so the app is in Solo. Local data was created deliberately so that retention is not vacuous: practice sessions with notes, an attachment **with a user-typed title**, and a Score. Backend baseline: `auth.users` 16, `account_directory` 15, `posts` 98, `follows` 6, `post_comments` 3, `connected_attachments` 35, storage `attachments` 10, `avatars` 3. **Method:** revoke Études manually via Settings → Apple Account → Sign in with Apple, then foreground the app. **PREDICTED RESULT — authentication withdrawal, not deletion.** (1) `[C-45] check reason=foreground state=revoked action=clearConnectedIdentity` in the Release log (`state=notFound` equally acceptable; Apple documents no propagation timing, so `authorized` on a first check is an observation to record and re-check, not a failure). (2) The app drops to Solo / signed-out. (3) **ALL EIGHT BACKEND COUNTS UNCHANGED and the `44a6018e…` row still present** — this is what proves no `delete_account_v1` ran. (4) Every local item survives: sessions, notes, Score, media. (5) **THE ATTACHMENT'S TYPED TITLE SURVIVES — this is the load-bearing discriminator of the whole test.** `signOut()` deletes the per-user attachment title mappings (`AuthManager:880-883`) and `clearConnectedIdentity` does not; they are otherwise identical. A lost title would mean the wrong primitive is wired up, and no other assertion in this test could detect that. (6) No `[LocalFactoryReset]` lines and no return to onboarding. **Failure signatures, pre-registered:** onboarding screen or missing journal → `LocalFactoryReset` ran; `auth.users` 16 → 15 → `delete_account_v1` ran; title gone but everything else intact → `signOut()` is wired instead of `clearConnectedIdentity`. **RUN EXECUTED 2026-08-13. FIVE OF SIX ASSERTIONS PASSED; THE SIXTH EXPOSED A DIFFERENT DEFECT AND ITS PRE-REGISTERED EXPLANATION WAS WRONG.** Log: `[C-45] check reason=credentialRevokedNotification state=revoked action=clearConnectedIdentity`. **It fired through the NOTIFICATION path, not the foreground poll** — so `credentialRevokedNotification` is genuinely observed and working, which no other test could have shown. The app dropped to Solo; **all eight backend counts were unchanged and the `44a6018e…` row survived**, proving no `delete_account_v1` ran; no `[LocalFactoryReset]` lines appeared; sessions, notes, Scores and attachments all survived. **The sixth assertion failed: the attachment's typed title was gone.** **The pre-registered reading of that signature — "`signOut()` is wired instead of `clearConnectedIdentity`" — is FALSIFIED**, by the log line naming the action and by the earlier structural grep. The failure signature was right; the explanation attached to it was wrong, and the explanation is what a reader would have acted on. **Filed as C-47**, a namespacing defect that C-45 exposed rather than caused: nothing deleted the title, and the two sites that could have did not run. **C-45's own contract — withdraw authentication, erase nothing — held.** Whether C-45 counts as Resolved depends on whether the acceptance criterion "local metadata remains intact" is read as "not deleted" (met) or "still visible to the user" (not met); it is left open until C-47 lands, because the second reading is the one that matters to a user | **P1** | **RESOLVED 2026-08-13, device-verified.** A controlled single-variable run — manual revocation via Settings, with local data verified persistent beforehand — produced `[C-45] check reason=credentialRevokedNotification state=revoked action=clearConnectedIdentity`, dropped the app to Solo, and left **all nine backend measures unchanged** with the account row intact: no `delete_account_v1`, no `LocalFactoryReset`, no membership cleanup. **Journal, notes, Scores, attachments AND the user-typed attachment title all survived**, which is what distinguishes `clearConnectedIdentity` from `signOut()` and was the whole point of choosing it. It fired through the **notification** path rather than the foreground poll, so `credentialRevokedNotification` is confirmed observed and working; the polled path remains built and unexercised. **An earlier run appeared to fail the title assertion and did not — see C-47, where the cause was misdiagnosed and the premise had never been checked** | RC (before submission) |
| C-43 | **Unfollow and Remove Follower each delete BOTH directional follow rows, so one action destroys two relationships.** `FollowStore:263` (`removeFollower`) and `FollowStore:298` (`unfollow`) both call `BackendShim:746`, which delegates to `deleteRelationship` — and that function issues **two** unconditional DELETEs (`:763-766`): `follower=me & followed=other`, then `follower=other & followed=me`. `follows_delete_involved` permits either party to delete either row, so RLS allows it and this is **not a security defect**; the problem is that the effect is broader than the UI labels promise. **Concretely:** A taps *Unfollow* on B, intending to stop seeing B's posts — and B's approved follow of A is destroyed too, so B silently loses access to A's feed without A choosing that or B being told. A taps *Remove Follower* on B, intending to revoke B's access — and A's own follow of B is cancelled, so A silently stops receiving B's posts. `declineFollow` (`BackendShim:741`) routes here as well, so declining an incoming request also deletes any outgoing request or approved follow the decliner held toward that person. **The reason the code gives is not a product decision, and that is what makes this a defect rather than a design.** The comment justifies the double delete as *detection*: PostgREST returns 204 even when zero rows matched, so attempting only one direction could report success while deleting nothing. That is a true problem about error handling, and it produces a data-loss behaviour as a side effect. The two operations also have distinct names and distinct `FollowStore` entry points, which is hard to reconcile with them being meant to do the same thing. **NOTE FOR WHOEVER FIXES THIS — do not simply delete one of the two DELETEs.** That reintroduces the exact ambiguity the comment was written to solve, and the caller would be unable to tell a real deletion from a no-op. The fix needs a way to learn what was actually removed — `Prefer: return=representation`, which makes PostgREST return the deleted rows, or an exact count header — and only then can each operation be scoped to its own direction. Fix the reasoning and the behaviour together. **Found 2026-08-13 while establishing whether A/B could be returned to a fresh request state through normal UI for B-14's runtime check** — they can, precisely *because* one unfollow clears both rows. Filed separately and deliberately not investigated or fixed as part of B-14 | P2 | **Confirmed defect** — by trace; the mechanism is unambiguous in source, but the user-visible effect has not been observed on device. Settled by: with A and B mutually following, have A tap *Unfollow* and check whether B's follow of A also disappears from the backend | 5 |
| C-42 | **The membership selection screen showed prices in USD while Apple's payment sheet showed GBP.** Observed 2026-08-12 on **TestFlight build 131**, Device B, during the B1 run — the purchase itself completed normally and Connected activated, so this is a display defect, not a transaction one. **The obvious cause is ruled out already:** `MembershipSelectionView:147` renders `Text(product.displayPrice)`, which is StoreKit's own storefront-formatted string; there is no hardcoded currency, no `NumberFormatter`, and no locale override anywhere on this path (the four `Locale(identifier: "en_US_POSIX")` sites are all date parsing, unrelated). So the string is whatever StoreKit handed back, and the question is **which storefront the `Product` objects were resolved against**, not how they were formatted. **Most likely a storefront mismatch rather than a code defect:** `Product.products(for:)` resolves against the device's App Store storefront, while a sandbox purchase is charged against the **sandbox tester's** country. If those differ — a US App Store account with a GB sandbox tester, or products fetched before the sandbox account was recognised — the two surfaces legitimately disagree, and no user would ever see it in production. **That would make this the same class as C-9, C-29 and C-30: a discrepancy visible only because the test environment differs from the shipping one.** It is filed rather than dismissed because the opposite is also possible — a genuinely stale or wrongly-scoped product fetch would show real customers a price in the wrong currency on the screen where they decide to subscribe, which is a store-listing accuracy problem as well as a trust one. **Settled by** reading `Storefront.current` alongside the `displayPrice` values at the moment the selection screen renders, and comparing both with the sandbox tester's country in App Store Connect: identical storefronts with mismatched currencies means a real defect; different storefronts means a sandbox artefact and this closes. Do **not** attempt that read by re-adding instrumentation to the purchase path without the standing removal condition (see C-38) | P2 | **Unverified** — one observation on the distribution artifact; mechanism not established, and a sandbox-only artefact is at least as likely as a defect. Deliberately not investigated during the Group B run that surfaced it | 5 |
| C-41 | **Vestigial discovery plumbing: the client still carries a privacy control it no longer has.** `ff2d4ff` (2026-07-08, "Connected discovery simplification") settled that Connected discovery is always on and relationship privacy is explicit follow approval, but it pinned the values rather than removing the machinery. What remains: `AccountDirectoryService.upsertSelfRow` and `autoGenerateAccountIDIfMissing` still take `lookupEnabled:` and `followRequestsEnabled:` parameters and immediately discard them (`:320`, `:389`, `let effectiveLookupEnabled = true`), while `upsertSelfRowOnce:425-426` writes the literals `"lookup_enabled": true` and `"follow_requests_enabled": true`; four call sites compute or pass a value that cannot have an effect, including `AuthManager:395` deriving one from `ProfileStore.discoveryModeRaw`; the `DiscoveryMode` enum (`ProfileView:150`) survives with UI labels and no UI, pinned to `.search` at `:942`, `:1681` and `:1684`; and `AuthManager:454` hydrates `lookup_enabled` from the backend into `ProfileStore` where nothing reads it. **Not a behavioural defect** — every path forces the same value, so the system is consistent. The cost is that the source reads as though a discovery preference exists, which is exactly what led B-2 to propose gating the directory RPCs on `lookup_enabled`: a change that would have blanked names and avatars for existing followers. **The columns are separate from the plumbing.** Dropping `account_directory.lookup_enabled` / `follow_requests_enabled` is DDL and belongs with Phase 3's migration tooling, not with a client cleanup; note `follow_requests_enabled` is still read by `follow_requests_open` and therefore by the `follows_insert_requester` policy, so it is live even though nothing can set it to false. Recorded during the 2026-08-12 directory hardening and deliberately left untouched to keep that unit bounded | P3 | **Confirmed gap** — dead plumbing, no behavioural defect. Filed so the misleading shape is on the register rather than rediscovered as a finding | 6 |
| C-22 | 53 deprecated AVFoundation and SwiftUI APIs (`videoOrientation`, sync `duration`/`tracks`/`naturalSize`, `copyCGImage`, `exportAsynchronously`, `onChange(of:perform:)`) | P3 | **Confirmed gap** *(build)* | 6 |
| C-51 | **A queued publish can reach `loadIncludedAttachments` with stale stored paths after an ordinary app/container rotation — and that is the ONLY route to the exposure, which is why it was never observed.** The stale-path branch of upload attachment selection is not reachable through the shipping UI: the only publish trigger is `AddEditSessionView:2081`, reached via the editor's save, and ~80 lines earlier that same save rewrites `attachment.fileURL` from `existingAttachmentURLMap` — which holds **resolved** URLs. **The normal editor save therefore self-heals every stale path in the session before publish runs**, so `loadIncludedAttachments` never sees one. The genuine exposure is a publish **enqueued in `SessionSyncQueue`** that flushes *after* the container rotates: the queue is file-backed and survives launches, so an offline publish that queues, followed by an ordinary app update, arrives at upload selection holding paths from the previous container. **Container rotation on app update is established fact, not conjecture** — observed 2026-08-15 on Device A, where an in-place upgrade left five attachments carrying paths under the old container while a newly written one carried the new. **Backup/restore does NOT preserve this route**, because `SessionSyncQueue` lives in the excluded `Application Support/MOTIVO/` and does not restore — decision **P5** behaving exactly as designed, so a restored device cannot inherit historical pending-publish intent. **The code exposure is already closed by Phase 2 U2**, which routes `resolveLocalFileURL` through the canonical resolver; pre-U2 the consequence was that a shared post arrived with its media silently missing and no error anywhere. **What is outstanding is verification, not a fix.** **Phase 4 is the owner because Phase 4's shared-only upload architecture rewrites the upload-selection surface itself** — verifying stale-path behaviour belongs with the work that rewrites the function, and Phase 4 already carries A2's proxy-based network acceptance, which is the natural vehicle for observing what actually uploads. It is deliberately **not** Phase 3: B-4/B-12/B-13 are `delete_account_v1` and storage-pagination verification, a different subsystem that merely shares a fault-injection blocker | P3 | **Confirmed gap** — coverage, not a behavioural defect. Exercising it needs fault injection: enqueue a publish, force a container rotation, then flush. **NOT resolved, and Phase 4 implementation is not expanded now.** Filed 2026-08-15 from the Phase 2 F3 analysis | 4 |

| C-52 | **The shared scheme's Run action silently reverted to Debug AND re-pinned the synthetic StoreKit configuration, undoing the deliberate change that closed four findings — and it rode in on a commit about something else.** `67d64f0` (2026-08-11) removed `StoreKitConfigurationFileReference` and set the Run action to Release, deliberately, because a pinned configuration applies **regardless of build configuration** and had silenced real StoreKit in Release too. **`0daecd1` (2026-08-14, "B-6 RESOLVED: structurally hardened and device-verified") reverted BOTH** — `buildConfiguration Release -> Debug` and the `Etudes.storekit` reference re-added — and **its commit message does not mention the scheme at all**. It survived from 2026-08-14 to 2026-08-20 while `CLAUDE.md` asserted the opposite state in two separate places, including the standing instruction "The Run action must stay on Release". **Consequence, had U5 QA begun from the committed scheme:** Debug carries `com.samueldixon.motivo.dev`, which App Store Connect does not know, so `Product.products(for:)` returns empty and C-29's signature appears instead of a purchase; and the pinned configuration produces `AppStore.Environment.xcode` transactions signed by **Xcode's local test certificate rather than Apple's root**, which U5's server verifier must reject and which `membership_environment_check` excludes by construction. **Every U5 device gate run from the committed scheme would have failed, and would have failed looking exactly like a U5 defect.** Found 2026-08-20 by reading the scheme during the U5 audit rather than by trusting `CLAUDE.md`'s description of it | **P1** | **RESOLVED 2026-08-20 by U5a.** Reverted, and the revert is verifiable rather than asserted: the restored file is **byte-identical to `67d64f0`'s version, same blob `013cc35`**, so the tree is provably back to the deliberate state rather than hand-edited to resemble it. Verified by `xcodebuild -showBuildSettings`: the Run action's Release configuration resolves `PRODUCT_BUNDLE_IDENTIFIER = com.sdsongs.etudes` — the only ID that can transact — with no `DEBUG` compilation condition, and no StoreKit configuration pinned. Debug and Release both compile clean. **`CLAUDE.md` corrected, and the drift recorded there rather than silently repaired**, so the next reader learns that the file and the description disagreed for six days. **The lesson is not "check the scheme":** it is that a durable document asserting a repository fact is not evidence of that fact, and this one had been re-read many times without anyone checking it against the file. `JWSFreshnessProbe` now emits `env=` for exactly this reason — so a run made under a pinned configuration is caught in its own evidence | 3 |

| C-53 | **`Etudes.storekit` is compiled into the shipping app bundle, in Release, for device.** The `MOTIVO` folder is a `PBXFileSystemSynchronizedRootGroup` — CLAUDE.md's "everything in `MOTIVO/` is auto-included in the app target" — and its `membershipExceptions` list contains exactly three entries: `debug_upload_test.m4a`, `debug_upload_test.mp4`, `Info.plist`. **`Etudes.storekit` is not among them**, so the StoreKit configuration file is auto-included as a resource and copied into `Etudes.app`. **Observed directly, not inferred:** a clean Release device build at 2026-08-20 20:52:19 contains `Etudes.app/Etudes.storekit` copied at 20:52:15 in that same build, **with the scheme's StoreKit Configuration set to `None`**. Someone already met this mechanism and excepted two debug media files; the StoreKit configuration was missed. **This is a shipping defect on its own terms** — the file carries `_developerTeamID`, `_applicationInternalID` and the full product definitions, and has no business in an App Store binary. **Found 2026-08-20 during the F3b run**, when the purchase sheet read "Xcode" on a real device in Release with the scheme confirmed clean, which is what prompted looking past the scheme. **C-52's fix was necessary but not sufficient**: unpinning the scheme closed one route to the Xcode environment while this second, independent route was shipping the file the whole time | P2 | **RESOLVED 2026-08-20, with Xcode closed.** One line added to `membershipExceptions`, and **that single line is the entire change** — `git diff` is one insertion in `project.pbxproj` and **zero Swift files**. **Proven by measurement, not by rebuilding and asserting:** the Release bundle inventory was captured BEFORE the change (15 items, `Etudes.storekit` present) and after (14 items), and the diff is **exactly one line — the removal of `Etudes.storekit` and nothing else**. A recursive search finds it nowhere in the bundle, and the Debug bundle is clean too. Debug and Release both compile clean; `plutil -lint` passes on the project file; `xcodebuild -list` still reports 3 targets and 2 configurations. The shared scheme was **not touched** (zero scheme files in the diff; Run action still Release with no configuration pinned). **Behaviour cannot have changed, and the reason is structural rather than reassuring:** no source file loads `Etudes.storekit` from the bundle — every `storekit` match in Swift is the framework name in a comment or an `import` — so nothing at runtime could have been reading it. The product identifiers are untouched, and the exception set is scoped to the `MOTIVO` app target alone, leaving both test targets unaffected. **The file is retained on disk** at `MOTIVO/Etudes.storekit`, so attaching it deliberately in Run -> Options still works; it is now genuinely opt-in, which is what `CLAUDE.md` claimed since 2026-08-11 and what was not true until now. **THE CAUSAL CLAIM IS STILL NOT MADE AND IS NOT RESOLVED BY THIS FIX.** The earlier fix was deliberately not applied while Xcode was open, because this session had just watched Xcode's in-memory state override an on-disk scheme edit. **The causal claim is deliberately NOT made.** That the file ships is fact; that it *selects* the Xcode StoreKit environment is the leading hypothesis and is **contradicted in part by TestFlight build 131**, which almost certainly shipped the same file (its mtime is 2026-08-09, before that build) and yet transacted on genuine sandbox StoreKit with a real purchase (B1 PASS). The refined hypothesis — bundled config **plus** launched-from-Xcode selects local StoreKit, while TestFlight ignores it — fits both observations and is **unproven**. **F3b's own run does not settle it either**: the tester reached a genuine Sandbox sheet after clearing Run -> Options, so the environment was correct at the time the evidence was taken, and the P2 result stands regardless. **Fix and re-verify before the next device gate**, and do not let "F3b passed" be read as "this is resolved" | 3 |

| C-54 | **The unit test target has been UNRUNNABLE since the product was renamed, and nobody noticed because nobody ran it.** `MOTIVOTests` sets `TEST_HOST = "$(BUILT_PRODUCTS_DIR)/MOTIVO.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/MOTIVO"` in both Debug and Release, while the product has been `Etudes.app/Etudes` for the whole life of this audit — `PRODUCT_NAME = Etudes`. Any `xcodebuild test` fails before compiling a line: *"Could not find test host for MOTIVOTests: TEST_HOST evaluates to .../MOTIVO.app/MOTIVO"*. **`CLAUDE.md` has said "Unit test suite is an empty template" since the beginning and that UNDERSTATES it** — the suite is not merely empty, it could not have been executed even if somebody had written tests, so "empty template" was a description of the symptom rather than the cause. Found 2026-08-23 at U5e, by writing the first real tests in the target and attempting to run them | P3 | **RESOLVED 2026-08-23, with Xcode closed. THE SEVEN U5e TESTS NOW EXECUTE AND PASS**, named individually in the run log: `attestationBodyIsJwsOnly`, `attestationBodyHasNoIdentityFields`, `outcomeMapping`, `unknownOutcomeIsNotSuccess`, `bindingTokenDecodes`, `bindingTokenRefusesJunk`, `endpointConstruction` — `** TEST SUCCEEDED **`, run **normally** with no command-line override. **THE DEFECT HAD TWO PARTS AND ONLY THE FIRST WAS VISIBLE FROM THE ERROR MESSAGE.** (i) `TEST_HOST` pointed at `MOTIVO.app/MOTIVO`; corrected to `Etudes.app/Etudes` in both configurations. (ii) With that fixed the build got *further* and failed on `@testable import MOTIVO` — **the app's Swift module is `Etudes`** (`PRODUCT_MODULE_NAME`), so the generated template's import had been stale since the rename too. **Neither was ever reached before, for the same reason: the target could not be built at all, so its second stale reference was hidden behind its first.** A third, unrelated adjustment was needed to run them — both services are `@MainActor`, so the tests are annotated `@MainActor` rather than relaxing the shipping types' concurrency surface, which a test-host fix has no business changing. **The delta is exactly what was intended and is measured, not asserted:** `git diff` on `project.pbxproj` is **4 lines, all of them `TEST_HOST`**, with **zero** references to `USES_XCTRUNNER` or the UI-test target; `plutil -lint` passes; Debug and Release both build clean; the U5e structural suite still passes 26 of 26. **`CLAUDE.md`'s standing "unit test suite is an empty template" line is corrected in the same pass** — it described a symptom whose cause is this row. **The fix was one setting in two configurations:** point `TEST_HOST` at `Etudes.app/Etudes`. **Deliberately not applied on 2026-08-23 because Xcode was open on the project**, and this session has twice watched Xcode's in-memory state overwrite on-disk project edits (C-52, and the Run -> Options recurrence during F3b). **A command-line override cannot work around it**: `xcodebuild test ... TEST_HOST=...` applies project-wide and `MOTIVOUITests` then fails with *"sets both USES_XCTRUNNER and either TEST_HOST or RUNTIME_TEST_HOST"*, so the two targets' requirements are mutually exclusive under a global override. **Consequence for U5e, stated rather than glossed: the seven unit tests written at U5e are COMMITTED AND NOT RUN.** They are not evidence yet. The U5e structural suite (26 assertions) and both app builds are unaffected and did run | 3 / RC |
| C-55 | **RESOLVED 2026-09-01 — the mechanism was a runaway SwiftUI re-render loop, NOT a navigation defect. See "C-55 — RESOLVED" below for the measured mechanism and the Device A acceptance.** **ON DEVICE A IN SOLO MODE, LOCAL SESSIONS DISPLAY IN `ContentView` BUT TAPPING THEM DOES NOT OPEN `SessionDetailView`.** Observed 2026-09-01. Both a session published earlier that day **and a brand-new session created and saved locally in Solo, with an audio attachment**, render in the journal list; tapping either does nothing — no navigation, not a blank screen. **THE ORIGINAL DEVICE B CONTRAST IS WITHDRAWN AS EVIDENCE, 2026-09-01 — IT WAS A DIFFERENT BUILD.** It was recorded as "the same client build" from inherited context rather than measurement; the premise was challenged and checked. **`devicectl`: SD beta burner (iPhone 16e) = Device A, carrying ONLY `com.sdsongs.etudes` (Release); SD iPhone (iPhone 17 Pro) = Device B, carrying BOTH `com.sdsongs.etudes` AND `com.samueldixon.motivo.dev` ("Études Dev", Debug).** The earlier Device B observation was almost certainly the Debug app — a different bundle with a different container — so it was never a controlled comparison. **REPLACED BY A FULLY CONTROLLED THREE-WAY COMPARISON, MEASURED 2026-09-01:**
| C-57 | **A SERVER AUTHORISATION DENIAL COLLAPSED A LOCALLY ENTITLED CLIENT INTO SOLO, IRREVERSIBLY, BY DELETING ITS CREDENTIALS.** `NetworkManager.request` routed **both 401 and 403** through `onAuthChallenge` (`NetworkManager.swift:282`, Phase 14.2.2). That handler is `AuthManager.ensureValidSession(reason: "network-auth-challenge")`, whose own comment states the design: *"refresh once, retry once; otherwise collapse to signed-out."* On a non-transient refresh failure it calls `signOut()`, which **deletes the Supabase access and refresh tokens from the Keychain**. `hasConnectedIdentity` then goes false and `ProductionAppModeActivation.resolve`'s third guard returns `.solo`. **MEASURED ON DEVICE A, 2026-09-02, from a retrospective device log archive.** After U6b enforcement was bound at 09:33:14 UTC, **the observed post-bind requests produced 403 AUTHORISATION responses — 32 of them in the launch at 10:34:27 BST, alongside 54× 200, 3× 400 and one 429 — and ZERO 401s anywhere in the archive.** The launch immediately before the bind saw 53× 200 and **no 403 at all**. The `[C-45]` credential monitor reported `state=authorized action=none` throughout, **including at 10:34:28**, so the Apple credential was never revoked and that path is eliminated. **403 is an AUTHORISATION denial and 401 is an AUTHENTICATION failure.** Refreshing a token cannot change an authorisation outcome, so the retry is guaranteed to fail and the only effect is the sign-out. **The sign-out is STICKY** — it destroys the stored credentials — **which is why the Solo state survived rolling enforcement back**, and why an earlier reading of that persistence as evidence *against* enforcement was wrong. | **P1** | **RESOLVED AND DEVICE-VERIFIED 2026-09-02.** **The fix is proven by a before/after on the SAME device under the SAME denials:** before it, 32 x 403 at 10:34 produced `signOut()`, deleted credentials and an irreversible collapse to Solo; after it, **17 denials at 10:42-10:46 and the client STAYED CONNECTED** — session alive at 10:42:30, `auth.users` still 17, `membership_binding` untouched, conflicts 0. **Fixed by the smallest possible change: the auth challenge now triggers on 401 ONLY.** `(status == 401 || status == 403)` → `status == 401`. 403 propagates to the caller through the ordinary denial path, which is what an authorisation denial is. Nothing else in `NetworkManager` or `AuthManager` was altered, and U6b was not touched. **IT VIOLATED A SETTLED ARCHITECTURAL RULE** — *local StoreKit entitlement governs client Solo vs Connected; authoritative server entitlement governs API access; a legitimate server-side denial must never itself collapse a locally entitled client into Solo.* **U6b DID NOT INTRODUCE THIS DEFECT — it made a latent one reachable**, because before enforcement no policy ever denied an authenticated member and 403 essentially never occurred. **A CONSEQUENCE WORTH RECORDING: C-35 exposure.** Account-deletion authority is `hasConnectedIdentity`, so a member signed out by this path may have been unable to reach the deletion route without re-subscribing — exactly what C-35 was filed to prevent. Observed reachable *before* the drop during QA; **not tested after it**, and the fix removes the mechanism rather than the need to check. **VERIFICATION.** `NetworkManager` is a singleton over `URLSession.shared` with a `private init()`, so it is **not injectable and a true behavioural unit test is impossible without redesigning it** — out of scope, and stated rather than glossed. Verified instead by **comment-stripped structural assertions** (C57-1..7 in `supabase/tests/u5/client-structural.sh`, **60/60 green**): zero `403` in `NetworkManager` CODE, exactly one `status == 401` challenge, the hook still wired to `ensureValidSession`, and `FollowStore`'s separate 401/403 *message* path untouched. **Comment stripping is load-bearing here** — the fix's own comment explains 403 four times and would have defeated a naive check, U5c-34's shape anticipated rather than repeated. **C57-7 was first written comparing a value to itself and could never fail; caught on its first run and pinned to a number.** Debug and Release both build clean; **MOTIVOTests 15/15**. | 4 |
| C-58 | **A SURVIVING FOLLOW RELATIONSHIP IS NOT INTELLIGIBLE TO THE LAPSED VIEWER ALLOWED TO MANAGE IT.** Under U6b enforcement an unentitled viewer sees followers rendered as **`User • <last 6 of user_id>`** (`PeopleUserRow.swift:86`) rather than a display name. **MECHANISM, TRACED AND NOT A BUG.** `PeopleView` resolves identities through `AccountDirectoryService.resolveAccounts` → `rest/v1/rpc/get_account_directory_by_user_ids` — **the correct retained-attribution RPC**, not `search_account_directory`, which it uses only behind the explicit search field. That RPC is **viewer-gated** under U6b, so an unentitled viewer receives nothing and the row falls back. **G10 is intact:** it forbids gating on the SUBJECT, and a lapsed author's attribution still resolves for an entitled viewer; what fires here is the VIEWER gate. **FOLLOWS IS THE ONLY SURFACE WHERE THIS BITES.** `resolveAccounts` also backs comments, feed rows and session detail, but an unentitled viewer cannot reach that content anyway. `follows` SELECT is deliberately **ungated** (D-U6-2 — you cannot delete what you cannot see), so it is the one place where rows survive that need attribution the viewer cannot obtain. | **P3** | **OPEN — bounded product/UX follow-up, NOT a P6 blocker and NOT a defect.** The current fallback is **safe** and relationship withdrawal remains fully functional; the row renders and unfollow/remove works. **PRODUCT DECISION TAKEN 2026-09-02, implementation deferred.** `User • <suffix>` is safe but is not the finished UX: *a surviving follow relationship should remain intelligible to the lapsed viewer who is allowed to manage and remove it.* **THE GLOBAL FIX IS REJECTED:** ungating `get_account_directory_by_user_ids` for unentitled viewers would **recreate a general UUID→identity resolver**, which is exactly what B-5 hardened these RPCs against. **ADOPTED DIRECTION — the narrow option:** permit retained identity resolution for an unentitled viewer **only where that viewer already holds an existing follow relationship with the requested `user_id`**. That reveals no newly discoverable relationship — the viewer already knows the row exists — and it makes an already-surviving relationship intelligible. **Not implemented; no code written; scope and shape to be settled when it is picked up.** | 4 |

| Device | Bundle | Mode | Journal row → `SessionDetailView` |
|---|---|---|---|
| **A** — SD beta burner | `com.sdsongs.etudes` (Release) | Solo | **FAIL** |
| **B** — SD iPhone | `com.sdsongs.etudes` (Release) | Solo | **PASS** |
| **B** — SD iPhone | `com.samueldixon.motivo.dev` (Debug) | Solo | **PASS** |

**WHAT IS AND IS NOT HELD CONSTANT, stated narrowly because the stronger form is not supported.** **Release CONFIGURATION is held constant** — both devices run the `com.sdsongs.etudes` bundle — and **Solo mode is held constant**. **Those two variables therefore do not explain the PASS/FAIL difference.** **It is NOT established that the BUILD is held constant.** Both report `1.0 (131)`, but that string is hard-coded and never incremented, so **the exact source binary or commit installed on either device cannot be identified or proved**. **Remaining variables are therefore TWO, not one:** the device and its local state, **and an unmeasured possible difference in the installed Release binary**. **The Debug row is an additional control only** — it rules configuration out as a confounder; **it is not the controlled A/B binary comparison** and must not be read as one. **Mechanism remains UNKNOWN.** On Device A the failure persists across a device restart, reproduces on **both pre-existing sessions and a brand-new locally created one**, and at least one of those same sessions **opens successfully via MeView → Highest Focus** — so `SessionDetailView`, the session data, Core Data and navigation elsewhere all work there.** **THE PATH IS ENTIRELY LOCAL.** A journal row's tap sets `pushSessionID` (`ContentView.swift:1294`); only the feed sets `pushRemotePostID` (`:1437`). The destination resolves `sessions.first(where: id == pushSessionID)` against a `@FetchRequest` over all local sessions and is attached **directly inside `NavigationStack` in `SessionsRootView.body`, not within any `canViewFeed` conditional** — so "the destination is not attached in Solo" was considered and is **false**. **Therefore this is NOT explained by the contemporaneous Supabase service incident** (no backend call is involved) **nor by U6b server enforcement** (which shipped zero client code, and which was rolled back at 16:58:29 while the symptom persisted). | **P2** | **RESOLVED 2026-09-01 — the mechanism is measured and the fix is accepted on Device A. See "C-55 — RESOLVED" below.** The cause was a runaway SwiftUI re-render loop saturating the main thread, and the tap was a casualty of it; two no-op guards removed it and **no navigation code was changed**. **THE INVESTIGATIVE TEXT BELOW IS RETAINED AS THE RECORD OF HOW IT WAS WORKED, AND ONE PART OF IT IS NOW KNOWN TO BE A DEAD END:** the two `.navigationDestination(isPresented:)` modifiers, correctly recorded here as a factual difference and explicitly NOT adopted as an explanation, were subsequently adopted as the leading hypothesis and **falsified by experiment** — a clean install of HEAD opens the row normally. **The refusal to record it as a mechanism was right, and the later failure to keep that discipline is the lesson.** Everything below stands as written at the time. — *superseded:* **OPEN. MECHANISM UNKNOWN — BUT NARROWED SHARPLY BY A DISCRIMINATOR, 2026-09-01.** **Device A was restarted; journal rows still do not open. The SAME session then opened successfully in `SessionDetailView` from MeView, where it appears as "Highest Focus".** **That eliminates four candidate explanations at once:** `SessionDetailView` itself works; the session's local data resolves and renders; Core Data on that device is not broken; and navigation elsewhere in the app works, so the `NavigationStack` is not wedged — a hypothesis that was raised earlier and is now **dead**. **THE FAILURE IS SPECIFIC TO THE `ContentView`/`SessionsRootView` JOURNAL-ROW → `SessionDetailView` PATH ON DEVICE A IN ITS CURRENT SOLO STATE, AND REPRODUCIBLE THERE.** **Two factual differences between the working and failing paths, recorded because the next investigator needs them and NOT as a proposed mechanism:** (i) MeView holds the **Session object** (`selectedInsightSession`) and passes it straight to `SessionDetailView`, while `SessionsRootView` holds a **UUID** (`pushSessionID`) and must resolve it via `sessions.first(where: id == pushSessionID)`, rendering `EmptyView()` on a miss — the working path has no lookup and the failing path depends on one; (ii) `SessionsRootView` carries **two** `.navigationDestination(isPresented:)` modifiers on the same view (`pushSessionID` and `pushRemotePostID`), where MeView carries one. **Neither difference is adopted as an explanation. No mechanism is proposed, and none should be recorded until one is measured** — C-47 is on file in this repository for a confident, fully-reasoned, wrong mechanism produced from exactly this position. **WHAT THE EVIDENCE DOES AND DOES NOT SUPPORT, stated precisely because the distinction is easy to lose:** two devices running the same *bundle and configuration* with different behaviour **rules out a newly deployed client binary as the cause**. **IT DOES NOT ESTABLISH THAT THE TWO BINARIES ARE IDENTICAL, AND NOTHING AVAILABLE CAN:** both report `1.0 (131)` because `MARKETING_VERSION` and `CURRENT_PROJECT_VERSION` are hard-coded in the project and never incremented, so **the version string cannot distinguish any two builds from this project, ever**; `devicectl` exposes no install date or hash; and the app displays no version anywhere. **That gap is what made the premise uncheckable, and it must not be cited as if it were confirmation.** It does **NOT** rule out a latent, state-dependent client defect, and it must not be recorded as if it did. Nor is there evidence that the `NavigationStack` is wedged — that was a hypothesis, it is untested, and it is not a finding. **THAT ONE-TAP DISCRIMINATOR HAS NOW BEEN RUN and is recorded above: other navigation works, so the failure is narrowed to the session-detail destination in `SessionsRootView` rather than the view hierarchy.** **THE SUPABASE INCIDENT CANNOT EXPLAIN THIS ONE, AND THE DETAIL MAKES THAT SHARPER RATHER THAN VAGUER.** The contemporaneous fault was **HTTP 401 JWT rejections at PostgREST** (persisted on 14.17, rolled back to 14.5). **Every symptom that fault explains is an authenticated-request failure** — Device B's empty feed with its empty-state absent, attribution falling back to `User`. **The journal tap makes no authenticated request at all**: it sets `pushSessionID` and the destination resolves against a local `@FetchRequest`. A PostgREST authentication fault has no path to it. **DEVICE A'S STATE MUST BE PRESERVED — do not reinstall, erase or reset it, and do not restart the Supabase project.** It is currently the only instance of this condition, and its local state is the evidence. **Device A in its current Solo state is now a USEFUL REPRODUCTION FIXTURE, not merely evidence** — the failure is reproducible there and nowhere else available. Preserving it is worth more than clearing it. **No client debugging unit has been opened**, deliberately: this is kept separate from U6b and from the PostgREST 401 incident, neither of which can reach a local navigation path. | 4 |

| C-56 | **`PracticeTimerView.requiresAppSetUpNow()` PERFORMS CORE DATA FETCHES FROM INSIDE `body`.** Filed 2026-09-01 from the C-55 investigation; **NOT investigated and NOT fixed — deliberately out of that unit's scope.** `PracticeTimerView.body` (`:1572`) reads `shouldRenderAppSetUpRoot` (`:1254`), which calls `requiresAppSetUpNow()` (`:1233`); whenever its early return does not hit — `auth.isSignedIn && BackendConfig.isConfigured && auth.backendBootstrapState == .existingAccount` — it runs a `Profile` fetch **plus** `fetchInstruments()` against `viewContext`, on every body evaluation. **Measured, not argued:** during C-55 the onboarding screen sat at ~100% CPU with **47 of 50 samples inside this one function**. **IT IS NOT A LOOP DRIVER AND MUST NOT BE RECORDED AS ONE** — it was the per-iteration *cost* inside C-55's re-entry loop, established by the fact that C-55's two-guard fix took the same screen to **0.1% with these fetches untouched**. So it is currently **latent and symptomless**, which is exactly why it is filed rather than fixed: a body-time fetch is a defect whose cost is bounded only by how often something else invalidates the view, and the next such loop makes it expensive again. Fixing it means deciding where that state should be computed instead, which is a design question, not a guard. **No fix attempted; no evidence gathered beyond the sample above.** | P3 | Open | 5 |

## C-55 — RESOLVED 2026-09-01. A render loop, not a navigation defect

**THE NAVIGATION HYPOTHESIS WAS WRONG AND WAS KILLED BY AN EXPERIMENT, NOT BY
MORE READING.** The leading explanation was the two stacked
`navigationDestination(isPresented:)` modifiers on one view in `ContentView`
(introduced by `182fed5`, whose message claims "No UI, filtering, rendering, or
interaction changes"). It predicted that a clean install of HEAD would fail.
**A fresh simulator install, Solo, one locally created session, opened
`SessionDetailView` normally.** The hypothesis died there. **No navigation code
was changed by this fix, and none needed to be.**

### The measured mechanism

An `.onReceive` attached to the **root scene body** mutated state that
invalidated **that same body**. `@Published` re-delivers its current value to
each new subscriber, and `removeDuplicates()` is reconstructed on every body
evaluation with no memory of prior values — so the handler ran again on the next
frame. Self-sustaining, off the display refresh.

**Device A, live process, read-only `xctrace --attach` Time Profiler, 12 s —
50 of 4,418 backtraces on this path:**

```
CA::Transaction::commit()                      ← every frame
  -> _UIHostingView.layoutSubviews()
    -> ViewGraphRootValueUpdater.render(...)
      -> Update.dispatchActions()
        -> SubscriptionView.Subscriber.updateValue()      ← onReceive firing
          -> closure #6 in closure #1 in MOTIVOApp.body.getter
            -> AppModeManager.applyActivation(auth:isEntitled:)
              -> applyBackendRuntimeMode(for:) -> setBackendMode(_:)
                -> -[NSUserDefaults setObject:forKey:] -> NotificationCenter post
```

The simulator reproduced the same shape through the sibling branch, where the
leaf is named exactly: `MOTIVOApp.swift:403 -> MembershipAttestationCoordinator
.reset() -> Published.subscript.setter -> ObservableObjectPublisher.send()`.

**Two writes that were INTENDED to be no-ops and were not:**

- `AppModeManager.applyMode` called `applyBackendRuntimeMode` **before** its own
  `guard self.mode != mode else { return }`, so `setBackendMode` wrote
  `UserDefaults` and posted a change notification on every call. Device A's arm.
- `MembershipAttestationCoordinator.reset()` wrote `lastOutcome = nil` and
  `isAttesting = false` unconditionally. `@Published` has no equality test, so
  writing `nil` over `nil` still sends `objectWillChange` on a root
  `@StateObject`. The simulator's arm.

### The fix — 18 added lines, two guards, zero behaviour change

`setBackendMode` returns without writing when the stored value already equals the
requested one. `reset()` returns early only when there is genuinely nothing to
clear. Both make an already-intended no-op actually be one.

### Causality is established, and the two halves are stated separately

**The loop is causally established.** A/B/A on the simulator: guard in 0.1% CPU
-> guard reverted **12.9%** and the loop frames return (`reset` 3,
`applyActivation` 10, `MOTIVOApp.body.getter` 70) -> guard restored 0.1% and all
zero. **The fix was falsified on demand and survived.**

**Separately: on the original failing Device A, removing the loop also restored
`ContentView` journal -> `SessionDetailView`**, scored against a rule fixed
before the run (loop gone + row opens = resolved; loop gone + row still fails =
performance defect fixed and C-55 stays open). Both halves landed:

| Device A, idle on the journal | Before | After |
|---|---|---|
| Time-profile export | 8,910,605 bytes | **1,032 bytes — schema only** |
| Backtraces in 12 s | 4,418 | **0** |
| `applyActivation` / `setBackendMode` / `NSUserDefaults` | present | **0** |
| `SessionRow.body.getter` | present | **0** |
| View Body Updates / 10 s | 29,764 | not re-measured (corroboration only) |
| Journal row -> `SessionDetailView` | **FAIL** | **OPENS** |

**Zero samples was checked for validity before being believed** — the trace TOC
confirms `process type="attached"` pid 780, 12.785 s, "Time limit reached". An
idle process yields no samples; a failed attach looks identical from outside.
**The install was verified rather than assumed:** binary UUID moved
`79072CC6-...` -> `5C03043A-...` and the device bundle container
`AAB80DFA-...` -> `AF7BBB85-...`. Both builds report `1.0 (131)`, so the version
string could not have told us which was running — the same ambiguity that sent
the first day of this investigation down the wrong path.

**WHY DEVICE B NEVER EXHIBITED THE LOOP IS UNKNOWN, AND SAYING SO IS PART OF THE
CLOSURE.** Device A's arm (`applyActivation` -> `setBackendMode`) has existed
since `df84e08` (2026-07-08) and `1074ece` (2026-07-21) — **well before any
plausible Device B build — so "B is older" does NOT explain B working.** It is
unmeasured, it blocks nothing, and B's Release install is deliberately left
untouched as a fixture in case the question is ever worth settling.

### The mechanism was already written down three weeks earlier

**"`removeDuplicates()` does not dedupe across renders"**, in this same file
under "Activation path — observed behaviour, 2026-08-11", states the re-entry
mechanism exactly: *"SwiftUI reconstructs `$membershipState.removeDuplicates()`
on every body evaluation, and a fresh publisher has no memory of prior values,
so each re-subscription re-emits the current one."* It was recorded as an
**observation about C-38** and never read as a defect. **The knowledge was not
missing; the connection was.** That is a different failure from the ones this
file usually records — not a document drifting from reality, but a correct
observation nobody re-read in a new context.

### One incidental consequence, and one thing NOT fixed

The 100% CPU stall on the first-run setup screen is gone (0.1% after). Input was
being **silently dropped** there — a defect every new user would have met.

**`PracticeTimerView.requiresAppSetUpNow()` is NOT fixed** — see C-56. It was the
per-iteration cost inside this loop, never its driver, which is why onboarding
now idles at 0.1% with those fetches untouched.

## Activation path — observed behaviour, 2026-08-11

From `ActivationTrace` on instrumented TestFlight build 130, on real StoreKit.
**These are observations, not findings.** They are recorded because the
activation path had never been watched at runtime, two C-38 hypotheses died on
contact with this data, and the next person to touch this code should not have
to rediscover it.

**The healthy sequence.** Activation needs five gates and they satisfy in a
fixed order, not simultaneously — the last one to arrive is what flips the mode:

```
appOnAppear        entitled=false signedIn=false token=false backendID=false → solo
entitlementRead    resolved=entitled                     (~60 ms after launch)
membershipEntitled entitled=true  signedIn=false                             → solo
                   …Sign in with Apple…
membershipEntitled entitled=true  signedIn=true  token=false                 → solo
backendUserIDChanged              signedIn=true  token=true  backendID=false → solo
signIn stage=supabaseSessionStored signedIn=true token=true  backendID=true
membershipEntitled entitled=true  signedIn=true  token=true  backendID=true  → CONNECTED
```

The Apple credential (`signedIn`), the Supabase exchange (`token`) and the
derived `backendID` land as three separate steps roughly 1.3 s apart.

**`removeDuplicates()` does not dedupe across renders.** The activation triple —
`membershipEntitled`, `currentUserIDChanged`, `backendUserIDChanged` — fired
**thirteen times in twenty-one seconds**, clustered around UI events rather than
on a timer, and fired after a `membershipState entitled -> entitled
changed=false` write. SwiftUI reconstructs
`$membershipState.removeDuplicates()` on every body evaluation, and a fresh
publisher has no memory of prior values, so each re-subscription re-emits the
current one. **Any reasoning of the form "that transition is consumed and cannot
fire again" is unsafe here** — which is exactly how C-38's second explanation
died. Note this does *not* clear C-24, whose completion paths live in
`ProfileView` rather than on this publisher.

**`@Published` delivers to `onReceive` on `willSet`.** Activation is logged
*before* the `didSet` probe records the change. Ordering in these logs is not
evidence of causation running backwards.

**Two `refreshEntitlement` calls run concurrently at launch.** At 17:14:43.767,
`membershipState` reached `.entitled` *before* `entitlementRead` logged — only
possible if a waiter awaited the in-flight task and published its result first,
then the owner resumed to log its own read and publish the same value again.
`start()`'s refresh racing the `scenePhase == .active` refresh is the likely
pair. **Relevant to C-38 if it is ever confirmed:** `purchase()` calls
`refreshEntitlement(forceAfterCurrent: true)`, and if a refresh is already in
flight — which the StoreKit sheet dismissing makes likely, as C-13's cell warns —
the waiter path publishes that in-flight result *before* performing its forced
fresh read. That is a window for a stale value to be published, and it is a
different shape from the single-stale-read hypothesis C-38 currently carries.

**C-24 re-verified on real StoreKit.** The 17:21 run was a reinstall with a live
entitlement: routed past membership selection straight to sign-in, and reached
Connected cleanly. Previously verified under the synthetic configuration only.

### The first purchase ever watched on Apple's sandbox — 2026-08-11, ~20:44

Device A, fresh Sandbox Apple Account, development build run from Xcode with the
StoreKit configuration cleared. This closed C-38; two further observations are
worth keeping.

**The whole acquisition path, end to end, with no anomaly:**

```
entitlementRead   resolved=notEntitled forced=false wasInFlight=false   (×many, pre-purchase)
                  …Apple "Sandbox" payment sheet, confirmed…
entitlementRead   resolved=entitled    forced=true  wasInFlight=false
membershipState   notEntitled -> entitled changed=true
entitlementRead   resolved=entitled    forced=true  wasInFlight=false
purchaseGuard     isEntitled=true
                  …Sign in with Apple…
signIn            stage=supabaseSessionStored signedIn=true token=true backendID=true
activation        reason=membershipEntitled … resolved=connected previous=solo
```

**C-1's observed mechanism did not recur either, and that is worth stating
carefully.** C-1 records — from three independent runs — that a forced refresh
fired by `transactionUpdate.verified` immediately after a verified purchase
published `notEntitled`, and only a later refresh published `entitled`. Here both
forced refreshes resolved `entitled` on their first read. **C-1's observation is
not retracted and its fix is untouched:** removing client authority over
irreversible deletion is correct whether or not a negative read is ever spurious,
because invariant 3 turns on the *reversibility* of the decision, not on the
frequency of the bad input. What this run does narrow is the environment — like
C-38, the spurious negative has only ever been seen under `Etudes.storekit`.

**`forced` and `wasInFlight` distinguish the two refresh paths, and it is not
obvious from the log alone.** A direct forced refresh logs `forced=true`; a
forced *re-entry* after waiting recurses into `refreshEntitlement()` with the
slot already cleared, so it logs `forced=false wasInFlight=false` — identical to
an ordinary launch refresh. The waiter itself publishes at `:250` and never logs
a read at all. So the signature of a waiter having run is **a `membershipState`
publish with no `entitlementRead` of its own preceding it**, which is exactly
what both launches in this session show and what neither purchase shows.

**Instrumentation removed immediately after this run**, per the standing
condition. Everything above is the record it left behind.

## Copy alignment (not code defects)

| ID | Item | State | Phase |
|---|---|---|---|
| D-1 | "Share with followers" defaults ON. **Behaviour is intentional and unchanged** — chosen from TestFlight evidence that the opposite default produced empty feeds. Changeable via the persistent "Default to Private Posts" preference; per-session toggle retained; notes and attachments keep independent controls. Copy task: scope "private by default" claims to Études proper, state that Connected follows the user's sharing preference and where to change it, note that Thoughts default to private. Must match the revised App Store privacy labels. | **Product decision settled** — copy not yet written | 4 |
| D-2 | Notes on unshared sessions uploaded unless separately marked private | **Product decision settled** — dissolves under shared-only uploads | 4 |

---

## Backend findings

| ID | Finding | Sev | State | Phase |
|---|---|---|---|---|
| B-1 | `delete_account_v1` recursive sweep of `attachments/users/<uid>` destroys Connected assets other members still reference. **RULE REVISED 2026-08-13 — attachments a departing member sent are now DELETED, rows and objects, even where a live recipient reference remains.** Not a correction: the retention rule was right given the architecture and settled decisions as they stood. The input changed — Apple's account-deletion guidance treats content shared with others as UGC that deletion should remove, and the deployed representation could not sustain the "that is the recipient's copy" defence the rule leaned on, because `sender_user_id` is NOT NULL with no FK and `connected_attachments_sender_storage_path` pins the object to `users/<sender>/connected/…`. **Verified 2026-08-13 on Device A:** `b105d553`, a live sent reference held by Account B, was deleted with its storage object — the row the previous function explicitly preserved, which is what makes the run discriminating. A recipient-owned representation on a neutral path is a Phase 4 question | P0 | **Resolved** — device-verified 2026-08-11 on the D5/D6 run, in **both** directions, which is what makes it conclusive. The departing member's bucket prefix went from five objects to exactly one: the live sent asset survived (D5) and the recipient still opened it from their inbox, while the soft-deleted sent asset (D6) and all three post attachments (D9) were removed. Preservation is therefore reference-counted on `deleted_at IS NULL` rather than blanket, so it neither destroys what recipients still hold nor spares what nobody references | 1 |
| B-11 | **No server-side membership enforcement anywhere.** Every policy and RPC gates on `auth.uid()` and the follow graph only; nothing consults an entitlement. The entire paid Connected API — posts, comments, shares, attachments, directory — is therefore exercisable by any authenticated Apple user able to obtain a Supabase session. This is the missing enforcement boundary for the paid service itself: Connected is sold as a membership and the server has no concept of one. Requires a server-held entitlement record fed by App Store Server Notifications V2, plus policy predicates that consult it | **P1** | **Confirmed defect. Still true as filed, re-verified against the deployed catalog 2026-08-16: 33 of 33 RLS policies are `{authenticated}`-scoped and NOT ONE consults an entitlement; a search of the deployed policies, functions, columns and constraints for `entitle*`, `subscription*`, `membership*`, `storekit`, `app_store` and `original_transaction` returns zero matches.** Exposure is larger than the row states — all 99 `posts` rows are `is_public = true`, so the readable surface for any authenticated non-member is the whole corpus. That is D-2 / Phase 4's defect, not a new one, but it sizes this row. **TWO-STAGE ACCEPTANCE, agreed 2026-08-16 — this row is NOT split, because the implementation units already give the separation and splitting would move the denominator for no verification benefit.** Stage 1: enforcement predicates run in **shadow**, recording per denied request which clause would have decided it, against a prediction committed before the window opens. Stage 2: **binding**, permitted only when gate G11 has passed — **G11 PASSED 2026-09-01, 16 of 16, zero falsifiers** — and the enforcement-path Sandbox QA mechanism is resolved, **which is NOT met and is ON THE CRITICAL PATH**. **THE GRANDFATHER-POPULATION CONDITION IS RETIRED, 2026-09-01, SEE B-36 — it is not satisfied, it is gone.** It was unsatisfiable by construction (the snapshot has no entitlement predicate, so identities that never had a subscription can never acquire authoritative state) and it protected a population the account holder has confirmed does not need protecting: a pre-release beta cohort with no production customers. **The grandfather mechanism is to be RETIRED rather than discharged**, which is why this row's own U6c obligation below can disappear with it. **THE SECOND CONDITION WAS RESTATED 2026-09-01 AND THE CHANGE IS NOT COSMETIC.** It read *"the shadow reports zero decisions resting solely on the migration-compatibility clause"*, which is a criterion satisfied by OBSERVING NOTHING — and **B-34 established three ways to observe nothing while the traffic is real**: an evaluated predicate whose side effect is rolled back, an `OR` branch that short-circuits before the observer, and a surface simply never reached. Dormancy was already a fourth. **So the shadow window is now ONE-DIRECTIONAL: a non-zero grandfather-only count BLOCKS binding and is trustworthy, because any observation that landed is real; a zero count can never UNBLOCK it.** **The unblocking evidence is a CENSUS over the frozen snapshot, not a sample of traffic** — enumerate all `cutover_identity_count` identities and require each to hold authoritative membership state or carry an explicit disposition. A census has no silence problem. **First run 2026-09-01, read-only, aggregates only: 1 of 16 holds authoritative membership state and 15 do not**, so binding is blocked on measured evidence rather than on inference. The shadow window had seen only 1 of those 15. **CARRIED POST-PHASE OBLIGATION — U6c. SUPERSEDED 2026-09-01 BY B-36 AND RETAINED ONLY AS THE CONDITIONAL FORM: everything in this paragraph applies IF the grandfather mechanism is kept. The decision is to retire it, in which case U6c collapses into an ordinary cleanup migration with no clock, no liveness check and no post-phase obligation. It returns in full the moment a production customer exists.** Phase 3 is explicitly allowed to close with the bounded cutover snapshot still present. U6c removes it when either every snapshot UID has authoritative membership state **or** twelve months have elapsed since binding — **and in either case** after a final **liveness** check, where any snapshot UID **active** since binding but still lacking membership state is evidence that U5 failed for that identity and blocks removal until dispositioned. **CORRECTED 2026-09-01 by B-35: this specified `auth.users.last_sign_in_at`, which GoTrue advances only on re-authentication and never on token refresh, so an identity on a long-lived session reads as dormant while actively issuing requests — and it fails OPEN, removing the snapshot out from under identities still relying on it. Key on `max(auth.sessions.updated_at)`, cross-checked against `auth.users.updated_at`.** **The shadow metric must never stand in for U6c safety**, because it proves something about observed traffic only and a dormant subscriber generates none. Owner: this row. **Due: twelve months after U6b binds.** **U5's EVIDENCE MODEL WAS CORRECTED BEFORE IMPLEMENTATION, 2026-08-16 — see B-24. Read the correction precisely, because it is narrow.** What is superseded is only the wording that the server *"ignores"* the client artefact. What survives unchanged is that **client data is never authoritative for current entitlement**: the server still obtains current entitlement state **directly from Apple**, and no client assertion grants or revokes anything. What is added is that the **Apple-signed transaction JWS, after server-side signature verification, is evidence of possession and therefore of ownership** — it can only come from `currentEntitlements` on a device signed in as the owning Apple Account. The old wording discarded that, which is what left ownership unproven. So the corrected model is: **JWS = who, live Apple read = now, `appAccountToken` = durable binding.** Enforcement itself is unaffected — `connected_member()` reads membership state, and how that state came to be trusted is U5's concern. **U3 IS DEPLOYED TO PRODUCTION, 2026-08-17, AND THIS ROW IS STILL OPEN — the distinction is the whole of U3's design.** The schema, the entitlement predicate and the cutover snapshot now exist in production, but **no policy calls `connected_member()`** (verified zero at P11) and every one of the 33 policies is unchanged and byte-identical, so the enforcement boundary this row describes is still absent. U3 built somewhere for U4–U7 to write; it enforces nothing. **The cutover facts this row's U6c obligation depends on, recorded here so the obligation is actionable without re-deriving them:** boundary `cutover_at` = **2026-08-17 19:08:27.125223+00**, `cutover_identity_count` = **16**, verified complete at `cutover_verified_at` = **2026-08-17 19:09:48.080684+00**, `grandfather_enabled` = true with no expiry set, `u6b_bound_at` still **null**. **No production UID is recorded in this repository**, deliberately — the snapshot's contents are outside B-23's structural capture, and its completeness was proven by the P4–P8 convergence procedure rather than by the gate. **U6c's twelve-month clock has NOT started**, because it runs from U6b binding, which has not happened. **NEW OBLIGATION ADDED 2026-08-20, owned by this row: the enforcement-path Sandbox QA mechanism is DEFERRED TO U6b.** D4 settled that Production `connected_member()` means Production entitlement only, with **no exception mechanism inside the predicate** — a first proposal for a per-identity allowlist was rejected precisely because it put a shippable exception inside the function that defines paid access. U5 needs no such mechanism at all (its claims are about rows in `membership`/`membership_binding`, and nothing in U5 reads the predicate), and **U6a needs none either, because the shadow window denies nothing by construction**. The need appears only when enforcement becomes *binding*. **Deferring is the B-24 lesson applied:** designing the exception now means designing it against an enforcement surface that does not exist, which is how the original U5 activation design came to prove authenticity while proving nothing about ownership. At U6b the options — a separate QA project, a Production subscription bought for the purpose, or a tester carve-out *outside* the predicate — can be weighed against the real surface, with an enumerable tester population. **U6a must also keep sandbox testers' would-be denials out of its shadow report**, which is what `membership_state()`'s new `sandbox_only` value exists to make separable | 3 |
| B-2 | **Account directory is fully scrapeable.** `search_account_directory` and `get_account_directory_by_user_ids` are `SECURITY DEFINER` and never consult `lookup_enabled`, so a member who has opted out of discovery is still returned. With B-15's 2-character substring floor, the whole directory can be enumerated by any authenticated user. (Membership enforcement is tracked separately under B-11) — **PREMISE CORRECTED 2026-08-12; the `lookup_enabled` half is withdrawn.** The literal claim is true (neither function references the column) but the remedy it implies is wrong for both RPCs, and would have caused damage. **There is no discovery opt-out in the product.** `ff2d4ff` "Connected discovery simplification" (2026-07-08) removed it: `ProfileView:1281` reads *"Connected discovery is always enabled. Relationship privacy is handled by explicit follow approval"* and sets `enabled = true`; `AccountDirectoryService:320`/`:389` discard the passed parameter with `let effectiveLookupEnabled = true`; `:425` writes the literal `"lookup_enabled": true`. The column records nobody's preference. **The two RPCs are not the same kind of surface, and that is the distinction the finding missed.** `search_account_directory` has exactly one caller (`PeopleView:601`, People → Find) and is the app's only discovery surface. `get_account_directory_by_user_ids` has thirteen (`ContentViewRowSupport:796`, `BackendSessionDetailView:946`, `CommentsView:801`, `BackendShim:1644`, `ConnectedAttachmentShareUI:477`, `SessionDetailView:2265`, `PeopleView:167/178/189/200`, `FollowingListView:96/375`, `FollowersListView:155`) and **every one resolves an id the viewer already holds** through an existing relationship or an item already delivered to them. Resolution is not discovery. **Production data settles it rather than the argument.** 15 directory rows: 7 `true`, 8 `false`; all 8 belong to live `auth.users`, and every one was created between 2026-03-01 and 2026-06-06 and last signed in **on or before 2026-06-06** — i.e. all of them predate `ff2d4ff`, whose predecessor client defaulted `DiscoveryMode` to `.none`. Every account that has signed in since is `true`. So `false` is pre-July residue. Gating **resolution** on it would blank names and avatars for 8 of 15 accounts across feed rows, comment threads, share pickers and follower lists (today's blast radius happens to be small — of the 8: 0 approved follows, 0 comments, 2 with posts, 1 with attachments — but that is a fact about current data, not a property of the design). Gating **search** on it would hide 8 accounts that never opted out, while changing nothing for anyone who has used the app since July. **Same failure mode as B-9:** a defensible-sounding predicate resting on reasoning that does not survive contact with the deployed code. **What survives of this row is its second sentence** — the directory is enumerable by any authenticated user — and that mechanism is B-15's 2-character floor, Phase 4. The vestigial client plumbing is recorded separately as C-41 and deliberately not touched here | P1 | **Confirmed defect, narrowed to the enumeration half.** The `lookup_enabled` half is **withdrawn, not deferred** — it is not a defect and must not be "fixed". Remaining scope is B-15 | 4 (via B-15) |
| B-3 | `delete_account_v1` deletes the departing member's comments on others' posts (`author_user_id` clause) — contradicts the retention decision. The `post_comments → posts` cascade already covers own-post comments. **RULE REVISED 2026-08-13 — the departing member's own comments are now DELETED, and the `author_user_id` clause this row objected to is deliberately back.** Same reasoning as B-1: retention was correct at the time, and Apple's guidance changed the input. **Verified on Device A:** `6ed7b788`, A's comment on B's post, was deleted, while `944a70cb` — B's own reply on B's own post merely addressed to A — survived. **B-19 IS UNCHANGED AND IS WHY THE PREDICATE IS SCOPED TO `author_user_id` ALONE:** adding `recipient_user_id` would destroy third-party replies and was the original P1 | P1 | **RESOLVED under the REVISED rule — device-verified 2026-08-13.** **Read the cell, not the word: what is Resolved is DELETION, not retention.** This row was previously Resolved on the opposite behaviour, and that is corrected here rather than quietly overwritten. The 2026-08-11 D5–D9 run confirmed the departing member's comment on another member's post **survived**, which was the correct outcome under the rule then in force; the rule was revised on 2026-08-13 (see the finding column), so that evidence records the superseded semantics and no longer speaks to what ships. **The current semantics are verified on the C-35 run, 2026-08-13, against a prediction committed before the erase:** `6ed7b788` — A's comment on B's post — was **deleted**; `post_comments` 6→3 and B's authored count 3→2 both matched; and it was confirmed as a product change rather than a row count, with A's comment gone from the thread on Device B's screen. The deployed predicate is `author_user_id` alone, re-verified 2026-08-14 as byte-identical to committed source. **B-3's own client half is now MOOT and is closed on that basis** — the departing member's comment no longer survives, so no unresolvable *author* can be rendered. The surviving-recipient case is B-19's and is tracked there | 1 |
| B-19 | **`delete_account_v1` deletes other members' comments on their own posts.** The `recipient_user_id.eq.<uid>` clause in the `post_comments` delete. Established from the deployed RPCs: `add_post_comment` writes `owner=postOwner, author=commenter, recipient=postOwner`, while `reply_to_commenter` and `respond_to_commenters` write `owner=postOwner, author=postOwner, recipient=theCommenter`. So for departing member D, `recipient_user_id = D` matches replies **authored by another member, on that member's own post**, addressed to D — third-party content destroyed because D was merely the addressee. Distinct from B-3, which concerns D's *own* comments, and worse in kind. The same statement's `owner_user_id.eq.<uid>` clause is entirely redundant: `post_comments_post_id_fkey` is `ON DELETE CASCADE`, so deleting D's posts already removes every comment on them. Consequence for the rewrite: retained replies will carry `recipient_user_id` values pointing at deleted accounts, and `post_comments` has no FK to `auth.users` while `account_directory` does cascade — so the recipient becomes unresolvable and the client must handle it | **P1** | **RESOLVED (retention) — device-verified twice, and the second run is the discriminating one.** 2026-08-11 (D5–D9): the post owner's own reply, addressed to the departing member, survived the erase. **Re-verified 2026-08-13 on the C-35 run under the REVISED deletion rule, which is the evidence that counts** — `944a70cb`, B's own reply on B's own post merely addressed to A, survived an erase whose entire point was that the departing member's *own* comments now go. A predicate that had reached `recipient_user_id` would have passed every other assertion in that table and failed only this one, so the two revisions are separable and B-19 is confirmed intact across the change. Predicate confirmed as `author_user_id` alone in the deployed function, re-verified byte-identical 2026-08-14. **CLIENT-RENDERING HALF — NARROWED BY A SOURCE READ 2026-08-14, AND NO LONGER OWNERLESS.** The blocker recorded under the table (feed empty; needs a viewer following a third account) is **superseded**: the C-35 run rendered the thread on Device B *after* A's deletion. **The premise is also narrower than filed.** `CommentsView` hydrates directory identity for **authors, the owner and the viewer only** (`:767-768`, `:684-685`) and never for `recipientUserID`, which is used solely for owner fan-out collapsing and reply eligibility (`:1370`, `:1441-1461`) — dictionary-keyed on the raw id string, no force-unwrap, no directory lookup. Every name path falls back to `"User"` (`:260`, `:876-879`), never a raw UUID and never blank. **So an unresolvable recipient has no render site at which to fail**, and this is a cheap acceptance confirmation rather than an open risk. **A live fixture already exists in production and needs no staging** — see `qa-plan.md` D8, which now names it. **RENDERING HALF RESOLVED 2026-08-14 — device-observed on Device B / Études Dev, screenshot retained.** The C-35 discriminator thread (`944a70cb` on post `81e080cc`, recipient `92d6b718`, an account deleted on 2026-08-13 with no `auth.users` row and no directory identity) **opened instantly and rendered normally**: author "You" with avatar, body intact, **no raw UUID, no blank or broken identity UI, no null/undefined, no spinner and no delay.** **BOTH HALVES OF B-19 ARE NOW CLOSED**, and the finding is fully Resolved rather than Resolved-in-part. **What makes this more than a green screen:** the prediction was written from source *before* the observation and named the exact non-failure — that the recipient's name would simply be absent, because it is never a render site — so the run confirms a stated mechanism rather than reporting that nothing looked wrong. **Cost of the correction, worth recording:** this had sat unowned since 2026-08-11 behind a blocker that was disproved on 2026-08-13 and a premise that the rule change had made moot, and it took one existing fixture and one thread open to close. The expensive part was never the test | 1 |
| B-4 | Every delete in `delete_account_v1` ignores its result; reports success on partial failure. `posts` has no FK to `auth.users`, so orphans become permanently unreachable by any RLS policy | P1 | **RESOLVED 2026-08-16 — verified against a faithful local reproduction (B-23), NOT verified in production, and the qualifier is part of the disposition rather than a footnote.** The B-23 gate was green immediately before the run and again afterwards; the function under test was the **real, unmodified `delete_account_v1`** (`git diff` against HEAD empty, last touched by `0f5896d`). Fault injection was the fourth route, available only because B-23 now exists: **DDL against a disposable local database**. The three routes Phase 1 declined stay declined — no temporarily-broken production deploy, no QA-only injection inside the function, no DDL against production. The trigger is deliberately **not** in `supabase/migrations/`, and the B-23 gate would fail on an extra trigger if it ever were. **The negative direction is now executed.** Fixture: three local identities, A holding 2 posts, 2 sent attachment rows, 1 received row, 1 authored comment on another member's post, 2 shares, 1 comment view, 1 avatar object and 2 attachment objects — every count asserted non-zero first, because an empty fixture is not a pass. A `before delete on public.posts` trigger made step 5's fourth statement fail, which is **after** received attachment rows, the recursive storage sweep, sent attachment rows, the avatar sweep, `post_comment_views`, `post_shares` and `post_comments` had all succeeded. **Observed: `{"success":false,"step":"posts",…}`, HTTP 500** — the honest, step-named failure this row was opened for. State afterwards matched the prediction on every measure: sent rows 2→0, received 1→0, attachment objects 2→0, avatar 1→0, comment views 1→0, received shares 1→0, authored comments 1→0, while posts stayed at 2, follows at 4, the directory row and `auth.users` both present, and every protected third-party row untouched. **This is exactly what the 2026-08-11 production run could not establish**: that run was a success case and the old function returned success unconditionally, so a green result discriminated nothing. A named failing step does. Predictions committed at `ca697ec` before execution | 1 (fix) / 3 (verify) |
| B-5 | `get_account_directory_by_user_ids` is anon-executable with no `auth.uid()` check. **Verified against the deployed definition 2026-08-12, and the mechanism is worse than filed.** `proacl` is `{=X/postgres, postgres=X, anon=X, authenticated=X, service_role=X}` — the bare `=X` is the **PUBLIC** grant, so revoking `anon` alone would not have closed it. The definer is `postgres`, which carries `rolbypassrls = true`, so the function reads `account_directory` with **RLS bypassed**: `account_directory_select_owner` is inert inside it and is not a second line of defence. The body contains no reference to `auth.uid()` at all. The anon key ships in the app binary, so anon-executable means world-executable; exposure is bounded only by knowledge of a UUID, not by any check. **Observed in production 2026-08-12, not argued from source:** a POST to `/rest/v1/rpc/get_account_directory_by_user_ids` carrying only the shipped anon key and **no bearer token** returned **HTTP 200 with a full directory row** — `user_id`, `account_id`, `display_name`, `instruments`. **Companion exposure found while verifying this, and it is the more dangerous of the two.** `search_account_directory` carries the identical PUBLIC + anon grant, yet returns nothing to anon — because `ad.user_id <> auth.uid()` against a NULL uid yields NULL and filters every row. That predicate is **self-exclusion**, written so you do not appear in your own search results; its anon-blocking effect is an accident. Confirmed over the wire the same day: anon POST with a token matching exactly one row returned **HTTP 200 and zero rows**. Anyone "tidying" that comparison to `IS DISTINCT FROM` would open the entire directory to the world in one line. Fix carried by `supabase/sql/2026-08-12-directory-access-hardening.sql`: an explicit `auth.uid() IS NOT NULL` test in **both** bodies, plus `REVOKE EXECUTE … FROM PUBLIC, anon, service_role` on both, leaving the callable surface explicitly authenticated-only. `service_role` goes too — there is no service-role caller (`delete_account_v1` uses the `account_directory` **table**, `index.ts:265`/`:350`, never these RPCs) and the intended Phase 3 pattern is table access with service_role, so retaining the grant while the body returned zero rows would be a silent-empty-result trap that plain `LANGUAGE sql` cannot raise on | P2 | **RESOLVED — applied 2026-08-12, server-side verified, and device-verified the same day on TestFlight build 131.** Passed: **V3**, both RPCs now refuse the anon key over the wire — HTTP **401**, SQLSTATE `42501` *permission denied for function*, no directory data (the pass condition was any non-2xx permission failure; PostgREST answered 401 rather than 403, so do not assert on 403). **V4**, `proacl` on both is now `{postgres=X/postgres, authenticated=X/postgres}` — no PUBLIC, no anon, no service_role — with `follow_requests_open` untouched at `{postgres, authenticated, service_role}` and still `SECURITY DEFINER`. **V5a**, with no JWT claims the body returns **zero rows even to the owner role**, so the `auth.uid()` guard holds independently of the grant — the defence-in-depth claim is tested rather than assumed. **V5b**, an authenticated viewer resolving a `lookup_enabled = false` uid gets the row back with identity intact, and **V7**, an authenticated search for that same account still finds it: together the assertion that B-2's corrected premise is what actually shipped. The snapshot diff matched the prediction written before the apply — exactly 4 grant rows (`anon` ×2, `service_role` ×2) and 2 definitions, with `policies`, `rls_enabled`, `triggers`, `constraints`, `columns` and `storage_buckets` all unchanged. **V6 (QA E12) — PASS, and with fuller coverage than planned.** Run on **TestFlight build 131** across Devices A and B. Scoped down beforehand to the People/follow-graph surface alone, because Device B followed nobody approved and the other surfaces had no remote content to render; staging an approved A↔B edge during B3 made all four reachable after all. **All four passed**: feed identity and avatars, backend session detail and comment-author identity, People/follow-graph/request identity, and Connected attachment share-picker recipients. No blank names, no raw UUIDs, no missing users, no permission or auth failures. **That is the discriminating result**, because a call site reaching these RPCs without a bearer is now a visible 401 rather than a silent success — so the inference that none does is tested rather than assumed, and on the distribution artifact rather than a development build. **V8 (QA E13) — PASS, and it was executed incidentally by B3's staging rather than as a separate step.** Two `follows` rows were created after the hardening was applied: `92d6b718 → dfaf8d18` at 10:46:43Z and **`dfaf8d18 → 92d6b718` at 10:58:08Z, inserted from Device B on TestFlight 131** — the exact artifact and direction E13 specifies. Both inserts had to satisfy `follows_insert_requester`'s `WITH CHECK`, which calls `follow_requests_open(followed_user_id)`, and both were subsequently approved, so the UPDATE path ran too. The four older `requested` rows date from July and 11 August and are not evidence here. `follow_requests_open` was deliberately left untouched (B-18), and this confirms the directory work did not disturb it | 1 |
| B-6 | `attachments_select_via_visible_post` trusts an unconstrained jsonb column; appears to permit read access surviving revocation | P2 | **Mechanism established from schema; runtime confirmation pending.** Read from the deployed policy: `bucket_id = 'attachments' AND EXISTS (SELECT 1 FROM posts p WHERE EXISTS (SELECT 1 FROM jsonb_array_elements(p.attachments) a WHERE a.value->>'bucket' = objects.bucket_id AND a.value->>'path' = objects.name))`. **No predicate ties the post to the object's owner** — not `owner_user_id`, not a path prefix. And `posts_insert_owner` constrains only `owner_user_id = auth.uid()`, leaving the `attachments` jsonb unvalidated, with no trigger on `posts` to check it. So a member can insert a *private* post naming any object path they know, see their own post, and thereby satisfy the storage policy. The argument does not depend on whether `posts` RLS applies inside the subquery, since the attacker uses their own post. Practical gate is knowing the path, which recipients legitimately learn — hence the suspected revocation bypass. **Not promoted to Confirmed:** proof deferred to QA E8 rather than run as a write/rollback experiment against production. **Note, pending that test:** the Phase 4 "expected to dissolve" expectation looks doubtful — the missing ownership predicate is structural to the policy, and purging unshared rows does not add one. **THAT NOTE WAS RIGHT, AND IT DECIDED THE OUTCOME.** The finding was closed by **fixing the policy**, not by running E8b. **The invariant now enforced:** an attachment referenced by a post must belong to **that post's owner** — not to the current viewer. Stating it that way is the whole of the fix, because the obvious predicate (`object owner = auth.uid()`) would have broken every approved-follower read, which legitimately crosses owners from the viewer's seat. **A DEPENDENCY CHECK RAN BEFORE ANY SQL, LOOKING FOR A LEGITIMATE CROSS-OWNER REFERENCE THAT WOULD BREAK. THERE IS NONE.** (i) **Writers:** exactly one in production — `BackendShim.publish…` → `patchPostAttachments`, whose paths come from `storageObjectPath(owner:)` (`:1151`) as `users/<owner>/<postID>/<attachmentID>.<ext>`, with `owner` = `AuthManager.canonicalBackendUserID()` (`:829`), the signed-in user and never an input. The storage INSERT policy independently pins uploads to `users/<auth.uid()>/…`, so the two agree. (ii) **Path shapes:** that one. (iii) **Connected / shared / adopted flows never write `posts.attachments`** — Connected sends live in `connected_attachments` behind `connected_attachment_recipient_select`, untouched by this change; Scores adoption copies locally with no backend reference; `post_shares` carries post ids, not paths. (iv) **Legacy state, measured rather than assumed:** all **6** references across all 98 posts sit under their own post owner's prefix, and there are **zero** objects under any `debug/` prefix. (v) **Read paths:** the follower case above. **One flow is deliberately broken:** `DebugViewerView` (`#if DEBUG`, absent from Release) can PATCH arbitrary paths into `posts.attachments`; those stop being readable through the post path. Correct, developer-only, no such objects exist, and B-7's `debug/` escape was dropped the same day | **RESOLVED 2026-08-14 — structurally hardened and device-verified.** `ALTER POLICY` (not DROP + CREATE, so name, command and roles could not move), applied under explicit authorisation; SQL reviewed as a committed diff first. **Structural verification, all seven conditions green:** only `policies.json` moved, one line; `policyname`, `cmd`, `roles`, `permissive`, `schemaname`, `tablename` and `with_check` byte-identical; only `qual` differing (`6e392607…` → `a9ebe466…`); the nine other snapshot files untouched; the deployed policy **re-read directly from `pg_policies`** rather than trusted from the snapshot; and all backend and storage counts unchanged. **Device regression green:** on Device A, following Account B with an approved follow, B's posts render in A's feed and **their attachments open and render** — the approved-follower cross-owner read, which is the only path that actually exercises this policy and the one a naive binding would have destroyed. **The old exploit was deliberately NOT demonstrated:** crafting a malicious post would have meant writing fabricated data to production to prove a mechanism already established from the deployed policy, and the structural requirement plus green legitimate reads is sufficient for this hardening. **The Phase 4 "expected to dissolve" disposition is withdrawn, not deferred** — it was an expectation about unrelated future work, and this row is now closed on its own terms | 1 (RESOLVED — hardened) |
| B-7 | `sign_attachment_rpc` is dead (client never calls it) and contains a `left(v_path, 6) <> 'debug/'` authorisation bypass. **TWO CORRECTIONS TO THIS ROW'S WORDING, ESTABLISHED AGAINST LIVE STATE BEFORE THE FIX.** (i) **"Dormant" understated it:** the function was live and `EXECUTE`-granted to `authenticated`, so it was dead only in the sense that our client never called it — not unreachable. (ii) **The `anon` grant overstated it:** the body's first statement is `v_viewer := auth.uid()`, returning `{"error":"unauthenticated"}` when null, so the PUBLIC/anon grant was **not exploitable**. Anyone escalating on the grant alone would have repeated exactly the mistake B-18 was closed for. **The bypass was real, and its reach was small.** The path guard admitted `users/<owner>/<session_id>/…` **or** anything starting `debug/`, and `v_path` is read from the post's own `attachments` jsonb, which `posts_insert_owner` does not validate (B-6's premise) — so a post owner could write an arbitrary `debug/…` path into their own post and have this mint a signed URL for it. **Reach was the `debug/` prefix, which was empty in both buckets.** That is a fact about the data rather than the code, which is precisely why the code went | P2 | **RESOLVED 2026-08-14 — dropped from production under explicit authorisation, `DROP FUNCTION … RESTRICT`.** Dropped rather than repaired: repairing the predicate would have left a `SECURITY DEFINER` signed-URL minter that nothing calls, which is more surface than removing it. **Verified before:** 0 client references, 0 referencing functions, 0 referencing policies, 0 objects under `debug/`. **Verified after:** absent from `pg_proc`; the snapshot diff was **deletions only** and matched a delta committed beforehand (`functions` 14→11, `function_grants` 42→33, the other eight files byte-identical); and all nine backend/storage counts unchanged. The definition survives in git history if it is ever wanted | 1 |
| B-8 | Storage objects orphaned when an attachment is un-included — no cleanup trigger is attached. **WARNING FOR WHOEVER WRITES THE CLEANUP — the obvious heuristic destroys exactly what B-1 protects.** A sweep that deletes objects whose owning uid is absent from `auth.users`, keyed on the `users/<uid>/` path prefix, will delete every asset a departed member sent that a live recipient still holds. The prefix carries the **sender's** uid, and a dead sender is the *correct* state for a preserved asset — that is the entire point of the retention rule. Verified on live data 2026-08-11: such a heuristic flagged exactly one object, and it was D5's survivor, with a live recipient reference. **Liveness must be read from `connected_attachments.deleted_at IS NULL`, never from the path.** The same trap applies to B-10's dormant path-based functions, which is a second reason not to attach them as written | P2 | **Confirmed defect** | 4 |
| B-9 | `connected_attachments` rows never deleted on account deletion; no FK, no explicit delete, no DELETE policy | P2 | **Resolved** — device-verified 2026-08-11 on the D14 run, in both halves, against the deployed function. Previously *fix deployed, verification NOT REACHED, narrowed in scope.* The fix deletes rows where the departing member is the **recipient** only; rows where they are the **sender** are retained deliberately, because those are the recipients' inbox references and B-1 requires them. That narrowing is correct but it means B-9 as filed is not fully "fixed" — some rows are meant to survive. **The earlier D5-D12 run proved nothing here:** that account had zero received attachments before and after, so step 1 was a no-op and the received-deletion path never executed. A future cycle must have A *receive* from B as well as send. **Residue observed and worth a decision:** the soft-deleted sent row survived with its storage object correctly removed, so a tombstone row now points at an object the function itself deleted. Harmless — no recipient will read it — but it could mislead a future reader into thinking the object should exist. Candidate tightening: also delete sender rows where `deleted_at IS NOT NULL`. **That predicate is committed (not deployed); its justification as first recorded here was WRONG.** This cell said those rows are "precisely the ones whose objects were just swept". They are not. `connected_attachments_asset_recipient_unique` is `UNIQUE (asset_id, recipient_user_id)` and `connected_attachments_sender_storage_path` pins `storage_path` to `users/<sender_user_id>/connected/<asset_id>.<ext>`, derived from sender and asset alone — so one asset sent to two recipients is **two rows sharing one path**. With one recipient live and the other soft-deleted, step 3 correctly preserves the object under B-1 while the soft-deleted row still matches the predicate. The rule survives, the reason does not: the defensible justification is about **references, not storage** — a soft-deleted row is nobody's live inbox reference, and B-1 only ever required live ones to survive. **That case cannot be staged with two Apple IDs** (it needs a sender plus two distinct recipients), so it will remain unverified after the run below; do not let the two-account legs imply coverage of it. **A false comment was also found in the deployed function and corrected in the same commit:** step 1 carried an `ORDER IS LOAD-BEARING` block justified entirely on a member sending an attachment to themselves, which `connected_attachments_not_self` (`CHECK (sender_user_id <> recipient_user_id)`) and the insert policy's `recipient_user_id <> auth.uid()` both forbid. The sender- and recipient-scoped sets are disjoint, so step 1 cannot change step 2's result set and the ordering is immaterial. Behaviour was never affected; the comment was instructing future maintainers not to reorder for a reason that does not exist. **Historical residue measured 2026-08-11** (read-only sweep of all 34 rows): 29 rows have **both** sides dead, 1 has a live sender and a dead recipient — 30 rows the old function should have removed and never did. **None of them has a surviving storage object**, so this is row garbage only, with no reachable content and no exposure: the old function's recursive sweep destroyed the objects (B-1's defect) while leaving the rows (B-9's). The remaining 4 rows have a dead sender and a **live** recipient and are correctly retained. Cleanup is optional tidying rather than a release blocker. **D14, 2026-08-11 — PASS on every assertion, and it is the run that closes this row.** Three legs on one erase, against the deployed step 3b. The **received** row (B sends to A, A keeps it) was deleted — step 1's path executing for the first time in any run. The **sender tombstone** (A sends to B, B soft-deletes) was deleted — the only assertion the previously deployed function would have failed, which is what makes the run discriminating rather than decorative. The **live sender row survived with its object**, under the deleted user's own prefix, so B-1 did not regress, and the recipient confirmed the file still opens. All eight blast-radius counts matched a prediction written down *before* the erase: `auth.users` 16 → 15, directory 16 → 15, `connected_attachments` 37 → 35, `follows` 8 → 6, attachment objects 9 → 8, and posts, comments and avatar objects unchanged. **The strongest evidence was unstaged.** B held a pre-existing soft-deleted row from a *third-party* sender; step 3b is scoped `sender_user_id = <departing>`, so it had to survive — and did. Had 3b been written as "delete all soft-deleted rows", which is the obvious reading of this row's own original wording, that third party's reference would have been destroyed. **Two remainders, neither of them this fix:** the historical row residue measured above is untouched and remains optional tidying; and the two-recipient case cannot be staged with two Apple IDs, so it stays unverified — see `qa-plan.md` D14. **THE TWO-RECIPIENT CASE IS ASSIGNED TO PHASE 3, 2026-08-14.** It had a stated blocker and no owner, which is exactly the shape that survives a phase gate unnoticed. **What is unverified, stated exactly:** one asset sent to two recipients is **two rows sharing one storage path** (`connected_attachments_asset_recipient_unique` is `UNIQUE (asset_id, recipient_user_id)`; the path is derived from sender and asset alone). With one recipient live and the other soft-deleted, step 3b deletes the soft-deleted row while step 3 correctly preserves the object under B-1 — a row deletion whose object must survive. Intended, reasoned, never executed. **Why Phase 3 and not a third Apple ID:** it needs a sender plus two *distinct* recipients, and the rig has two real Apple IDs by hard constraint (Sign in with Apple cannot use sandbox testers). Phase 3's local/disposable instance removes the constraint entirely — identities can be minted directly, with no Apple ID and no purchase — and that is a cheaper and more repeatable route than recruiting a third person. **Same caveat as B-12: the instance makes the fixture safe to build; it does not build it.** Group with B-4 and B-13 as one Phase 3 backend-verification unit rather than three separate errands. **THE TWO-RECIPIENT SUBCASE IS EXECUTED, 2026-08-16 — verified against a faithful local reproduction (B-23), NOT verified in production. THIS ROW'S STATE IS UNCHANGED AND STAYS Resolved:** the row's own obligation was the fix, discharged by D14 on 2026-08-11, and only the subcase was ever carried. Three local GoTrue identities staged what two real Apple IDs could not: one asset, **one** `storage_path`, **two live recipient rows** (A→B and A→C), plus a third-party row B→C. **Observed: both recipient rows deleted, the single shared object deleted, no error from two rows naming one path, and the third-party row and its object survived**, with both bystander accounts untouched. **The prediction deliberately did NOT follow this cell's original wording, and the divergence is recorded rather than smoothed over.** The text above frames the subcase as *step 3b deletes the soft-deleted row while step 3 correctly preserves the object under B-1* — that framing predates 2026-08-13, when step 2, the reference-counted preservation, was **removed**. Under the deployed function step 3 sweeps `users/<uid>/` unconditionally and step 3b deletes every sender row, so both recipients lose the reference and the object goes. The observed behaviour matches the **settled post-revision** semantics, and the pre-revision description above is history rather than the expected result. Predictions committed at `ca697ec` before execution | 1 (fix) / **3 (two-recipient case — DISCHARGED 2026-08-16; the `3` is retained so the Phase-3 denominator stays mechanically recomputable)** |
| B-10 | Dormant `cleanup_post_attachments_on_*`: `SECURITY DEFINER`, path-based deletion, no ownership predicate. Attached to nothing, so latent rather than live. Repair with an ownership predicate and attach in Phase 4, or drop. **THE ROW OFFERED BOTH AND DROP IS THE RIGHT HALF, FOR A REASON THE ROW DID NOT CARRY.** The authorisation hole is real — both loop over the post's own unvalidated `attachments` jsonb and delete by the bucket/path found there while running `SECURITY DEFINER`, so RLS does not apply, and if either were ever attached a post owner could name **somebody else's** object path and have deleting or editing their own post destroy another member's file. **But repairing only that would have left a deeper defect fully intact: both `delete from storage.objects` in SQL.** That row is an index over S3, not the file — removing it destroys the only pointer and leaves the bytes behind, untracked residue in place of tracked residue, unreachable by every policy path and invisible to the orphan sweep that would otherwise find it. Observed first-hand on 2026-08-14 while clearing B-22 and now written up in `supabase/README.md`. **An ownership predicate would have closed the authorisation hole and preserved the storage-lifecycle bug**, so these bodies are not a foundation for Phase 4 — keeping them would have invited someone to attach them. **Phase 4's B-8/B-10 cleanup must go through the Storage API from a service-role context, not a SQL trigger.** One further nuance recorded so nobody escalates on it: both carried `EXECUTE` for `anon` and `authenticated`, which is inert — they return `trigger`, and Postgres refuses to call a trigger function outside a trigger context | P2 | **RESOLVED 2026-08-14 (defuse half) — both dropped from production under explicit authorisation, `DROP FUNCTION … RESTRICT`.** **Verified before:** 0 triggers using either. **Verified after:** absent from `pg_proc`, 0 orphaned trigger functions, and user triggers in the snapshot's scope still 5 — a raw count of 6 was my own unscoped query and resolves as public 1 + storage 4 + realtime 1, the last being outside what `capture-schema.sh` captures. Snapshot diff deletions-only and matching the committed delta; all nine backend/storage counts unchanged. **The repair half is not deferred, it is withdrawn** — the design was wrong, not merely unguarded. Both definitions remain in git history for inspection | 1 (dropped) / 4 (real cleanup, Storage API) |
| B-12 | `delete_account_v1` storage listing stops silently at 1000 entries | P2 | **RESOLVED 2026-08-16 — verified against a faithful local reproduction (B-23), NOT verified in production, and the qualifier is part of the disposition rather than a footnote.** The B-23 gate was green immediately before the run and again afterwards; the function under test was the **real, unmodified `delete_account_v1`** (`git diff` against HEAD empty, last touched by `0f5896d`). Fault injection was the fourth route, available only because B-23 now exists: **DDL against a disposable local database**. The three routes Phase 1 declined stay declined — no temporarily-broken production deploy, no QA-only injection inside the function, no DDL against production. The trigger is deliberately **not** in `supabase/migrations/`, and the B-23 gate would fail on an extra trigger if it ever were. **Executed with 1500 objects under a single prefix.** 1500 rather than 1001, so an off-by-one in the paging arithmetic would also have surfaced; and deliberately **not** spread across prefixes, which would have left every list call returning one short page — the already-proven case. **Before: 1500 objects under `users/<D>/bulk/`, with `distinct folders under users/<D>/ = 1`**, so a single `listFolder` call had to paginate. **After: exactly 0**, counted independently in `storage.objects` rather than inferred from the function result, with the function returning `{"success":true}`. A protected bystander's 2 attachment objects and 1 avatar object survived; `storage.objects` went 1503 → 3. **A residue of roughly 500 would have been the unpaged-first-iteration bug this row exists for.** Predictions committed at `ca697ec` before execution | 1 (fix) / **3 (verify)** |
| B-13 | `delete_account_v1` not idempotent | P2 | **RESOLVED 2026-08-16 — verified against a faithful local reproduction (B-23), NOT verified in production, and the qualifier is part of the disposition rather than a footnote.** The B-23 gate was green immediately before the run and again afterwards; the function under test was the **real, unmodified `delete_account_v1`** (`git diff` against HEAD empty, last touched by `0f5896d`). Fault injection was the fourth route, available only because B-23 now exists: **DDL against a disposable local database**. The three routes Phase 1 declined stay declined — no temporarily-broken production deploy, no QA-only injection inside the function, no DDL against production. The trigger is deliberately **not** in `supabase/migrations/`, and the B-23 gate would fail on an extra trigger if it ever were. **Idempotency is now established by execution rather than by inspection, and from the real partial state B-4 left behind rather than a fresh one.** With the fault removed, the same call with the same token returned **`{"success":true}`, HTTP 200**: the already-completed steps re-ran against empty sets and returned cleanly — no false failure — while posts 2→0, follows 4→0, the directory row and `auth.users` all completed. Protected third-party state was untouched throughout. **A third call returned `Invalid session`, HTTP 401 — and that is the CORRECT answer, exactly as `docs/qa-plan.md` wrote down long before it could be run.** `auth.users` is gone, so the token no longer resolves to a user. Do not "fix" it by treating 401 as success: the function derives its subject from the verified token, and accepting an unverifiable one would hand away the only authorisation gate it has. **Scored separately from B-4**, from one induced-failure/retry run, as agreed. Predictions committed at `ca697ec` before execution | 1 (fix) / 3 (verify) |
| B-20 | **`delete_account_v1` deletes the avatar by pointer, not by prefix.** Step 4 (`index.ts:164-179`) reads `account_directory.avatar_key` and removes exactly that object, while every other storage sweep in the same function lists `users/<uid>` and removes what it finds (`:132-156`). If the object exists and the column is null or stale, the photo survives the account. Three routes there: both of C-33's, plus a missing directory row, where `maybeSingle()` returns null and the step silently skips. After `auth.users` is deleted nothing can ever reach it — `avatars_delete_owner_only` requires `auth.uid()` to equal a user that no longer exists, so there is no owner, no client path and no policy path. Permanently orphaned personal data, and precisely the data the erase promises to remove. Same class as B-8, in a bucket B-8 does not mention. Fix: list `users/<uid>/` in the `avatars` bucket and remove the union of that and the keyed object. Bounded and safe — `avatars_insert_owner_only` pins the client to one name per user, so the sweep can reach exactly one object and cannot touch anyone else's | P2 | **Resolved** — fixed at `5714c53`, deployed, and device-verified on 2026-08-11 across both paths. D12 covered the ordinary keyed case, but **only D13 discriminates**: with `avatar_key` NULL the deployed function skips the pointer branch entirely, so the prefix sweep is the only thing that can remove the object — and it did. The old function would have left it permanently. Blast radius checked in the same pass: the three other live avatars were untouched. **D13's setup was simulated, not provoked** — the null-key state was created by a single authorised `UPDATE` on the disposable QA account rather than by blocking the storage DELETE through a TLS interception proxy. That was a proportionality call: reachability of the broken state is already established independently by C-33's two source-read routes, so the proxy would have re-proven a known fact at disproportionate cost. What D13 tests is the deletion's behaviour *given* the state, and that does not depend on how the state arose. **Historical backlog is NOT covered by this fix and is tracked as B-21** | 1 |
| B-21 | **Two avatars of deleted accounts are permanently orphaned in the `avatars` bucket.** Found on 2026-08-11 while checking D13's blast radius: the bucket held five objects against three directory keys, and the two extras belong to accounts that no longer exist — `users/35b77436-1373-415d-a2f5-8d11f27d66a4/avatar.jpg` (created 2026-07-14) and `users/98beb225-b533-4723-b2e9-dd4bd12973af/avatar.jpg` (2026-07-22). Both have **zero** `auth.users` rows, zero `account_directory` rows and zero pointers. **This is B-20 realised, in production, on real accounts** — previously argued from source and now observed as retained data. Nothing can reach these: `avatars_delete_owner_only` requires `auth.uid()` to equal a user that no longer exists, so there is no owner path and no policy path, and the client has no route at all. They are personal photographs belonging to people who asked for their accounts to be deleted. **The B-20 fix does not address them** — it stops new orphans being created and has no reach over ones that already exist. Clearing them requires a one-off service-role deletion of the two exact object paths, which is a production write and therefore needs explicit authorisation. Verify the two uids are still absent from `auth.users` immediately before deleting, since the whole justification is that no live account owns them. **Provenance established, and it lowers the stakes: both are the developer's own earlier beta accounts on the device now called Device A, not real TestFlight users.** So this is leftover test data rather than a privacy incident with real subjects, and the finding was filed P1 on the opposite assumption before that was known. **What survives the correction is the evidence value, which is the reason to keep this row after the files are gone:** B-20's mechanism is no longer a prediction. The old function ran for months and demonstrably produced orphans that nothing could reach — so B-20 was worth filing, and the fix was worth making, on observed grounds rather than argued ones | P3 | **RESOLVED — row closed 2026-08-13 on independently verified evidence; the clearing itself was done 2026-08-11.** Downgraded from P1 once provenance was established: no real user's data is involved. **The row had been left reading "being cleared manually", present tense, and was never closed** — a documentation lag found while assembling the Phase 1 exit view, not a second defect. **Verified independently rather than taken from the earlier session's word:** the whole-backend sweep on 2026-08-13 read the `avatars` bucket as **3 objects, every one under a live `auth.users` uid with a matching `account_directory.avatar_key`, and zero orphans** — so both July objects are gone and nothing has re-accumulated in the two days since. **This closes the observation, not the mechanism:** B-20 owns the deletion-by-pointer defect that produced it and is separately Resolved; if that fix ever regresses, this class returns. Keep the row as the observed instance behind B-20 | 1 |
| B-22 | **Historical residue that the revised deletion policy makes non-compliant.** Measured 2026-08-13, read-only, immediately after the B-1/B-3 revision. Under the old rules these rows were **correctly retained**; under the new rule — a departing member's own backend UGC is deleted — they are data that should no longer exist. **Four `connected_attachments` rows with a deleted sender and a live recipient, from three deleted accounts, plus one `post_comments` row authored by a deleted account.** The exact rows are recorded in the authorisation request rather than here. **The shape matters more than the count, and it lowers the stakes considerably:** the two rows whose recipient is a *third party* (`fee69f83`, account_id `qwerty`, display name "Mo", created and last seen 2026-03-01, Apple private-relay address — provenance **not** established, treat as potentially a real tester) have **no surviving storage object at all**; they are dead references pointing at nothing, so no third party is holding content from a deleted sender. The only two rows with live objects — one JPEG, one PDF, both from accounts deleted on 2026-08-11, which are almost certainly D5–D14 disposables — have the **developer's own account** as recipient. **None is adopted to Scores** (`saved_to_scores_at` null on all four), so the local-copy carve-out does not apply. **Same class as B-21:** a policy change makes existing data wrong, and the fix has no reach over what already exists. Clearing needs a one-off service-role deletion of exact row ids and object paths — a production write, therefore **explicitly authorised and deliberately NOT bundled with the function deploy.** Verify the three sender uids and the one author uid are still absent from `auth.users` immediately before deleting, since that absence is the entire justification. **RE-MEASURED READ-ONLY 2026-08-13 BEFORE CLEARING, AND THE RECORDED SHAPE WAS EXACT** — four rows, three deleted senders (`ab70e211` ×2, `5a32e9e7`, `389680c0`), one `post_comments` row (`f0daf6d7`, author `5a32e9e7`), two surviving objects, `saved_to_scores_at` null on all four. **ONE CORRECTION TO THIS ROW'S REASONING, WHICH ITS CONCLUSION SURVIVES.** The cell argued the Mo rows were low-stakes because they have no storage object, so "no third party is holding content from a deleted sender". That is true of the *content* and false of the *visibility*, and visibility was the load-bearing half. `fetchReceived` (`ConnectedAttachmentSharing:279`) filters on `recipient_user_id` and `deleted_at IS NULL` only — **no sender-liveness test and no object check** — and `PeopleView:256` then renders one "Shared with you" row per item, titled **"Connected musician"** when the sender's directory row is gone and subtitled with the filename. **Mo could still see two inbox entries naming a PDF from a deleted sender; only the download failed.** Still P2, still low-stakes, but the stated reason was wrong and the reason is what the next person implements from — the D14 lesson applied to this row's own argument. **THE WHOLE-BACKEND SWEEP FOUND THREE MORE RESIDUE CLASSES THIS ROW DOES NOT COVER, DELIBERATELY LEFT IN PLACE.** (i) **29 `connected_attachments` rows with BOTH parties deleted** (24 soft-deleted, 5 with `deleted_at IS NULL`), none with a surviving object — invisible to every live account, and not made non-compliant by the policy change. (ii) **One live row from a LIVE sender to a deleted recipient** (`2a6a5819`, the developer's own send, object alive) — the revised rule has no claim on it because the sender lives. (iii) **One object referenced by nothing at all** under a live user's prefix, `users/dfaf8d18…/connected/498b44b6-71ad-470a-9af1-563eea3c1397.pdf` (1938 B) — pure B-8. Plus **one orphan `post_comment_views` row** (post `dce002f7` no longer exists) — pure B-16. These belong to B-8, B-10 and B-16 in Phase 4 and clearing them by hand now would be a production write that buys nothing. **Everything else was clean:** zero orphaned posts, follows, directory rows or avatars; all 6 post attachment references across 5 posts resolve to existing objects; B-21's July avatar orphans have not recurred | P2 | **RESOLVED 2026-08-13 — cleared under explicit authorisation, and verified against a prediction committed beforehand.** Pre-flight re-asserted immediately before the write: all three sender uids and the comment author still absent from `auth.users`, the four rows still the intended records, and **exactly two** storage objects existing for them. Deleted by **explicit id and explicit object path only** — no predicate on sender liveness anywhere, so this was not generalised into an orphan sweep. **All nine measures matched the prediction**: `connected_attachments` 35 → **31**, `post_comments` 3 → **2**, `attachments` objects 10 → **8**, and `auth.users` 16, `account_directory` 15, `posts` 98, `follows` 6, `avatars` 3, `post_comment_views` 9 all unchanged. Mo's live inbox is now **0 rows** and Account B's two historical received entries are gone. **Tier 2 and Tier 3 confirmed untouched by direct re-query afterwards** — 29 both-dead rows still present, the orphan comment view still present, and the live-sender row and both live-owner objects still present. No structural drift: `capture-schema.sh` produced an empty diff after the write. **This closes the row, NOT the backend.** B-22 was one policy-change correction over enumerated records; the residue in (i)–(iii) above is still there by choice and is Phase 4's | 1 |
| B-14 | `follows_update_approve_by_followed` contains a tautological no-op guard; the approver can rewrite `follower_user_id` | P3 | **RESOLVED 2026-08-14 — structural fix verified 2026-08-13; the normal-approval regression check closed on the C-28 staging run.** The one thing catalog reads cannot establish is that a legitimate approval still succeeds end to end. That needs a real request → approval cycle through the app, which needs both Device A and Device B in Connected. **Normal UI can stage it** — one *Unfollow* from A clears both A↔B rows (see C-43), B then requests, A approves — but Device B is deliberately being held in its lapsed-member fixture state, and re-entitling it purely to satisfy this check was judged not worth destroying that fixture for. **CLOSED 2026-08-14 — the outstanding runtime check ran as a side effect of staging C-28's fixture, which is precisely the opportunistic closure this cell was waiting for.** The `connected_attachments` insert policy requires an approved follow, so staging forced a genuine request → approval cycle through the app: both `follows` rows between Device A (`44a6018e…`) and Account B (`dfaf8d18…`) were created at 10:21 and read back **`approved`** in the pre-delete baseline, so `approveFollow`'s `PATCH {status, updated_at}` still succeeds end to end after the column privileges were narrowed. **Environment recorded rather than glossed:** the approving client was Études Dev (Debug) under a synthetic StoreKit entitlement, because Device B's Release install is deliberately held lapsed. That does **not** weaken the result — the fix is a server-side column privilege and the client sends the identical PATCH regardless of build configuration or the source of its entitlement. **No crafted exploit write was performed**, as agreed. The code-level argument meanwhile is tight rather than merely plausible: `approveFollow` sends exactly `{status, updated_at}`, and V3 asserts against the live catalog that both remain writable while both participant IDs do not. Verified against the deployed policy. `WITH CHECK` reads `(followed_user_id = auth.uid()) AND (follower_user_id = follower_user_id) AND (followed_user_id = followed_user_id) AND (status = 'approved')`. Both middle clauses compare a column to itself and are therefore always true; `WITH CHECK` sees only the new row, so there was never an old value to compare against. Concrete consequence: the approver can take a genuine `status='requested'` row from A and, in the same `UPDATE`, rewrite `follower_user_id` to B while approving it — fabricating an approved follow edge for someone who never requested one. Since an approved follow is what grants feed visibility (`posts_select_public_or_owner`), this pushes the approver's public posts into an arbitrary user's feed. **Direction confirmed 2026-08-13:** that policy requires `f.follower_user_id = auth.uid()`, so the forged edge (follower=victim, followed=approver) exposes the **approver's** posts to the victim. Injection, not exfiltration — nothing of the victim's is revealed, which is what keeps this at P3. **SECOND HARM, not in the original cell:** the row is rewritten, not copied, so the genuine requester's pending request is **destroyed** — no approval, and no record they ever asked. **FIXED 2026-08-13 as a column privilege, not a policy predicate**, `supabase/sql/2026-08-13-follows-update-column-privileges.sql`. RLS cannot express "this column keeps its previous value" because `WITH CHECK` sees only NEW; the only alternatives were a trigger or a grant, and column privileges are evaluated **before** RLS, so `REVOKE UPDATE … FROM authenticated` + `GRANT UPDATE (status, updated_at)` removes the capability outright rather than filtering it. The tautological clauses were also deleted — an **honesty** fix, not the security fix, because left in place a future maintainer could revoke the grant believing the policy still covered this. **The full UPDATE caller map was established first**, since the revoke is global to the role: the approve `PATCH` (`BackendShim:714`, writing `status` and `updated_at`) is the **only** update of `follows` anywhere in the client — every other `PATCH` in the codebase targets a different table — while request is an INSERT and decline, unfollow, remove-follower and withdraw are all DELETEs. No deployed RPC updates `follows`; `delete_account_v1` deletes from it as `service_role`. **The participant IDs are never legitimately mutable:** they are the primary key, and every transition except approval creates or destroys a row rather than rewriting who it is about. `created_at` is never client-written either, so it was revoked too. **Verified structurally, with no exploit write, deliberately.** The defect was already conclusive from four catalog facts — the column grant, the policy's `USING`, the inert `WITH CHECK`, and the absence of any trigger — so a crafted UPDATE would have confirmed only documented Postgres semantics while writing fabricated data to production and destroying a real pending request. **After the fix, `has_column_privilege('authenticated','public.follows','follower_user_id','UPDATE')` is `false`**, which is stronger evidence than a failed exploit: the capability is gone, not merely filtered. `status` and `updated_at` remain `true`, and INSERT/DELETE/SELECT are untouched. The snapshot diff matched a prediction written beforehand — 3 column rows, 1 table row, 1 policy line, nothing else. **This finding also closed a source-of-truth blind spot:** `capture-schema.sh` recorded *function* grants but no table or column privileges, so the entire class of authority this fix lives in was invisible to the diff. It now captures both, and the committed pre-fix snapshot is the V1 record | 1 |
| B-18 | ~~**`follow_requests_open` is SECURITY DEFINER, anon-executable, and performs no `auth.uid()` check.**~~ Low-priority sibling of B-5, same class and same one-line fix. Exposure is genuinely small: it takes a `uuid` the caller must already possess and returns a single boolean, and it `coalesce`s to `true` when no directory row exists, so it cannot be used to test account existence. A `false` return reveals only that a known UUID has follow requests disabled. Worth noting the general point it shares with B-5: **anon-executable means world-executable**, because the anon key ships inside the app binary. **CORRECTED 2026-08-12 — the load-bearing half of this finding is FALSE against the deployed grant.** `proacl` is `{postgres=X/postgres, authenticated=X/postgres, service_role=X/postgres}`: **no PUBLIC entry and no anon entry**, and `has_function_privilege('anon', …)` is `false` both live and in the 2026-08-10 snapshot. The revoke this row asks for has already been done. Its framing — "same class and same one-line fix as B-5" — inverts the relationship: B-5's fix is the state B-18 was already in. The remaining half, no `auth.uid()` test in the body, is true and now buys nothing, since the only reachable callers are `authenticated`, `service_role` and the RLS policy. **The finding also rests on a false premise about reach:** it reads as though the function were dead client-side, and it is true that no Swift call site exists — but it is **called by `follows_insert_requester`**, whose `WITH CHECK` is `(follower_user_id = auth.uid()) AND (status = 'requested') AND follow_requests_open(followed_user_id)`. It is live and load-bearing. **The active risk is the opposite of the one filed, and it is why this row is kept rather than deleted.** `SECURITY DEFINER` is what makes the policy work: the requesting user has no SELECT on another member's `account_directory` row under `account_directory_select_owner`, so without definer the subselect would find nothing, `coalesce` to `true`, and **silently disable follow-request gating for everyone** — a fail-open, in precisely the place this row invites someone to "harden". Anyone acting on the original wording would have introduced that. Deliberately excluded from the 2026-08-12 directory hardening, which tightens the two directory RPCs to `{postgres, authenticated}` and leaves this function exactly as deployed | P3 | **CLOSED as Not a defect (register correction only; no code change).** The anon-executable claim is false as deployed; the residual body-check claim is unreachable. Correcting it matters more than the finding did — the fix it proposed would have made the system worse | 1 |
| B-15 | Weak anti-browse in `search_account_directory` (2-char substrings). **Now carries B-2's surviving half** — the enumeration claim, once B-2's `lookup_enabled` premise was withdrawn (2026-08-12), reduces to this mechanism. **READ BEFORE EDITING THIS FUNCTION.** `ad.user_id <> auth.uid()` is self-exclusion, not a security control. Against an unauthenticated caller it yields NULL and filters every row, which is the only reason anon saw nothing before the 2026-08-12 hardening — an accident, not a defence. Rewriting it as `IS DISTINCT FROM` would return the entire directory to any holder of the shipped anon key the moment a grant to anon returned, and Supabase's default privileges on `public` re-grant anon, authenticated and service_role on any **newly created** object, so a drop-and-recreate of this function restores that grant silently. The `auth.uid() IS NOT NULL` test added alongside the revoke is what makes that survivable; do not remove it while tightening the substring floor | P3 | **Confirmed defect** | 4 |
| B-16 | `post_comment_views` accumulates orphans | P3 | **Confirmed defect** | 6 |
| B-17 | **The backend has no source of truth under version control.** Edge Functions and schema exist only in the hosted Supabase project. This repository contains no `supabase/` directory, no `.ts`, no `.sql`; `~/supabase` holds only a CLI `.temp` scratch directory and is not a git repository. Consequences: the current `delete_account_v1` cannot be read before being rewritten, so B-1/B-3/B-4/B-12/B-13 would have to be fixed from finding descriptions alone; policy and RPC changes for B-2/B-5/B-14 have no diff, no review and no rollback; and nothing pins what is deployed to what was agreed. This directly blocks the Phase 1 backend block, and it matters most precisely where the risk is highest — irreversible account deletion. The register's own lesson applies: several findings were wrong because behaviour was inferred rather than read, and rewriting a P0 deletion path blind repeats exactly that mistake. Fix: pull the functions and schema into version control before any backend change | **P1** | **Resolved (source of truth).** `supabase/` now holds the `delete_account_v1` source and a structural catalog snapshot — functions with definitions, RLS policies, triggers, constraints, columns, grants, buckets. Pulled via `supabase db query --linked`, which goes through the Management API: no Docker, no `pg_dump`, and no credential handling. Verified data-free before commit. **Scope:** this is an audit-grade snapshot, not a replayable migration source — there is no DDL dump and no migration history. Migration tooling (`supabase db diff`, a local instance, hence Docker) is deliberately deferred to Phase 3, where server-side entitlement state will need it. Backend changes before then are reviewed against this snapshot | 1 |
| B-23 | **Production has a structural snapshot but no reproducible, migration-defined source from which the backend can be rebuilt.** `supabase/schema/` is a *record of what production looks like*, refreshed after the fact — not a definition production can be recreated from. There are no migrations, and `supabase/README.md` deferred them to Phase 3 from Phase 1 onward. Two consequences, and the second is what makes this a finding rather than a preference. **(i)** No environment exists in which a destructive or fault-injecting experiment is free, which is why **B-4, B-12, B-13 and B-9's two-recipient subcase have all been blocked since 2026-08-11** — four carried verification obligations resting on a prerequisite that appeared in no register row and therefore had **no durable owner**. That is precisely the shape the Phase 1 exit criticised, one level up. **(ii)** Every schema change to date was applied by hand and recorded afterwards; the snapshot diff is the only record, and a description is not a source. Same drift class as B-17, applied to schema rather than to function code. **Acceptance criterion:** a local Supabase instance stood up **from committed migrations**, against which all ten `capture-schema.sh` queries produce output **byte-identical** to the committed `supabase/schema/` snapshot — `functions`, `policies`, `rls_enabled`, `triggers`, `constraints`, `columns`, `function_grants`, `table_grants`, `column_grants`, `storage_buckets`. **The ten-file empty diff is the gate. Anything less is not a faithful baseline, and no verification run against it counts.** No normalisation may hide a genuine structural difference to make the gate green. **Explicitly NOT in scope:** migrating or re-basing production onto the migration history, and any production schema change. B-23 delivers a faithful *reproduction*; production remains exactly as it is | P2 | **RESOLVED 2026-08-16 — the revised criterion is met from a fresh reconstruction, and the criterion itself was tightened rather than relaxed to get there.** A local stack exists (Colima + `supabase start`, PostgreSQL 17.6 both sides) and `supabase/migrations/20260816000000_baseline_production_reproduction.sql` rebuilds production from committed migrations: 7 tables, 24 constraints, 6 non-constraint indexes, 33 policies, 11 functions, 1 trigger, 131 grants, 20 revokes. **Result from a destroyed-and-rebuilt environment: nine surfaces byte-identical, and `constraints` carrying exactly one approved, mechanically verified catalog-serialization exception.** **THE CRITERION IS NOW PRINCIPLED RATHER THAN A TOLERANCE.** Every surface reproducible byte-identically must be; a catalog-serialization exception is admissible only where individually demonstrated to be PostgreSQL normalization or catalog history rather than a semantic difference; each is declared in `supabase/baseline-exceptions.json` keyed to one row and one field; the tooling still **detects** it and asserts it is *exactly* the approved difference on both sides; and a new difference, a changed difference, or a declared exception that **no longer appears** all fail. **There is no whitespace or parenthesis normalizer** — that would conceal genuine future differences. **The one exception is `account_directory.account_id_format`**, left-nested in production and flat locally, logically identical because `AND` is associative. Admissible on evidence, not on assertion: both servers are 17.6; production's own emitted expression fed back into 17.6 yields the **flat** form; a **right**-nested source *is* preserved, so the parser flattens left-nesting specifically; therefore no migration SQL reproduces it on 17.6 and it is inherited catalog history. **`function_grants` was widened first, because the old capture could not see the thing it was supposed to protect.** `has_function_privilege` is true whether EXECUTE is direct or inherited from `PUBLIC`, which Postgres grants by default — so B-5's hardened RPCs were indistinguishable from untouched ones, with `get_unread_private_comment_groups` the proof (PUBLIC revoked, all three roles direct, rendered identically to the default state). The capture now records `can_execute`, `direct_execute` and `public_execute` via `aclexplode`. **Production was re-captured read-only under the widened definition and the local rebuild matched it with no change to the baseline**, so the agreement is a result rather than an artefact of changing the question. **The gate was proved by making it fail:** six negative cases — approved exception's local value moved, a second constraint difference, the exception no longer appearing, a `function_grants` difference, a policy `qual` difference, and a missing row — all exit non-zero, with the control passing. `verify-baseline.sh` + `check-baseline.py` is an executable gate, not an instruction to inspect and excuse a diff. **Production was not mutated**: only `function_grants.json` moved in the committed snapshot, it is a capture-definition change, and a re-capture is idempotent. **Local verification remains explicitly not production verification** | 3 |
| B-24 | **The frozen U5 activation design proved transaction authenticity and current Apple state, but NOT ownership by the authenticated Études identity.** The design read: *client supplies a transaction/original-transaction pointer → server asks Apple → Apple confirms subscription state*, with the server deliberately treating the client artefact as a pointer only — *"the server ignores its contents as evidence"*. **That is correct about freshness and silent about ownership.** Apple's answer confirms that *somebody's* subscription is active; it never returns the Apple Account, and no Apple surface ever will. So an authenticated claimant presenting **another member's `originalTransactionId`** would have been issued an entitled membership row: Apple truthfully confirms the subscription, and nothing bound it to the caller. **Original transaction identifiers must not be treated as secrets** — they appear in receipts, support correspondence, App Store email and our own logs — and Apple guarantees no secrecy property for them. **THE CORRECTION IS A COMBINED EVIDENCE PROTOCOL, because no single artefact carries both properties.** (i) The **Apple-signed transaction JWS** (`Transaction.jwsRepresentation`) is **possession/ownership evidence**: it is obtainable only from `Transaction.currentEntitlements` on a device signed in as the owning Apple Account, and it is signed by Apple, so it cannot be minted. (ii) A **live server→Apple status read** is **current authoritative entitlement evidence**, because a JWS may be stale. (iii) **`appAccountToken`** is the **durable Études-account binding** — set at purchase for new joins, and attached retroactively to existing subscriptions via `Set App Account Token`, which Apple carries into all future renewals. **The three are not interchangeable and the previous design kept only the second.** **Rebinding authority, stated without overclaiming:** Apple *stores and returns* the binding but does **not** enforce it against us — our In-App Purchase key can call `Set App Account Token`, which overrides any previous value. The protection is therefore our rule, not Apple's: ordinary application logic must **never** automatically overwrite a token belonging to a **live** `membership_binding`; a mismatch is a security and account-recovery event requiring explicit operator disposition; a token matching **no** live binding is an orphan (typically after an explicit Études account deletion) and a JWS-verified claimant may rebind. **A SECOND, NARROWER INSTANCE WAS CAUGHT AND CORRECTED IN U4, 2026-08-20, BEFORE ANY DEPLOYMENT.** U4's first revision let notification ingestion CREATE the initial `membership` row, supplying `binding_method = 'purchase'` on the reasoning that `Set App Account Token` has not shipped, so any token Apple reports must have been attached at purchase. **The reasoning was sound for that window and the design was still wrong, which is the whole lesson of this row restated one level down:** `binding_method` and `bound_at` record *how ownership was proved*, and a value inferred from what has not been built yet is provenance invented rather than established. **The canonical writer is now UPDATE-ONLY and U4 contains no `INSERT INTO public.membership` at all** — asserted structurally over `pg_get_functiondef` (A47f) as well as behaviourally (A47/A47b, E14e), so it cannot drift back. Ownership establishment is U5's alone. **CAUGHT BEFORE IMPLEMENTATION. NO DEFECTIVE PRODUCTION IMPLEMENTATION EVER EXISTED** — no client or server code was written against the superseded design, and U5 does not exist. Found on 2026-08-16 while designing U3's schema, by asking what enforces the `user_id ↔ originalTransactionId` binding | **P1** | **RESOLVED 2026-08-30 — the protocol has run for real, in BOTH directions, against genuine Apple.** Originally *Confirmed defect (design)* — the fix is designed and frozen; U3 lands the schema that makes it enforceable (`membership_binding`, and `membership.binding_method`/`bound_at` NOT NULL so the database cannot hold an ownership-unverified membership row), and **U5 implements the protocol itself**. Open until U5 does. **U5b LANDED THE SQL HALF, LOCAL ONLY, 2026-08-23 — AND THE ROW STAYS OPEN.** `membership_establish_v1` is now the only INSERT into `public.membership` anywhere, asserted structurally over `pg_get_functiondef` across the whole schema (A67/A67b) as well as behaviourally; provenance is **derived from which artefact carried the token** — the JWS itself means `'purchase'`, our own Set App Account Token call means `'legacy_claim'` — so there is no `p_binding_method` parameter for a caller to assert, which is the U4 correction applied one level down. A29 (foreign live token refused and recorded) and A31 (orphan distinguished from mismatch) are **discharged in full**; the live-binding mismatch now has a durable home in `membership_binding_conflict`. **A30 IS NOT DISCHARGED**: Set App Account Token and the independent Apple re-read are HTTPS round trips belonging to U5c/U5d, and U5b asserts only the SQL-side precondition that establishment REFUSES on a token Apple has not confirmed as ours. **U5c AND U5d COMPLETED THE SERVER HALF, 2026-08-23, AND THIS ROW REMAINS OPEN.** `membership_attest_v1` now implements the full protocol locally — authenticated identity from the verified session, claim-checked client JWS (B-31), live Apple read, token comparison resolved **inside** the write transaction, and atomic establishment through U5b's writer with no second writer anywhere. Every branch of the matrix is proven end to end against a programmable Apple: new purchase, legacy claim, orphan rebind, live-binding conflict (**which never issues Set App Account Token at all** — Apple would let us overwrite another identity's token, which is exactly why our rule is the only protection), propagation delay, terminal refusal and retryable failure. **A30's LOCAL half is discharged as an ORDERING proof** — observed outbound `GET, PUT, GET, GET`, with the call immediately after the PUT asserted to be a read — because an implementation that establishes on the PUT's own 200 produces an identical row and is wrong. **THE LEGACY HALF OF THE PROTOCOL IS NOW PROVEN AGAINST GENUINE APPLE, 2026-08-25.** On Device A with a real Sandbox subscription: a genuine JWS passed the **pinned** anchor (S-1); the legacy claim established `binding_method='legacy_claim'`, whose provenance is reachable only via `GET -> PUT -> GET` (S-2); and a real `DID_RENEW` then ingested as **`applied`** where all 14 earlier notifications for the same subscription were `unmapped` (S-3) — **Apple carried our binding token into a genuine renewal**, which is exactly what this row predicted and what no stub could show. `pending_cleanup_at` NULL throughout, provenance immutable across the refresh, `connected_member()` false and `membership_state()` `sandbox_only`. **THIS ROW NEVERTHELESS STAYS OPEN**, because B-24's design has two halves and only one has met Apple: the **bound-at-source** path — a new purchase carrying `.appAccountToken` at StoreKit, yielding `binding_method='purchase'` with **no PUT issued** — is a different code path and needs a fresh sandbox tester. **THE REMAINING REASONS:** the endpoint is **not deployed**; **no client exists** to present a JWS (U5e/U5f), so the protocol has never run end to end with a real participant; and everything is **verified against a faithful local reproduction**, never against Apple — S-1, S-2 and S-3 discharge only in genuine Sandbox. This row closes when the protocol runs for real, not when its server half compiles. **THE BOUND-AT-SOURCE HALF IS NOW DISCHARGED (B-24n, 2026-08-30), AND WITH IT EVERY REASON THIS ROW LISTED FOR STAYING OPEN.** Those reasons were: the endpoint not deployed; no client existing to present a JWS; and everything verified only against a faithful local reproduction. All three are now false — `membership_attest_v1` is live, U5e/U5f ship the client, and both branches have met Apple. On a FRESH Sandbox tester, a U5f purchase carrying `.appAccountToken` established `binding_method='purchase'` on a new `originalTransactionId`, with `pending_cleanup_at` and `entitlement_ended_at` both NULL, `connected_member()` false, `membership_state()` `sandbox_only`, and the conflict table empty. **`'purchase'` PROVES AN ABSENCE**: provenance is derived, never supplied, so that value is written only when the client's own Apple-signed JWS already carried our stored token — meaning the legacy branch was never entered and **no Set App Account Token PUT was issued at all**. **The binding row was never touched** — `created_at` and `updated_at` both `2026-08-25 17:31:54.005854`, identical — so one unchanged token survived a legacy claim, eleven renewals, a cancellation, a genuine expiry, deletion of the membership row, **a change of Apple Account**, and a fresh purchase. That last step is the part no local reproduction could show, and it measures rather than argues this row's own claim that the token is an attribute of the IDENTITY, not of a subscription. **The `SUBSCRIBED`/`INITIAL_BUY` notification landed `ignored`/`unestablished`, which is a PASS** — the binding matched on the first notification of the subscription's life, where `unmapped` would have falsified it — and incidentally proved U4's UPDATE-ONLY rule on genuine Apple traffic, since no row existed at 14:58:15 and ingestion created none. **What this row does NOT close: enforcement and cleanup.** Nothing consults membership, no worker exists, and Production remains untouched — those are C-26, B-11 and C-31, owned by U6/U7 | 3 |

| B-25 | **Derived entitlement fails into scheduled cleanup when Apple omits a field, and two independent limbs produce it.** Verified against Apple's live schema 2026-08-19: **every field of `JWSRenewalInfoDecodedPayload` is optional**, and `data.signedRenewalInfo` is documented as appearing **only for auto-renewable subscription notifications**. **Limb A — `renewal_date`.** `renewalDate` means *the time the most recent auto-renewable subscription purchase expires*. A writer that sources `membership.renewal_date` from `renewalInfo.renewalDate` alone, on a payload where Apple omits it, stores NULL; `connected_member()` then evaluates `coalesce(null > now(), false)` → false, so a **live subscription derives not-entitled** and under U7 that schedules cleanup. The reliably-present per-period paid-through value is `transactionInfo.expiresDate`. **Limb B — ordering key.** `membership.renewal_info_signed_date` is `NOT NULL`, and `renewalInfo.signedDate` is optional, so a legitimate signature-valid state-bearing notification carrying transaction info but **no renewal info can never write a membership row at all**. U3's constraint is correct and is NOT relaxed — *a row that cannot be ordered must not exist* — but its consequence had to be designed for rather than discovered at runtime. **Remedy, agreed 2026-08-19, and it needs no schema change:** the canonical writer computes `renewal_date := renewalInfo.renewalDate ?? transactionInfo.expiresDate`; if neither that nor `renewalInfo.signedDate` is available it **refuses to write and returns `needs_reconciliation`**, which triggers a live `GET /inApps/v1/subscriptions/{id}` whose `signedRenewalInfo` does carry a `signedDate`. Refusing-and-reconciling is strictly safer than writing a null. **Reinforced by the U4 scheduling rule:** `pending_cleanup_at` and `entitlement_ended_at` may be set **only** on state that is signature-verified, ownership-mapped through a live binding, and complete — the `needs_reconciliation` branch leaves both untouched. `membership_cleanup_requires_end` is necessary but not sufficient for that rule, which is enforced in the writer and asserted separately | **P1** | **IMPLEMENTED AND LOCALLY VERIFIED 2026-08-20 — not deployed.** `derive.ts` coalesces `renewalInfo.renewalDate ?? transactionInfo.expiresDate`; anything lacking a usable paid-through date OR an orderable `renewalInfo.signedDate` returns disposition `incomplete`, which writes **no state and no schedule** and returns `needs_reconciliation`. **The limb that matters is asserted in both directions:** U4b-8/9 prove that `renewalDate` absent with `expiresDate` present is **still applied** — writing NULL there would have derived "expired" and, under U7, scheduled cleanup on a live subscription — and A51e/E30 prove `pending_cleanup_at` stays **null** on the incomplete path. **RESOLVED 2026-08-20 — DEPLOYED TO PRODUCTION and verified there**, P0-P13 of `supabase/sql/README-u4-deployment.md`. The structural delta matched the committed prediction on all ten surfaces and B-23 returned GREEN after recapture | 3 |
| B-26 | **`membership_notification.outcome = 'duplicate'` is structurally unwritable, so replays leave no record and QA G2 asserts something no correct implementation can produce.** `membership_notification_uuid_key` is `unique (notification_uuid) where notification_uuid is not null`. A replayed notification carries the same `notificationUUID`, so a second row cannot be inserted — the CHECK enum anticipates a value the index forbids, and the only alternative (updating the surviving row's outcome to `'duplicate'`) would destroy the record of what the first delivery actually did. The strongest observable under the deployed schema is *absence of a second row*, which is a weak assertion because an endpoint that silently drops everything produces it too. **Remedy, agreed 2026-08-19 — additive, on a table holding zero rows:** add `delivery_count integer not null default 1` and `last_received_at timestamptz`, written by `on conflict … do update set delivery_count = delivery_count + 1, last_received_at = now()`. The stored `outcome` remains that of the first delivery; **`'duplicate'` becomes a value the entry-point function RETURNS to its caller rather than one it stores.** G2 is then restated to assert a counter going 1→2 with `membership` unchanged — which proves the replay was seen and refused, not merely unprocessed | P2 | **IMPLEMENTED AND LOCALLY VERIFIED 2026-08-20 — not deployed.** `delivery_count` and `last_received_at` added to `membership_notification` (zero rows at the time, as predicted); the dedupe is a single `on conflict … do update` so a replay cannot race. **`'duplicate'` is now a value the entry point RETURNS and never stores** — the stored `outcome` remains the first delivery's. QA G2 is restated accordingly: A48–A48e and E18–E20 assert `delivery_count` 1→2 with `membership` unchanged, which proves the replay was seen and refused rather than merely unprocessed. **RESOLVED 2026-08-20 — DEPLOYED TO PRODUCTION and verified there**, P0-P13 of `supabase/sql/README-u4-deployment.md`. The structural delta matched the committed prediction on all ten surfaces and B-23 returned GREEN after recapture | 3 |
| B-27 | **The notification outcome vocabulary conflates hostile traffic with benign non-applicable traffic, and the highest-volume routine category has no home but `rejected`.** `outcome ∈ ('applied','stale','duplicate','rejected')` with `failure_category` required exactly when `rejected`. That leaves `rejected` as the only slot for Apple's own `TEST` notification, `RENEWAL_EXTENSION` summaries, `EXTERNAL_PURCHASE_TOKEN`, `RESCIND_CONSENT` — and, decisively, for **every notification whose subscription is not mapped to an Études identity**, which between U4 and U5 is *all of them*, since no Apple subscription carries an `appAccountToken` until U5 sets one. A `rejected` count is a security signal; loading it with the commonest routine category destroys it, and B-11's shadow window is later read against exactly this record. **Remedy, agreed 2026-08-19 — three CHECK replacements on a zero-row table:** extend `outcome` with `'ignored'`; change `membership_notification_failure_only_when_rejected` to `(failure_category is null) = (outcome not in ('rejected','ignored'))`; extend `failure_category` with `'unmapped'` and `'not_applicable'`. **`'signature'` and `'decode'` are deliberately RETAINED in the enum even though B-29 moves outer-envelope failures out of this table entirely** — an inner JWS that fails verification *after the outer envelope verified* is a genuine and serious anomaly (Apple signed the envelope and its contents disagree) and must keep a durable per-row home | P2 | **IMPLEMENTED AND LOCALLY VERIFIED 2026-08-20 — not deployed.** `outcome` admits `'ignored'`; `failure_category` admits `'unmapped'`, `'not_applicable'` and `'incomplete'`; the paired constraint is renamed `…_failure_only_when_categorised`; and `…_accepted_is_complete` relaxes **`environment` alone, for `'ignored'` alone**, so a genuinely Apple-signed payload carrying none is not forced to look like a reject. **The dividend is measured, not asserted:** an unmapped notification records `ignored`/`unmapped` (A50b, E22) and Apple's own TEST notification records `ignored`/`not_applicable` (E24), so `rejected` in this table now means only *Apple-signed but refused downstream*. **RESOLVED 2026-08-20 — DEPLOYED TO PRODUCTION and verified there**, P0-P13 of `supabase/sql/README-u4-deployment.md`. The structural delta matched the committed prediction on all ten surfaces and B-23 returned GREEN after recapture | 3 |
| B-28 | **ASSN V2 x5c chain verification had no proven route in the Supabase Edge Runtime, and the official Apple library is unusable there in a way that is indistinguishable from a forgery.** ASSN V2 payloads are JWS signed with an **x5c chain** (leaf → Apple WWDR intermediate → Apple root), not a JWKS, so verification requires walking and validating an X.509 chain against a **locally embedded** Apple Root CA G3 — the root presented in `x5c[2]` is attacker-supplied and must never be trusted as given. The repo's existing `revoke_apple_identity_v1` proves Web Crypto ES256 signing and JWKS verification and covers none of that. **RESOLVED BY THE U4a GATE, 2026-08-19 — run inside `public.ecr.aws/supabase/edge-runtime:v1.74.3` (Deno 2.1.4), in BOTH the main worker and a spawned user worker, with identical results. Full evidence in `docs/qa-plan.md` under "U4a — GATE RESULT".** **Route `node:crypto.X509Certificate`: DEAD.** The class exists and `hasX509` probes true, but `X509Certificate.prototype.verify` and `.toString` both throw `ERR_NOT_IMPLEMENTED`. **Capability presence is not capability, and the cheap probe said the opposite of the truth** — the same shape as C-19 and B-18, where structure implied a behaviour that reading further disproved. **Route `@apple/app-store-server-library@1.6.0`: IMPORTS CLEANLY AND REJECTS EVERYTHING, INCLUDING PAYLOADS IT SHOULD ACCEPT.** Against a fixture built to Apple's own shape — P-384 root and intermediate with `ecdsa-with-SHA384`, P-256 ES256 leaf, and both Apple marker OIDs (`1.2.840.113635.100.6.2.1` on the intermediate, `1.2.840.113635.100.6.11.1` on the leaf) — it returned `VerificationException` with `status = 1 (VERIFICATION_FAILURE)`, `cause: Error [ERR_NOT_IMPLEMENTED]: Not implemented: crypto.X509Certificate.prototype.toString`. **The severity is not that it fails; it is that it returns the SAME status for its own runtime incapacity as for a genuine forgery.** Deployed on this runtime it would have rejected 100% of real Apple traffic while filling the audit table with rows indistinguishable from an attack. It fails closed, so nothing could be forged past it — but every legitimate notification would have been lost, and the record would have said "attack". **Route `@peculiar/x509@1.12.3` + Web Crypto: PROVEN, and adopted.** Verified the good fixture end to end including both nested JWS, and rejected all eight negatives with distinct, meaningful reasons: tampered payload, substituted root, `alg: none`, absent `x5c`, short chain, reordered chain, tampered nested `signedTransactionInfo`, wrong pinned anchor, and an expired leaf. **The real Apple link is proven, not simulated:** the genuine `Apple Worldwide Developer Relations Certification Authority G6` certificate verified against the genuine `Apple Root CA - G3` (both P-384, `ecdsa-with-SHA384`) → `true`, with a correct `false` against an unrelated root, and the G6 Apple OID present. Cost: **43 ms** module import, **4.51 ms** mean full verification over 20 iterations. **What the gate did NOT prove, stated rather than glossed: no genuine Apple-signed ASSN payload was verified, because none can be legitimately obtained without a deployed endpoint.** The leaf link and the JWS signature are proven only against a faithful synthetic chain. **The first genuine Apple-signed payload this project can obtain is Apple's own test notification at U4i, and that is where the remaining half of this row discharges** | **P1** | **RESOLVED 2026-08-20 — a genuine Apple-signed payload verified through the deployed path.** Apple's own test notification was requested via `appstore_reconcile_v1`, Apple reported `firstSendAttemptResult = SUCCESS` on attempt 1, and three seconds later `membership_notification` held exactly one row: **`TEST` / `ignored` / `not_applicable` / `delivery_count = 1`, Sandbox, 4777 bytes**, with a well-formed digest. **THE PROOF IS WHERE THE ROW LANDED, NOT THAT ONE DID.** It is in `membership_notification` as a verified notification and NOT in `membership_notification_reject_stat`, which remains **empty — zero signature failures, ever**. Had the verifier been dead in the way this row documents for the official Apple library, the payload would have landed in the reject aggregate with `failure_category = 'signature'` while the endpoint still returned healthily from outside. **The x5c chain, the intermediate->leaf link and the JWS signature are now all proven against Apple's real certificates rather than a synthetic chain, which is precisely what this row was held open for.** **Route resolved 2026-08-19 and re-provable on demand.** The route is now in production source — `_shared/appstore/jws.ts`, `@peculiar/x509@1.12.3` exactly pinned with a committed `deno.lock` carrying sha512 integrity for all 19 packages — and the gate is **re-runnable rather than a recorded result**: `supabase/tests/u4a/run.sh` re-proves all three routes on every run, against the shipping module, and returns **25 of 25**. **The honest limit is unchanged and must not be quietly dropped: no genuine Apple-signed ASSN payload has ever been verified by this code.** Root→intermediate is proven against Apple's own certificates; intermediate→leaf and the JWS signature are proven only against a faithful synthetic chain. **Apple's own test notification at U4i was the first genuine Apple-signed payload this project could legitimately obtain, and it discharged this row on 2026-08-20** | 3 |
| B-29 | **The notification endpoint is the project's first genuinely unauthenticated surface, and persisting a row per rejected POST would hand any caller an unbounded durable INSERT primitive.** `delete_account_v1` and `revoke_apple_identity_v1` both set `verify_jwt = false` but perform their own `auth.getUser(token)`; **U4's ingestion endpoint has no such option**, because Apple sends no Supabase JWT and the JWS signature is the entire authorisation story. **The first proposal — persist every reject, capped only by body size — was NOT ACCEPTED, 2026-08-19, and correctly so:** it bounds row *size* while leaving row *count* under the control of anyone who learns the URL, which both costs storage without limit and drowns the diagnostic signal the table exists to carry. **U3 is not contradicted by bounding it.** U3's surrogate primary key exists so that a decode failure with no extractable `notificationUUID` is *representable*; it does not require that every such payload be *persisted*. **The agreed treatment is three tiers, and the principle is that durable per-row persistence is a privilege earned by passing signature verification.** **Tier 1 — pre-verification structural rejects write NOTHING**: method, content-type, a hard body cap, three-part JWS shape, base64 decodability, header parse, `alg = ES256`, `x5c` present with ≥3 entries. O(1) CPU, zero rows, answered `400`. **Tier 2 — outer-envelope signature failures increment a FIXED-CARDINALITY aggregate**, a new U4-owned table keyed on `(hour_bucket, failure_category)` carrying `count`, `first_seen`, `last_seen` and a capped sample of payload digests — so the table cannot exceed hours × categories rows however large the flood, while U3's stated question (*one payload replayed, or many different ones*) stays answerable from the sampled digests. **Tier 3 — signature-verified notifications persist per-row in `membership_notification` exactly as U3 designed**, bounded by Apple's own sending rate. **The dividend is that `rejected` becomes meaningful again**: in `membership_notification` it can now only mean *Apple-signed but refused downstream*, which is low-volume and always worth reading. **Residual, stated rather than hidden:** an attacker can still cause one bounded upsert per Tier-2 request, which is a load concern and not a growth concern, and the aggregate is diagnostics that an operator may purge freely — unlike anything in `membership`. **One consequence reverses a frozen QA expectation and is not adopted unilaterally — see the G2 amendment proposed in `docs/qa-plan.md`:** G2 currently requires an unsigned payload be answered `200`, reasoning that it will never become valid so retrying achieves nothing. **B-28 falsified the premise.** The catastrophic case is not a forgery but a verifier that rejects everything, which is exactly what the official Apple library would have done on this runtime; under `200` every legitimate notification is lost immediately, whereas under `5xx` production retries five times over 72 hours and a fix inside that window recovers them. Sandbox never retries either way, so the choice costs nothing there | P2 | **Confirmed defect (design), caught before implementation.** **Tiered remedy AGREED and IMPLEMENTED 2026-08-20 — not deployed.** Tier 1 is `structurallyPlausible()` plus a 64 KB body cap and a content check, and it touches no database at all; Tier 2 is `membership_notification_reject_stat`, keyed `(hour_bucket, failure_category)` with a digest sample capped at eight, so **the table cannot exceed hours × categories rows however large the flood**; Tier 3 is unchanged from U3's design. **Measured rather than argued:** E6/E7 show Tier 1 writing zero rows AND zero counters, E12/E13 show two signature failures producing two increments in **one** row, and A56–A56g show twelve rejects producing one row with an eight-entry sample. **The G2 status-code amendment is now AGREED and live** — 400 structural / 5xx signature / 200 durably handled — and its reasoning is recorded in `docs/qa-plan.md`. **RESOLVED 2026-08-20 — DEPLOYED TO PRODUCTION and verified there**, P0-P13 of `supabase/sql/README-u4-deployment.md`. The structural delta matched the committed prediction on all ten surfaces and B-23 returned GREEN after recapture | 3 |
| B-30 | **Two entry-point RPC calls from an Edge Function are two PostgreSQL transactions, so the originally proposed U4 write surface — a separate `record_notification` and `apply_state`, each granted to `service_role` — could not be atomic.** An audit row could commit while the state change it describes rolled back, or the reverse, leaving `membership.last_notification_uuid` naming a notification with no audit row. **PROVEN LOCALLY, 2026-08-19, rather than assumed** — three observations on the B-23 instance through PostgREST with the service role: two successive `rpc/u4a_txid` calls returned `pg_current_xact_id()` **20888** and **20889**, so each request is its own transaction; a single RPC that inserted and then raised left **no row**; and two RPCs where the first inserted and the second raised left the **first row committed**. Probe objects were created in `public`, dropped, and the **B-23 gate re-run GREEN** with only the standing `account_id_format` exception, so nothing survived the experiment. **Resolution, agreed 2026-08-19: one entry point per path, one transaction per entry point, and the canonical writer is internal.** `membership_ingest_notification_v1` records, resolves the binding and applies state in a **single** transaction; `membership_apply_reconciliation_v1` does the same for a live Apple read; both delegate to `membership_apply_state_v1`, which is **granted to no role at all** and is reachable only from inside those two `SECURITY DEFINER` entry points — so "one canonical writer" and "atomic per path" are both satisfied, which the two-granted-function shape could not do. **Expected outcomes are RETURN VALUES, never `raise`** — `stale`, `duplicate`, `unmapped`, `needs_reconciliation` — so nothing routine can roll back the audit row; only genuine infrastructure faults raise, and losing a diagnostic row is then the correct trade against committing half-applied state. **The Apple network call is never inside a transaction**; a notification needing reconciliation commits its audit row first, the HTTPS read happens outside, and the second apply is separately atomic and ordered by `renewal_info_signed_date`, so a failure between the two is self-healing rather than corrupting. **`service_role` still receives ZERO table DML** — U3's A3f invariant (no grantee other than the owner in `relacl` for any of the five tables) survives U4 unchanged, and the whole U4 privilege delta is EXECUTE on two functions | P2 | **RESOLVED IN DESIGN AND IMPLEMENTED 2026-08-20 — not deployed.** Four entry points carry EXECUTE for `service_role` and each does everything its path needs in ONE transaction; the canonical writer `membership_apply_state_v1`, the B-24 resolver and the audit recorder are granted to **no role at all**. **Proven by attempting it rather than asserted:** A45b shows `service_role` holds no EXECUTE on the three internal functions and A46e shows a direct call refused, while A46–A46d show direct INSERT, UPDATE, DELETE and even SELECT on the membership tables all refused. **`service_role` still holds zero table privilege on all six membership tables (A44), so U3's A3f invariant survives U4 unchanged** and the entire U4 privilege delta is four EXECUTE grants. **AUDITED 2026-08-20 and found minimal, with two tightenings:** the reconciliation selector now returns three columns instead of five, because `renewal_info_signed_date` and `pending_cleanup_at` were never read and staleness ordering happens inside the function — **a scheduling column has no business leaving the database on a path with no use for it** — and `membership_record_reject_v1` now validates its own inputs. **`membership_record_reject_v1` could fold into the ingest entry point and deliberately does not:** that would put the function able to write `membership` on the path an unauthenticated caller reaches by sending garbage, whereas kept separate the Tier-2 path can touch nothing but the bounded aggregate. **RESOLVED 2026-08-20 — DEPLOYED TO PRODUCTION and verified there**, P0-P13 of `supabase/sql/README-u4-deployment.md`. The structural delta matched the committed prediction on all ten surfaces and B-23 returned GREEN after recapture | 3 |
### `delete_account_v1` — deployed, not yet verified

**Eight rows above depend on one function: B-1, B-3, B-4, B-9, B-12, B-13,
B-19 and B-20.** The rewrite carrying all eight is **deployed to production** —
version 4, `verify_jwt: false`, committed at `ca00189` (rewrite) and `5714c53`
(B-20, avatar by prefix as well as by pointer). Production was confirmed
drift-free against the captured baseline before the deploy, and the deployed
source was confirmed byte-identical to the committed source after it.

**None of the eight moves to Resolved on the strength of that.** They move when
QA D5–D13 has been run, per the rule at the top of this register: an item
becomes Resolved only when code has changed *and been checked*. Deployment is
not a check.

### Run 1 result — 2026-08-11, D5–D9 + D11 + D12 combined

Six preconditions staged on one disposable account, then a single Erase All, with
a read-only snapshot either side. Backend result was a clean pass on every
runnable row: the bucket prefix went 5 → 1 leaving exactly the live sent asset,
both retained comments survived, and `auth.users`, `account_directory`, `posts`,
`follows` and `post_comment_views` all went to zero. The recipient confirmed the
surviving attachment still opens.

**Five of the eight are now Resolved: B-1, B-3, B-19, B-20 and B-9** — the first
four after runs 1 and 2, B-9 after run 3 (D14). The other three are not, and the
reasons differ — read the cells rather than assuming
a green run covered them:

| Finding | Why the runs do not close it |
|---|---|
| B-4 | Positive direction only. A green run proves nothing the old function would not also have produced |
| B-12 | Never executed — five objects against a 1000-entry page boundary |
| B-13 | D10 deferred; no safe fault-injection path exists yet |

### Run 2 result — 2026-08-11, D13

Staged on a second disposable account by setting `avatar_key` to NULL with the
object left in place, then erasing. **PASS, and it is the only run that proves
anything about the B-20 fix:** with the pointer null the deployed function skips
the keyed branch entirely, so the prefix sweep is the sole remaining route to
the object — and the object was removed. The old function would have left it.
**B-20 is Resolved.** Setup recorded as **simulated rather than provoked**; the
reasoning is in B-20's cell.

Checking D13's blast radius is what surfaced **B-21** — two avatars from accounts
deleted in July, still in the bucket, unreachable by any path. B-20 observed in
production rather than argued from source. Both turned out to be the developer's
own earlier beta accounts, so no real user's data was involved; the row was
downgraded to P3 and the files cleared manually.

### Run 3 result — 2026-08-11, D14 (B-9)

The first run against the **step 3b** deploy, and the first in which B-9's
received-row path has ever executed. Staged on two accounts across two devices,
each signed in with a different iCloud Apple ID: three attachment legs, one
erase, read-only snapshots either side.

**PASS on every assertion.** The received row was deleted, the sender tombstone
was deleted, and the live sender row survived with its object under the deleted
user's own prefix — B-1 unregressed, confirmed by the recipient opening the file
afterwards. **B-9 is Resolved.**

Two things worth carrying forward, neither of which is the fix itself:

**A prediction was written down before the erase and all eight counts matched
it** — `auth.users` 16 → 15, directory 16 → 15, `connected_attachments` 37 → 35,
`follows` 8 → 6, attachment objects 9 → 8, with posts, comments and avatar
objects unchanged. Predicting first is what made the check binary. Every previous
run in this register was read *after* the fact, where "nothing looks wrong" is a
much weaker claim than "these eight numbers, and no others, moved".

**The best evidence in the run was not staged.** Account B happened to hold a
soft-deleted attachment row from a *third-party* sender. Step 3b is scoped
`sender_user_id = <departing>`, so that row had to survive — and did. Had the
step been written the way B-9's own cell originally described it, "delete the
rows whose objects were just swept", the natural implementation would have taken
every soft-deleted row and destroyed a third party's reference. The wrong
justification would have produced a wrong fix that this run would still have
passed, had B's data not happened to contain the counter-example. **That is an
argument for correcting a finding's stated reasoning even when its proposed
predicate is right.**

**Expected residue, recorded so it is not filed twice.** The departing member's
received row named an object under **B's** prefix, and A's sweep only covers A's
prefix — so that object now has no reference at all. This is the unowned-object
lifecycle the function header already defers to B-8/B-10 in Phase 4. It was
predicted before the run rather than discovered after it.

### Whole-backend orphan sweep — 2026-08-11, read-only

Prompted by B-21: the old function ran for months, so the question was what else
it left behind. Answer: **very little, and nothing exposed.**

- **Zero orphaned `posts`** — B-4's worst case, rows unreachable by any RLS
  policy, never materialised.
- **Zero orphaned `post_shares`, `post_comment_views` or `follows`.**
- **Zero orphaned storage objects in `attachments`.** The avatars bucket was the
  only place with residue, which is precisely where the audit had no coverage.
- The only `post_comments` with a dead author or recipient are the two from
  D7/D8 earlier the same day — retained by decision, working as designed.
- 30 dead `connected_attachments` rows, none with surviving storage. Measured
  under B-9.

**One false positive, worth more than the sweep itself.** The first pass flagged
an orphaned attachments object by asking whether the uid in the path prefix still
existed in `auth.users`. It was D5's survivor. The prefix carries the **sender's**
uid, and a dead sender is the *correct* state for a preserved asset — so the
obvious cleanup heuristic destroys exactly what B-1 protects. That warning is now
recorded on B-8, which is where someone will eventually write that cleanup.

**Two client-side gaps run 1 exposed, neither of them backend defects.**
First, D7 and D8 also ask that the now-unresolvable author renders gracefully,
and that could not be reached: comments exist only in feed view **by design**,
and the feed is empty when the viewer follows nobody — which is exactly the state
an erase leaves behind when the departing member was the only account they
followed. Verifying it needs a viewer who follows a third account. Second, the
avatar-propagation observation that narrowed C-34 and produced C-37.

**THE FIRST GAP IS SUPERSEDED — corrected 2026-08-14, and the correction is the
point.** Both halves of that paragraph turned out to be wrong, and neither was
caught by re-reading it. (i) **The stated blocker is false.** The C-35 run on
2026-08-13 rendered the thread on Device B *after* A's deletion — "A's comment is
gone from the thread" is an observation that required the thread to be on screen.
C-39 had already recorded the contradiction ("both observations cannot be right")
without anyone drawing the consequence for these two rows. (ii) **The gap was
attributed to the wrong half.** It says "the now-unresolvable **author**", but
under the revised rule the departing member's comments are deleted, so no
unresolvable author survives an erase at all — B-3's client half is moot. What
survives is an unresolvable **recipient**, which is B-19's alone. A gap recorded
once and never re-read outlived both the rule change that dissolved half of it
and the run that disproved its blocker; this is the shape to watch for at a phase
gate. B-19's cell now carries the narrowed premise and the fixture.

**CLOSED 2026-08-14.** Both halves are gone: B-3's was moot under the revised
rule, and B-19's was observed green on Device B / Études Dev using the existing
C-35 fixture — no staging, no new account, no destructive action, and the
non-failure it confirmed was predicted from source beforehand. The second
client-side gap in the paragraph above (avatar propagation) remains live as C-34
and C-37, Phase 5.

`verify_jwt` is pinned in `supabase/config.toml` (`43be489`). Without it the CLI
defaults to `true` and any deploy from a clean checkout would silently change
the authorisation configuration of the deletion path as a side effect of
shipping unrelated code — caught before the B-20 deploy, not after.

**Retracted:** an earlier P0 asserted cross-user storage destruction via the
cleanup triggers. `pg_trigger` shows they are attached to nothing, so the attack
does not work. What survives is B-10.

---

| B-31 | **`verifyAppleJWS` performs no audience binding — it verifies the x5c chain and the ES256 signature and checks NO claim.** Not `bundleId`, not `productId`, not `environment`, not freshness. **This is correct-as-deployed and becomes a P1 the moment U5 exists**, which is why it is filed now rather than after. U4's only callers are Apple's own notification stream and our own authenticated reconciliation reads, so the set of payloads reaching the verifier is already constrained by who can deliver them, and environment is filtered separately in `appstore_notifications_v1` against `APPLE_ASSN_ALLOWED_ENVIRONMENTS`. **U5 inverts that entirely: the transaction JWS arrives FROM A CLIENT**, so as written, any Apple-signed transaction JWS from **any app on any device** passes verification and would be accepted as ownership evidence for an Études subscription. **The claim checks are not hardening; they are the difference between the artefact proving something and proving nothing.** Mandatory for U5: `bundleId` equal to `APPLE_IAP_BUNDLE_ID`, `productId` within the two known Connected products, `environment` within a NEW `APPLE_ATTEST_ALLOWED_ENVIRONMENTS` (deliberately separate from the ASSN variable — they answer different questions, and coupling them means a future notification change silently alters attestation), and a freshness bound whose viability is what the U5a F3b gate exists to determine. Found 2026-08-20 during the U5 audit, by asking what the verifier would accept from a caller who is not Apple | **P1 (conditional)** | **Confirmed gap — scheduled, not missing by accident, and not a defect in deployed code.** Same disposition shape as C-26: it becomes a defect only if U5 ships without it. **Open until U5 implements the claim checks.** Agreed as decision D-F2 on 2026-08-20: claim binding is **mandatory** for U5, not optional hardening. **U5c BUILT THE CLAIM BOUNDARY, 2026-08-23, AND THIS ROW STAYS OPEN — the distinction is deliberate and is the whole point of the row.** `_shared/appstore/attest.ts` now enforces bundleId, environment, product, Family Sharing and `originalTransactionId` **after** signature verification, with `verifyAppleJWS` left byte-for-byte untouched so U4's notification path still answers only "did Apple sign this" (asserted at U5c-67). 68 module assertions cover it, and **every hostile fixture is VALIDLY APPLE-SIGNED** — a regression makes a stranger's genuine subscription *accepted*, not failing, which is why fixtures of that shape are the only ones that could test this. **BUT NOTHING CALLS IT YET.** A module that enforces a boundary is not the same as an endpoint that uses it, and this project has been bitten by exactly that gap before. **RESOLVED 2026-08-23 BY U5d, on both halves of the condition this cell named** — implemented and verified **against a faithful local reproduction**, NOT in production, and **not deployed**. *Structural:* `E5d-STRUCT2` asserts the shipping endpoint contains **zero** calls to `verifyAppleJWS`, and `E5d-STRUCT1` that it imports and calls `verifyAttestationJWS`. That check is only one line because the live-read verification was deliberately moved into `_shared/appstore/readAuthoritativeState` — **a design decision taken to make the assertion possible**, so "client input goes through the claim boundary" stops being a reviewer's judgement about which variable reached which call. *Behavioural:* `E5d-5`..`E5d-12` drive the real endpoint with another app's genuine Apple-signed subscription, a wrong product, a disallowed environment and a FAMILY_SHARED transaction — every one refused **422**, and `E5d-11` proves **none of them reached Apple at all**. **A caveat worth keeping: all three of these structural assertions first FAILED on a correct file**, because the endpoint's own header states "THIS FUNCTION NEVER CALLS verifyAppleJWS" and a raw text search found the sentence claiming the property. They strip comments now — the second occurrence of that shape in two units. **What is still outstanding is not this row:** the endpoint is undeployed and no client presents a JWS yet, which is B-24's remainder, not B-31's **`deviceVerification` was examined as a stronger alternative and rejected on evidence:** it binds a transaction to a device via `AppStore.deviceVerificationID`, and the nonce is StoreKit's rather than ours, so a server that never sees the device cannot use it as a challenge and a replaying client supplies the matching identifier as easily as the legitimate one. Recorded because it is the obvious thing the next reader will reach for | 3 |

| B-32 | **`appstore_reconcile_v1`'s `notification_history` mode ALWAYS reports zero, and is structurally incapable of reporting anything else.** It maps each history item with `String(h?.notificationUUID ?? "")` and filters the empties — but **`NotificationHistoryResponseItem` has no top-level `notificationUUID`.** Apple's reference gives it exactly three properties: `sendAttempts`, `signedPayload` and `firstSendAttemptResult` (read from Apple's own documentation 2026-08-23, not inferred). The UUID lives **inside the signed payload**, which the function never decodes and never returns. So every item maps to `""`, every item is filtered out, and `apple_notification_count` is `uuids.length` = **0** whatever Apple actually sent. **OBSERVED, not reasoned: a live Sandbox query over 2026-08-19..24 returned `apple_notification_count: 0` while `membership_notification` held 14 rows from that exact window — including Apple's own TEST notification, whose `attemptDate` (1787248109170) Apple itself reported inside the window at U4i/P13.** **THE CONSEQUENCE IS THAT G3 CANNOT BE SCORED WITH THIS ENDPOINT.** G3 is missed-notification recovery, and this mode exists as *"the only honest way to score G3 — sandbox never retries, so 'we never received it' is otherwise unfalsifiable"*. As written it would report that Apple sent nothing, which makes a genuine gap indistinguishable from a quiet Apple. **It is the mirror image of the mistake U4's own header warns about** — that function insists an empty Apple result "must read as 'no answer' and not as 'not entitled'", and then in the diagnostics path treats an unparseable shape as a confident zero. **It was never caught because the local suites do not exercise this mode against a realistic history response** — it is diagnostic-only, and U4's P12/P13 used `request_test_notification` and `test_notification_status` instead. Found 2026-08-23 during U5g's pre-flight reconciliation | P2 | **RESOLVED 2026-08-23 — DEPLOYED AND RECONCILED AGAINST REAL APPLE HISTORY.** The corrected `appstore_reconcile_v1` (v2 -> v3, SHA changed) returned **`apple_notification_count: 14`, `unverifiable_items: 0`** for the same Sandbox window that had returned a structural `0` before the fix — and the reconciliation was done on **SETS, not counts**: Apple's 14 uuids and our 14 `membership_notification` uuids are **identical**, with **zero** in Apple-but-not-ours and **zero** in ours-but-not-Apple. The deployed bundle was download-diffed and contains `readNotificationHistory`, `unverifiable_items` and `pagination_token` with **zero** occurrences of the old bare `.notificationUUID` read. **The fix, for the record:** The fix lives in a new `_shared/appstore/history.ts`: each item's `signedPayload` is **verified against the pinned anchor** and the `notificationUUID` read from the verified payload. **Verification is a REQUIRED PARAMETER, not an internal detail** — there is no decode-only route through the module — because this function is the evidence G3 is scored against, and an item Apple did not sign must never be able to manufacture agreement with our own records. Items that fail verification are **counted as `unverifiable`, never silently dropped**: a zero count with `unverifiable > 0` is explicitly not 'Apple sent nothing', which is the distinction the old code could not express. `hasMore` and `paginationToken` are now surfaced too, closing the second gap — a partial page previously looked exactly like a complete one. **25 deterministic assertions (U4b-49..U4b-73)**, including three hostile shapes that are validly *formed* but not validly *signed* (`tampered_payload`, `substituted_root`) and a mixed page proving contamination runs in neither direction — the genuine item still reported, the forgery still refused. `U4b-68` pins the pair the old implementation could never produce: an empty history reads `0 items / 0 unverifiable`, distinguishable from a failed parse. **547 local assertions green; B-23 delta unchanged at 20, so this adds no schema surface.** **STILL OUTSTANDING: the corrected code is UNDEPLOYED**, and the live 0-vs-14 disagreement that exposed it has therefore not been re-run. This row closes only when the deployed endpoint returns a history that reconciles against `membership_notification`. Folded into U5g's already-required deliberate redeploy of `appstore_reconcile_v1` rather than carrying a known-broken diagnostic through the deploy and redeploying again later. **Superseded detail:** the original disposition below said 'NOT FIXED, and deliberately not fixed in this turn' (U5g's read-only investigation forbids production mutation, and the fix is a function redeploy). **The fix is to decode each `signedPayload` and read `notificationUUID` from the verified payload** — the verifier already exists in `_shared/appstore/jws.ts`, so this is a use of proven code rather than new machinery, and it should verify rather than merely decode. **It does NOT block U5 deployment**, and the reason is worth stating rather than assuming: U5's establishment protocol performs its own live authoritative Apple read and never consults notification history, so a blind diagnostic cannot mislead it. **What it does block is G3**, which is U4-owned and already listed as an outstanding gate. **A second, smaller gap in the same mode:** the response omits Apple's `paginationToken`, so a history longer than one page could not be followed even once the UUID bug is fixed | 3 |
| B-33 | **U3's migration comment asserts that a function referenced from an RLS policy expression needs no EXECUTE grant, and it is FALSE — implemented as written it would have denied every Connected query for every member.** The comment reads *"`connected_member()` needs no grant at all: at U6 it is evaluated inside RLS policy expressions, which run as part of the query rather than as a direct call — so it decides what clients may see while remaining unreachable BY them"*, and U3 revokes EXECUTE from public, anon, `authenticated` and `service_role` **on that basis**. **Measured on the local B-23 reproduction 2026-08-30: EXECUTE IS checked, against the INVOKING role, for functions referenced from a policy qual.** A policy consulting the ungranted `connected_member()` raises `permission denied for function connected_member` — not a mis-scoping but a **total Connected outage on the first query after U6b binds, for entitled and unentitled members alike**. **Isolated to EXECUTE by three controls and a discriminator, because the naive experiment cannot tell a privilege error from a false predicate:** a SECURITY DEFINER twin returning *true* is refused (so it is not the predicate value), a plain invoker-rights twin returning *true* is refused (so it is not a SECURITY DEFINER quirk), and granting EXECUTE on that same twin — changing nothing else — returns rows. **THE SAME MEASUREMENT CONDEMNS THE OBVIOUS FIX.** Granting the existing uuid-taking `connected_member(uuid)` to `authenticated` does make policies work, and it simultaneously makes the entitlement predicate **a membership oracle over the entire user base**: called as `authenticated` through PostgREST it answered for a uuid the caller had no relationship to. That is a shippable exception inside the function that defines paid access — **precisely what D4 rejected once already**, one level down. Inlining the SQL instead is also dead: a qual's table access is invoker-checked too, so `exists (select 1 from public.membership ...)` raises `permission denied for table membership` against U3's deliberate revokes. **THE FIRST REVISION OF THE EXPERIMENT REPORTED THE OPPOSITE, AND THAT IS THE TRANSFERABLE LESSON.** It wrote the qual as `connected_member(...) or true` so that a merely-false predicate could not be misread as a privilege error; **Postgres folds `or true` to a constant and never evaluates the function**, so it returned rows and 'proved' the comment right. **A defensive clause destroyed the only thing being measured, and it failed in the reassuring direction — agreeing with the claim it was sent to test.** | **P1** | **Confirmed defect (design assumption recorded in a deployed artefact's durable comment). CAUGHT BEFORE IMPLEMENTATION — no defective implementation has ever existed**, and it could only have been found this way: the comment is correct-sounding, `connected_member()` genuinely *is* unreachable as a direct call, and no amount of reading separates that from unreachable inside a qual. The deployed schema is **not** wrong and needs no change — U3's revokes are correct and stay; what is wrong is the sentence U6 would have implemented from. **Adopted fix, measured rather than reasoned: a ZERO-ARGUMENT `SECURITY DEFINER` wrapper over `auth.uid()`, granted to `authenticated` only**, whose body reaches the still-ungranted `connected_member(uuid)`. It has no argument to aim, so the oracle is structurally unbuildable rather than merely discouraged. `connected_member(uuid)` and `membership_state(uuid)` stay revoked from all three client roles. **A second, independent constraint rides on the same experiment:** the wrapper must be invoked as `(select wrapper())` and never bare — the bare form lands in the per-row Filter at 278ms/5000 rows against 3.9ms as an InitPlan, and **both forms are functionally correct, so review cannot catch it**. **RESOLVED 2026-09-01 BY U6a, ON BOTH HALVES OF ITS OWN STATED CONDITION.** (i) `public.connected_member_self()` is **deployed to production** — zero-argument, `SECURITY DEFINER`, `revoke`d from public/anon/`service_role` and granted to `authenticated` alone — and is consulted by all 23 observed policies, **23 of 23 wrapped as `(select …)` with ZERO bare calls, verified in the deployed catalog rather than in the tree**. `connected_member(uuid)` carries EXECUTE for no client role and `membership_state(uuid)` for `service_role` only, so the oracle stays structurally unbuildable. (ii) **The U3 migration comment is corrected in place, and the false sentence is QUOTED rather than deleted** — the thing worth recognising is a claim that is correct-sounding and cannot be falsified by reading, so removing it would destroy the warning while fixing the error. The same correction is applied to U3 acceptance's `A3h`, whose comment restated the claim; its assertion was always right and only its reason was wrong, and U3 re-ran **97/97** after the edit. **No production defect ever existed and none was fixed:** U3's revokes were correct throughout. Evidence: `supabase/tests/u6a/execute-in-policy.sh` and `predicate-shape.sh`, re-runnable, with the production posture restored and asserted at the end of each | 3 |
| B-34 | **THE U6a SHADOW WINDOW IS STRUCTURALLY BLIND ON ALL FOUR `storage.objects` WRITE POLICIES, AND THE CAUSE IS NEITHER OF THE TWO HYPOTHESES THAT WERE COMMITTED.** Observed in the production window 2026-09-01: 3 of 3 storage SELECT surfaces fired, **0 of 4 storage INSERT/UPDATE surfaces ever did**, while an object was demonstrably created in the `attachments` bucket by an authenticated owner. Two hypotheses were written down first — (a) the policy is evaluated but `auth.uid()` is NULL there, so the observer's first branch returns early; (b) the policy is not evaluated at all because `storage.objects` is not `FORCE`d and its owner bypasses RLS, which would have been **a pre-existing enforcement gap U6a merely revealed**. **BOTH ARE FALSE.** (b) is refused by experiment: with `attachments_user_insert_auth`'s `WITH CHECK` set to `false`, an authenticated upload through storage-api is **REJECTED** with *new row violates row-level security policy* — **RLS genuinely gates the storage write path**. (a) is refused by the policy's own predicate: the path check `lower((storage.foldername(name))[2]) = lower((auth.uid())::text)` **passed**, which it cannot do on a NULL uid. **THE ACTUAL MECHANISM: THE PREDICATE IS EVALUATED AND ITS SIDE EFFECTS ARE DISCARDED.** Proven with a sequence, because `nextval()` is not transactional and therefore survives a rollback: across one HTTP upload the probe sequence advanced **exactly once** (`last_value` 1 → 2, `is_called` f → t) while `shadow_enforcement_stat` gained **zero rows** and the object was created. The observer ran, and its `INSERT` was rolled back. That is consistent with storage-api performing an RLS-checked insert whose effects are discarded and then writing the real row through a path that bypasses RLS — `relforcerowsecurity` is **false** and the owner is `supabase_storage_admin`, so owner-bypass is available. Corroborated at both ends: an unconditionally-raising observer makes the upload fail **500 / P0001**, so the observer is reached; and a direct SQL `INSERT` as `authenticated` with `request.jwt.claims` set **does** persist an observation, which is the control that stops a null result being mistaken for a broken experiment. **TWO SECONDARY FINDINGS FROM THE SAME RUN.** (i) All permissive policies for the command are evaluated, so an `attachments` insert also evaluates `avatars_insert_owner_only` — the SQL control recorded **`storage.avatars_insert` AND `storage.attachments_insert` at the same microsecond** — meaning **surface labels can be misattributed and counts are policy evaluations, not accesses**. (ii) In `post_comments_select_visible` the open author branch is evaluated first, so `OR` short-circuits and **a comment author reading their own comments is permanently invisible to the window** — Q1's deliberate design, with a metric consequence nobody had stated. | **P2** | **OPEN — coverage defect in the U6a metric, NOT a production defect, and the distinction is the whole finding.** No enforcement gap exists: storage writes are RLS-gated today and will be gated by U6b. What does not exist is **evidence**. **CONSEQUENCES THAT MUST REACH U6b: (1) the shadow window can never produce a single observation about storage writes, so their absence must never be read as safety;** (2) `would_deny` counts are lower bounds, not request counts; (3) a surface's silence has at least three causes — never reached, allowed by an earlier OR branch, or observed-and-rolled-back — and only the first is what a reader assumes. **A shadow window's silence is not evidence.** Also established in the same investigation: `get_account_directory_by_user_ids` **is** instrumented, and its silence is because the client call is **cache-gated** (`AccountDirectoryService.swift:258` runs only for identities missing from the local cache), so **G10's surface is reached only on a cold cache** — which makes shadow evidence about U6b's most delicate change weak by construction rather than merely absent so far. Evidence: `supabase/tests/u6a/README-storage-write-blindspot.md`, predictions committed at `919ce6c` before measurement. **VERIFIED AGAINST A FAITHFUL LOCAL REPRODUCTION, NOT IN PRODUCTION** — the local stack is the same software, not the same deployment; the production half is the four read-only catalog facts (P-A1–P-A4), all of which held. | 3 |
| B-35 | **U6c's SNAPSHOT-REMOVAL SAFETY CHECK IS SPECIFIED ON `auth.users.last_sign_in_at`, WHICH CANNOT ANSWER THE QUESTION IT IS ASKED — AND IT FAILS OPEN.** The check, recorded in `CLAUDE.md` and QA's U6c row, reads: *any snapshot UID that has **signed in** since U6b but still lacks authoritative membership state blocks snapshot removal until dispositioned*. **GoTrue advances `last_sign_in_at` only on re-authentication, never on token refresh**, so an identity holding a long-lived session is fully active and reads as though it has not signed in for months. **MEASURED, NOT REASONED, on production 2026-09-01:** the two identities that generated the 48 U6a shadow observations that day carry `last_sign_in_at` of 2026-08-25 and **2026-08-14** — the latter three days BEFORE `cutover_at` — while `auth.users.updated_at` and their `auth.sessions` rows both moved that same day. The 16-identity census run against the column reported **0 of 15 grandfather-dependent identities active since cutover**; re-run on `max(auth.sessions.updated_at)` it reported **1**, and `auth.users.updated_at` agreed on every bucket independently. **THE FAILURE DIRECTION IS THE WHOLE FINDING: it fails OPEN.** A blind check reports *nobody active*, permits removal, and destroys the compatibility snapshot out from under identities still relying on it — the one outcome U6c exists to prevent, arriving silently and looking exactly like a pass. | **P2** | **RESOLVED 2026-09-01 IN THE SAME UNIT THAT FILED IT — documentation only, and there is no production defect to fix.** U6c has not run and the clock has not started (`u6b_bound_at` is null), so nothing was ever decided on the bad predicate. Corrected in `CLAUDE.md` §U6c and in `docs/qa-plan.md`'s U6c row: the check now keys on **`max(auth.sessions.updated_at)` per user, cross-checked against `auth.users.updated_at`**, with `last_sign_in_at` permitted as a recorded value and **never as the predicate**. **The original sentence is quoted in the correction rather than deleted**, for the same reason B-33's was: the claim is correct-sounding, `last_sign_in_at` is exactly what a reader would reach for, and removing it would fix the error while destroying the warning. **RELATIONSHIP TO B-34, because they are the same error at two levels and should be read together:** B-34 is *a shadow window's silence is not evidence*; this is *an auth column's silence is not evidence*. Both are metrics that report ABSENCE where activity exists, and in both the absent reading is the one that unblocks the next step. **Consequence for U6b entry, and it is the useful half:** the 16-identity census — an enumeration over a frozen snapshot — replaces the zero-direction inference from the shadow window entirely. It found **1 of 16 pre-cutover identities holds authoritative membership state and 15 do not**, which is a complete measurement immune to every silence mechanism in B-34. Evidence: this census, read-only, aggregates only, no UID recorded. | 3 |
| B-36 | **THE U3 CUTOVER SNAPSHOT HAS NO ENTITLEMENT PREDICATE — IT CAPTURED EVERY IDENTITY THAT EXISTED, NOT EVERY IDENTITY THAT WAS PAYING — AND THE POPULATION IS A PRE-RELEASE BETA COHORT.** The population statement is `insert into public.membership_cutover (user_id) select u.id from auth.users u where u.created_at < cutover_at`, and its own note says *pre-cutover = auth.users.created_at < cutover_at*. **No subscription or entitlement test appears anywhere in it.** So the grandfather clause is not a *protect existing payers* mechanism; it is a *don't break anyone who existed* mechanism, and it currently grants full Connected entitlement to all 16 pre-cutover identities, paid or not. **CLASSIFICATION, read-only and with no identifier leaving the database:** all 16 are Sign in with Apple, all email-confirmed, **all 16 completed onboarding** (display name and instruments), **zero test-like display names and zero test-like account IDs**, created on **14 distinct days between 2026-02-26 and 2026-08-15 with no batch clustering**. 2 are confirmed fixtures (Device A, Device B); **9 are best explained as genuine external TestFlight beta testers** — all using Hide My Email, 5 of them publishing 19 posts across spans of up to 130 days, 4 holding follow relationships; up to 5 are plausibly developer-owned and **cannot be resolved from the data**. 100 posts exist across the population and none of the 15 has ever attested. | **P2** | **RESOLVED 2026-09-01 BY A PRODUCT DECISION, NOT BY A FIX, AND THE DISTINCTION IS THE FINDING.** The account holder confirmed that **Études has never been publicly released, there are no production customers, and preserving beta accounts or their server-side content is not a release requirement.** The database could not have established that — the Production ASSN URL is unset, so a Production purchase would have been invisible either way; what it corroborates (zero Production membership rows ever, zero Production notifications, two Sandbox subscriptions in total) is merely consistent. **CONSEQUENCE: U6b Gate 2 is RETIRED rather than satisfied**, and it was **unsatisfiable by construction** — an identity that never had a subscription can never acquire authoritative membership state, and cannot attest to one that never existed. It is replaced by this single recorded decision. **The grandfather mechanism is to be retired entirely**, which collapses `connected_member()`'s middle `coalesce` arm, drops `'grandfathered'` from `membership_state()`, deadens `membership_cutover` and three `membership_control` columns, **makes U6c disappear as previously conceived** (no twelve-month clock, no liveness check, no dated post-phase obligation), moots B-35 operationally and removes G4-B2 as a gate. **That simplification is available ONLY because the cohort is disposable; a single production customer would make all of U6c necessary again, and that dependency is recorded so a later reader cannot adopt the architecture without its premise.** **STANDING RULE ADOPTED THE SAME DAY: where the identity or purpose of an existing account is relevant and uncertain, ASK THE ACCOUNT HOLDER — do not infer it from indirect evidence.** This classification was built from email shape, content and activity because the question predated the rule; it is not the pattern to repeat. Design and the prediction-first shadow experiment: `supabase/sql/README-grandfather-retirement.md`. | 3 |

## Verified sound — do not revisit

**Backend:** RLS enabled on every public table with policies.
`posts_select_public_or_owner` correct (private posts owner-only; public require
an approved follow). `connected_attachments` INSERT policy binds the storage
path to sender UID and asset ID; SELECT and UPDATE recipient-scoped. The
immutability trigger. All three comment RPCs validate properly.
`follow_requests_open` enforced. Buckets private, MIME allow-listed,
size-limited. `delete_account_v1` authorisation (subject derived from the
verified token, never the request body).

**Client:** attachment privacy fails closed; session deletion is fail-closed;
`StagingStore`'s file-backed design; all DEBUG bypasses correctly gated; no
hardcoded credentials; no analytics, crash-reporting or attribution SDK; the
three lifecycle paths (sign out / expiry / erase) are correctly separated and
cannot trigger one another.

---

## Lessons for future work

Several findings were wrong because behaviour was inferred from names and
structure rather than checked:

- A P0 was filed for cross-user storage destruction on the assumption that two
  functions named `cleanup_post_attachments_on_*` were triggers. They are
  attached to nothing.
- C-13 was filed P3 with "it terminates in practice." It cannot terminate; the
  compiler caught what a close read missed.
- An avatar-loss risk on expiry was asserted and turned out not to exist — the
  avatar is already written to the device-local namespace. **That clean answer
  then closed the subject.** It is the register's only avatar entry and it sits
  under *verified sound*; the opposite question — whether the *backend* copy is
  released when it should be, and whether a replacement reaches anyone — was
  never asked. C-33, C-34, C-35 and B-20 all came out of asking it later. The
  audit was organised by mechanism, which is why `delete_account_v1` carries
  seven findings that all land on attachments, comments, posts and follows, and
  none on step 4, and why `qa-plan` walks the attachments bucket through six
  destructive rows and the avatars bucket through none. **A clean verdict in one
  direction is not coverage of a subject.**

**Every identity field has two local scopes** — `profile.__local_etudes_profile__.*`
and `profile.<backendUserID>.*` — and four findings are now the same mistake in
that one design: C-19, C-27 and C-36 on location, C-34 on the avatar. The
recurring shape is a read or a write that picks one scope without a fallback, at
a moment when only the other scope is populated. When touching an identity
field, the question to ask first is *which scope holds this right now, and what
happens at the instant sign-in or sign-out flips the answer.*

**The Domain 3 expiry table makes six promises** (`architecture.md:76-83`). Five
now have findings or QA rows behind them. "Directory identity + avatar →
Deleted" was the sixth, and it was the one nobody had probed. Worth reading that
table as a checklist rather than as description.

**A finding read from source can be right about the mechanism and wrong about
the scope.** C-34 was derived correctly — the cache key really is
content-invariant and the `NSCache` really has no TTL — and then asserted that
every render site would serve the stale image until relaunch. Device evidence on
2026-08-11 showed two of them self-healing. The mechanism survived; the
prediction did not, because `NSCache` evicts on its own and views that are
recreated per presentation quietly refetch. **The trap is that a correct
mechanism makes the predicted consequence feel already verified.** It is not:
the consequence is a separate claim, and here it was the one that mattered,
because the sites that appear to work are the ones most likely to be spot-checked
and to produce a false all-clear. Two things came out of asking why: C-34
narrowed to the feed row, and C-37, a different site where the fetch is
unreachable by construction.

**Verify before asserting.** Cheap checks — a grep, a build, one query — have
repeatedly changed conclusions.
