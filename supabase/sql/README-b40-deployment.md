# B-40 — production deployment runbook (PREPARED, NOT EXECUTED)

**Prepared** 2026-09-15 under `claude-scope-014.md` and `codex-scope-review-014.md`.

**Status:** nothing here has run against production. Deployment needs Samuel's explicit approval after Codex reviews the artifacts and the rehearsal.

**Change:** migration `20260915150000_b40_teen_follow_requests_closed.sql` (SHA-256 `c39560b9…`). It replaces three function bodies with one read-time rule, `follow_requests_enabled AND age_band <> 'band_13_17'`:
- `account_privacy_requests_open`;
- `account_privacy_self_v1`;
- `account_privacy_upsert_v1`.

**Unchanged:**
- grants, policies and data;
- Edge functions and client;
- discovery.

**No `supabase db push`,** no other migration, no recompute.

## 1. Artifacts

| Step | File | What it does |
|---|---|---|
| Apply | `2026-09-15-b40-apply-production.sql` | The migration, verbatim, between guards |
| Rollback | `2026-09-15-b40-rollback-production.sql` | The three pre-B-40 definitions exactly as in `supabase/schema/functions.json` at `379e5c0` |

**Generation:** both are generated by `claude-evidence/scope014/gen_b40_artifacts.py`. **Never hand-edit; regenerate.**

**Submission form, required:** `supabase db query --linked "$(cat <artifact file>)"`, positional. This is the hosted route measured at CP-0 and used for the 2026-09-15 B-39/scope 011 deploy.
- **Why each file starts with `begin;`:** positional text that starts with `--` is parsed as a CLI flag.
- **Not used:** `-f`, and the local CLI.

**Every file is ONE submission:** `begin` → PRE guards → body → POST guards → `commit` → one read-only `s14_verification` row.
- **Abort semantics:** every abort-worthy check runs before `COMMIT`.
- **Scoring:** the response body only, never the exit code.
- **Success:** a verification row naming the artifact with `"committed": true`.

### Guards

**PRE:**
- exactly one function for each of the three B-40 targets and three deployed B-39/scope 011 names;
- the targets at their expected md5, `PRE_PROD` for apply and `POST_B40` for rollback;
- the deployed bodies and the `membership_state` comment unchanged.

**Stored before the body:**
- every other `public` function;
- public and storage policies;
- EXECUTE grants on the seven privacy and follow-request functions;
- `account_privacy` count plus row md5;
- `follows` count;
- `enforcement_enabled`.

**POST:**
- the targets at their expected md5 (`POST_B40` for apply, `PRE_PROD` for rollback);
- deployed bodies and comment unchanged;
- every stored value unchanged;
- the retained writer still executable by `authenticated`;
- `account_privacy_requests_open` not executable by `anon` or `authenticated`.

### Literals

| Literal | Value | Source |
|---|---|---|
| `PRE_PROD` | `account_privacy_requests_open` `f730fb8adba2011f0600f16ef6883d5d`, `account_privacy_self_v1` `1f87a5832b702c369f169217b2eaceae`, `account_privacy_upsert_v1` `13df8a41f6011aba4e68fdd5ea615065` | Hosted read, 2026-09-15 21:58:45 UTC; equal to the snapshot at `379e5c0` |
| Deployed bodies | `connected_member` `53775fd4dd46661ffd0a85d0a191c60a`, `membership_entitled_until` `962d8c937cdc03cbe7a06f2d0da0a84e`, `membership_apply_state_v1` `e77f869be62c319012203cb2fafbbe2d`; comment `2a5375a43415b878823c3d51b856624f` | Same read |
| `POST_B40` | `account_privacy_requests_open` `8ae56aad5011cb8111203f405aee7812`, `account_privacy_self_v1` `2b4384ae3a9c8e7ed241019e38720971`, `account_privacy_upsert_v1` `050c675daa5f1828295f2039e871acfe` | Rehearsal phase L; stored-body md5 cross-checked against the migration |

**Artifact SHA-256** (rehearsed in attempt `20260915T220559Z`, byte-identical in `supabase/sql/`):

```
c9af7efe434cc9c597713984a8accf43bf78069c362681da251297d07418e0d8  2026-09-15-b40-apply-production.sql
1791422dadb952d38997182b99a8834623a4bc30267ffcb6ba9abeec635840d9  2026-09-15-b40-rollback-production.sql
```

## 2. Deploy-day sequence (requires Samuel's approval; each step scored before the next)

**Preconditions:**
- the artifacts, runbook and test extension are committed at a Samuel-authorised checkpoint, and pushed;
- artifact SHA-256 values equal §1;
- `supabase/.temp/project-ref` is `rlwtqxumfobakvdueugm`.

**Steps:**
0. **Transport probe (read-only).** `supabase db query --linked "select 1 as first; select 'probe-last' as marker;"`. Expect exactly the `marker` row.
1. **Fresh read.** Repeat `claude-evidence/scope014/hosted/b40-readonly.sql`, timestamped. It must show the B-40 targets at `PRE_PROD`, deployed bodies and comment unchanged, and enforcement on.
   - **Record for Samuel:** the teen row count and the pending requests toward teens (both 0 at 21:58 UTC).
   - **If pending requests toward a teen exist,** hold for Samuel's decision. A read rule does not remove existing pending rows, and no data repair is authorised.
2. **Recapture structure** with the verbatim capture queries into a **scratch** directory, never `capture-schema.sh --linked` in the checkout. It must be byte-identical to the committed snapshot. **Any difference stops for assessment.**
3. **Apply.** Expect a `s14_verification` row with `"committed": true`. Then run a read-only inspection of the three target md5s (`POST_B40`), the deployed bodies and enforcement.
4. **Recapture:** exactly the three B-40 definitions differ from the committed snapshot. Update the snapshot record to that capture; its commit needs separate approval.
5. **Optional read-only confirmation:** repeat step 1's read. The targets should be at `POST_B40`, with unchanged counts.

**Afterwards:**
- a full local rebuild compared with the recaptured snapshot is predicted **GATE MET** (standing `account_id_format` exception only);
- the B-40 register row moves from "not deployed".

**Unaffected:** B-44 device QA uses adult accounts.

## 3. Rollback

**Rollback artifact:** its PRE requires `POST_B40`.
- **Expected:** a verification row, the targets back at `PRE_PROD`, and every stored value unchanged.
- **Then:** recapture; it must be byte-identical to the pre-deploy snapshot.

## 4. Stop rules

- **Response:** an error body, a missing verification row or an unclear response stops. Inspect state read-only before any retry, and never retry with edited SQL.
- **Drift:** any PRE md5, comment or structural drift is assessed. Guards are not weakened and production is not adjusted.
- **Pending requests toward a teen:** hold for Samuel's decision.
